# BakeIT Change Documentation

## 2026-09-13 — Brownie preparation, spreading, and sliced presentation (started)

- **Additional user request (2026-09-14):** Randomized textures on cooked brownies and more embedded walnuts. Extend the existing procedural shader with stable object-space seeded crust variation; add partially buried top fragments and exposed walnut bits on the cut faces. Keep raw batter color and interaction logic unchanged. Verify by rendering the final cut grid and moving the tray.

- **Additional request implemented:** Per-square seeded toasted patches, fine pores and crust fissures now use object-space coordinates. There are 28 partially buried top kernels and five embedded cut-face fragments per square (60 total). Final full-grid/cut-face renders inspected; texture seed remains unchanged while moving the tray. Runtime material/mesh cleanup and reset checked. Final scene saved and clean, no temporary objects, zero Console errors/warnings.

- **2026-09-14 implementation:** Added `BrowniePreparationVisual`, `BrownieFoodGeometry`, non-emissive `BrownieFood.shader`, and editor-only `BrowniePresentationQA`. Updated receiver presentation, walnut measuring contents/transfer colors, food stage/material ownership, pan spreading and slicing, HUD/highlight targets, and the rerunnable Brownie asset builder. Scene and generated assets were saved through the existing builder.
- **Visual corrections:** Removed the generic pre-mix B1 surface; retained exact ingredient identities before whisking. Fixed gray raw rendering with neutral ambient food shading while keeping #663C02. Replaced walnut powder disk with a dense shared-mesh pile; seated pan kernels per spreading surface and raised baked details to the crust. Added closed clump/spread volume, nearly full parchment footprint, a 4:3 pan footprint, and twelve solid cut squares. Replaced floating straight crack rods with procedural crust fissures.
- **Verification:** See the 2026-09-14 test entry. Public ingredient transfers, trigger-driven mixing/spreading/cutting, outward pickup orientation, final twelve-square geometry and reset were checked. Perfect-bake presentation was injected; a complete oven/user-input playthrough was not claimed.
- **Status:** Implemented and automatically checked; full player-input regression and aesthetic approval remain pending. Earlier screenshots are retained as diagnostics, with final renders clearly named `brownie-qa-final-*`.

- Requested: restore distinct Sugar, Flour, Eggs, butter and powders on the bowl floor before whisking; fix Walnut scoop geometry; use #663c02; form a thick transferred clump that spreads to parchment edges; turn the held spatula outward; make cut results resemble the supplied square Brownies.
- Inspection: Brownies currently reuse one elevated B1 mesh for every ingredient; Walnut measures use a brown powder cylinder; pan spreading changes only slab scale; slicing is not implemented. These are presentation/workflow gaps, not player errors. Console initially clean.
- Plan: extend existing receivers and food presentation, preserve measuring/recipe gates, implement 4-by-3 knife slicing after baking and pan removal, verify actual rendered preparation and spreading states as well as interaction callbacks.
- Status: in progress. Agent: Codex.

## 2026-09-13 — Ingredient front labels, pantry fills, and walnut rework

- **Requested objective:** Make the Baking Powder and Cocoa Powder names clearly visible on their models, add convincing visible Flour and Sugar contents, and rework the Walnut source/Brownie pieces from the supplied chopped-walnut reference.
- **Scene in scope:** `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- **Planned implementation:** Audit the existing front-panel label orientation and size, extend the rerunnable pantry builders with contained surface geometry, and replace smooth/paired Walnut lobes with denser, asymmetric crinkled pieces while preserving all interaction roots, colliders, ingredient IDs, quantities, and recipe progression.
- **Label correction:** The Cocoa and Baking Powder labels existed but were embedded behind each can's outer badge/decor geometry. Cocoa moved to local Z `.0615` with an `.080 x .037 m` plate and `.00315` character size; Baking Powder moved to local Z `.0575` with a `.072 x .052 m` plate and `.00215` character size. Both now render beyond the model-front bounds.
- **Flour/Sugar contents:** The Flour Tin retains its contained fill block and now has three shallow overlapping powder mounds plus a subtle scoop furrow immediately below the rim. The Sugar Jar material is brighter and has a contained three-part mound plus 18 small visible crystals; its existing transparent jar, scoop trigger, lid, and clamp remain unchanged.
- **Walnut source:** Rebuilt the source as 34 chopped pieces in three mound layers. Each piece uses one of three shared, flat-shaded, deterministically deformed kernel meshes with two raised folds and an embedded crease. Colors were shifted toward golden tan. There are no paired heart lobes, loose physics pieces, or visual colliders.
- **Brownie Walnut presentation:** Bowl/pan Walnut clusters now use seven uneven lobes plus a shallow crease, with lighter golden material values. The existing rule remains: B1 Walnut details are inactive until the sixth whisk pass.
- **Files/assets changed:** `IngredientNameLabelSetup.cs`, `PantryContainerAssetSetup.cs`, `SugarJarAssetSetup.cs`, `RemainingIngredientAssetSetup.cs`, `BrowniePanStageSetup.cs`, their generated Flour/Sugar/Walnut assets and prefabs, Brownie Walnut materials/details, and `Kitchen Prototype.unity`.
- **Visual QA:** Isolated front render confirmed readable Cocoa/Baking Powder names, visible contained Flour/Sugar surfaces, and a full glass Walnut bowl. The initial Walnut revision still resembled smooth coffee beans; it was rejected and replaced with the final faceted/deformed kernel meshes. Long crease rods in the second preview were also replaced by an embedded crease.
- **Play Mode:** `INGREDIENT_VISUAL_PLAYMODE_PASS`. Flour, Sugar, Walnuts, Cocoa, and BakingPowder retained their exact measured-source identities, Rigidbodies, and `PreparationItem` components. Labels were in front of the outer model bounds; Flour/Sugar remained below their rims; Sugar had 18 crystals; the Walnut source had 34 craggy pieces, 34 shared-mesh renderers, zero visual colliders, and no left/right heart lobes; the 84 revised Brownie lobes remained hidden in the initial state.
- **Failed attempts/corrections:** One diagnostic used deprecated `GetInstanceID` and was rerun without it. Two builder calls coincided with Unity dynamic-command assembly reloads and were rerun successfully after the reload completed. The first broad Scene-view frame was blocked by the counter fascia, so isolated `DontSaveInEditor` review clones were used and then removed.
- **Final state:** Play Mode stopped, scene saved cleanly, zero QA objects remain, and the final Console query reported zero errors and zero warnings.
- **Status:** Complete for the reported model/readability issues; normal player-distance aesthetic acceptance remains pending.
- **Performed by:** Codex via repository edits and Unity MCP.

## 2026-09-04 21:39 +08:00 Repository ignore configuration

- Requested objective: Establish Git ignore rules so generated Unity and local-development files are not committed.
- Change type: Repository configuration.
- Scene edited: None.
- Files created: `.gitignore`, `BakeIT_Notes/change-documentation.md`, `BakeIT_Notes/Current Project Status.md`, `BakeIT_Notes/version-control.md`, `BakeIT_Notes/test-log.md`, `BakeIT_Notes/known-issues.md`, and `BakeIT_Notes/design-decisions.md`.
- Files edited, renamed, or removed: None.
- Prefabs and GameObjects changed: None.
- Components or Inspector values changed: None.
- Materials, models, audio, and other Unity assets changed: None.
- Project settings or package changes: None.
- Behavior changed: Git now ignores Unity-generated caches, temporary files, builds, IDE output, local Plastic SCM metadata, local agent state, operating-system files, Blender backups, and the local manuscript. Unity source assets, `.meta` files, packages, and project settings remain trackable.
- Validation performed: Used `git check-ignore` against representative ignored and required source paths, inspected `git status --short`, counted the initial trackable file set, and reviewed its largest files.
- Validation result: Passed. `Library`, `Temp`, `Logs`, `UserSettings`, generated solution files, `.vscode`, and the manuscript are ignored. Scripts, scenes, `.meta` files, `Packages/manifest.json`, `ProjectSettings`, and project Markdown files remain trackable. The initial set contains 1,359 files; none exceeds 50 MB and the largest is approximately 4.51 MB.
- Console errors or warnings: Not applicable; no Unity runtime behavior changed.
- Failed attempts or rollbacks: None.
- Known limitations: Large binary Unity assets are handled by regular Git. Git LFS is not currently required by file size, but may be useful if repository growth becomes excessive.
- Follow-up work: Review the first staged file set before the initial commit and configure Git LFS only if required by asset sizes or hosting limits.
- Performed by: Codex through local repository tooling.

## 2026-09-04 22:01 +08:00 Git staging error correction

- Requested objective: Diagnose the Git permission error shown while preparing the Unity repository.
- Change type: Repository configuration correction.
- Scene edited: None.
- Files created: None.
- Files edited: `.gitignore`, `.gitattributes`, `BakeIT_Notes/version-control.md`, `BakeIT_Notes/change-documentation.md`, and `BakeIT_Notes/test-log.md`.
- Prefabs and GameObjects changed: None.
- Components or Inspector values changed: None.
- Materials, models, audio, and other Unity assets changed: None.
- Project settings or package changes: None.
- Behavior changed: Nested `.plastic` workspace metadata is now ignored, and detected text files use LF consistently so Windows does not emit LF-to-CRLF conversion warnings for Unity text assets.
- Validation performed: Confirmed the reported `Temp/FSTimeGet-*` path matches the `Temp` ignore rule, confirmed no generated directory was already tracked, ran a complete `git add --dry-run .`, checked `.plastic` and `Temp` ignore matches, and inspected Git attributes on a Unity ProjectSettings asset.
- Validation result: Passed. The dry run exits with code 0 over 1,355 files and contains no warning, error, fatal, `Temp`, or `.plastic` lines. Unity asset attributes resolve to `text=auto` and `eol=lf`.
- Console errors or warnings: None in the final Git dry run.
- Failed attempts or rollbacks: The original `.plastic` rule only matched a repository-root directory and did not exclude the nested Unity project metadata; it was replaced with a recursive rule.
- Known limitations: The repository files remain unstaged; staging and committing are intentionally left to the user.
- Follow-up work: Refresh the Git client, stage from the repository root, review the staged files, and create the initial commit.
- Performed by: Codex through local repository tooling.

## 2026-09-04 22:10 +08:00 Confirmed prototype defect correction initiated

- Requested objective: Fix the confirmed BakeIT prototype defects while preserving deliberate testing values and leaving planned recipe, mode, scoring, cloud, dashboard, and VR work for later development.
- Current evidence: Unity MCP is connected with no Console warnings or errors. `Kitchen Prototype` is the active saved scene but is absent from Build Settings. `Prop_Tray` has a configured dough receiver but no parchment receiver. The existing oven door interaction is active and working independently, while `OvenController` has no serialized reference to that sibling door. The manuscript still lists pandesal instead of brownies and Unity 2022 LTS instead of the Unity 6 project line.
- Intended scene work: Add `Kitchen Prototype` to Build Settings; add a parchment placement trigger and snap point to `Prop_Tray`; connect the existing `FreeStove_Door` component to `OvenController` without modifying the door interaction; save the scene and project settings.
- Intended manuscript work: Preserve the document's structure and premise while changing the approved recipe scope to chocolate-chip cookies, brownies, and cupcakes and aligning the implementation statement with Unity 6.
- Intended validation: Compile, inspect the resulting hierarchy and serialized references, run focused Edit Mode invariants, perform Play Mode smoke and interaction-state tests through Unity, review final Console output, and render and visually inspect every manuscript page.
- Status: In progress.
- Performed by: Codex through Unity MCP and local document tooling.

## 2026-09-04 22:34 +08:00 Confirmed prototype defect correction completed

- Requested objective: Fix the confirmed BakeIT prototype defects, preserve the working oven door and deliberate test-friendly tuning, and realign the manuscript without treating planned systems as broken features.
- Change type: Scene configuration, build configuration, manuscript alignment, verification, and project documentation.
- Scene edited: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Files created: `BakeIT_Notes/tray-and-parchment-system.md` and `BakeIT_Notes/oven-and-temperature-system.md`.
- Files edited: `ProjectSettings/EditorBuildSettings.asset`, `BakeITCapstoneManuscript.docx`, `BakeIT_Notes/Current Project Status.md`, `BakeIT_Notes/known-issues.md`, `BakeIT_Notes/design-decisions.md`, `BakeIT_Notes/test-log.md`, and this change document.
- Temporary files removed: The Editor-only `BakeITTemporaryPlayModeVerifier.cs`, its `.meta` file, and the empty `Assets/BakeIT/Editor` folder metadata. No temporary verification code remains in the project.
- GameObjects added: `Prop_Tray/ParchmentSnapPoint` and `Prop_Tray/ParchmentPlacementZone`.
- Components and Inspector values changed: Added a trigger `BoxCollider` and `TrayLinerReceiver` to `ParchmentPlacementZone`; assigned its `placementPoint` to `ParchmentSnapPoint`; assigned the existing `OvenBakeZone` and existing `FreeStove_Door` `OvenDoorController` to the two previously null `OvenController` references.
- Project settings changed: Replaced the enabled `Assets/Scenes/SampleScene.unity` build entry with the active `Assets/BakeIT/Scenes/Kitchen Prototype.unity`. Incidental Sentis and App UI settings serialized by Unity were removed from the diff.
- Manuscript changed: Replaced the outdated recipe list with chocolate-chip cookies, brownies, and cupcakes and replaced `Unity 2022 LTS` with `Unity 6`. The document's overall structure, research premise, and pagination were retained.
- Behavior changed: Parchment now snaps to and moves with the tray; the existing dough receiver now enforces parchment-first placement; the baking controller now evaluates the existing working oven door and bake zone; builds start from the prototype scene.
- Behavior intentionally unchanged: Oven-door dragging, prototype bake duration, interaction distances, temperature values, current generic dough flow, and all planned recipe, mode, scoring, VR, cloud, and dashboard work.
- Validation performed: Focused Play Mode state-machine verification, post-Play serialized-reference and scene invariants, build-scene inspection, final Unity Console query, DOCX structural assertions, 110-page PDF/PNG rendering, changed-page visual review, page-group review, and automated blank-page and edge-collision checks.
- Validation result: Passed. Dough was rejected before parchment, parchment snapped and stabilized, dough was accepted afterward, the oven references the working closed door and bake zone, the active scene is saved and is the sole enabled build scene, and the final Unity Console contains 0 errors and 0 warnings. The manuscript remained 110 pages with both requested alignment changes present and no detected render defects.
- Failed attempts or rollbacks: A background Play Mode probe did not advance sufficiently; a temporary verifier was used instead. Deprecated calls in the verifier were replaced before its passing run. A private-field dynamic check was corrected to serialized-property inspection. MCP reconnected after a domain reload. MCP asset deletion requiring UI interaction was replaced with targeted file removal and refresh. The packaged DOCX renderer could not find LibreOffice, so Microsoft Word PDF export plus Poppler rendering was used. Incidental Unity package-setting serialization was rolled back from the final diff.
- Known limitations: No persistent automated tests were added. The complete user-driven recipe flow, recipe data architecture, guided modes, scoring, VR, cloud integration, and instructor dashboard remain future work.
- Follow-up work: Implement the recipe definition and recipe-session foundation, map the current interactions into the chocolate-chip-cookie workflow, then add brownies and cupcakes before modes, scoring, VR, and cloud/dashboard integration.
- Status: Complete.
- Performed by: Codex through Unity MCP and local document tooling.

## 2026-09-04 22:51 +08:00 Chocolate-chip-cookie dough portioning initiated

- Requested objective: Add a physical way to divide the prepared dough into approximately four or more evenly sized cookie portions, with the yield proportional to the represented dough quantity.
- Scene being edited: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Existing system evidence: The bowl activates one `Dough` object, the preparation board changes it to `FlattenedDough`, the tray currently accepts one whole flattened object, and the oven currently evaluates only that single object. No portioning system or cutting tool is configured in the scene. Licensed knife and pizza-cutter prefabs are available in the existing asset pack.
- Pre-change Console evidence: 13 errors and 10 warnings were present after recent manual workflow interaction. Errors came from trigger-exit callbacks dereferencing consumed or destroyed ingredients in `DoughBoardController` and `TrayReceiver`; warnings came from assigning velocity after Rigidbodies were already kinematic in the bowl, board, and parchment transitions.
- Intended implementation: Reuse an existing knife prefab as a grabbable dough-cutting tool; portion flattened dough when the tool passes through the board work zone; calculate yield from represented flattened-dough volume against a calibrated four-cookie reference batch; create equal-volume cookie portions; allow the tray to collect the complete batch in an even grid; and bake all collected portions together.
- Intended stability work: Add destroyed-object guards to trigger-exit paths and avoid unsupported velocity writes on kinematic Rigidbodies.
- Intended validation: Compile, inspect serialized scene relationships, run Play Mode tests for invalid early cutting, default four-cookie yield, scaled-batch yield, equal portion volume, pickup/release, parchment-first tray placement, complete-batch oven readiness, and multi-cookie result propagation; then check the final Console and record all results.
- Status: In progress.
- Performed by: Codex through Unity MCP and local repository tooling.

## 2026-09-05 17:35 +08:00 Chocolate-chip-cookie dough portioning completed

- Requested objective: Focus on one complete dish first and add a physical way to divide prepared dough into approximately four or more evenly sized cookies, with yield proportional to dough quantity.
- Change type: Prototype interaction, batch-state integration, stability correction, Play Mode verification, and project documentation.
- Scene edited: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Files created: `Assets/BakeIT/Scripts/Interactions/DoughPortion.cs`, `Assets/BakeIT/Scripts/Interactions/DoughPortioningTool.cs`, their Unity `.meta` files, and `BakeIT_Notes/chocolate-chip-cookies.md`.
- Files edited: `DoughBoardController.cs`, `TrayReceiver.cs`, `OvenBakeZone.cs`, `OvenController.cs`, `TrayLinerReceiver.cs`, `BowlReceiver.cs`, `Current Project Status.md`, `tray-and-parchment-system.md`, `oven-and-temperature-system.md`, `known-issues.md`, `design-decisions.md`, `test-log.md`, and this change document.
- Temporary files removed: The Editor-only dough-portioning verifier, its `.meta`, and the empty `Assets/BakeIT/Editor` folder metadata. No temporary verification code remains.
- GameObject added: `Prop_DoughPortioningKnife`, instantiated from the licensed `Prop_Knife_06` prefab at world position `(-0.43, 1.18, 5.65)`.
- Knife components and Inspector values: Added `DoughPortioningTool`, `PickupPose` with held Euler angles `(90, 0, 0)`, and a `Rigidbody` with mass `0.35`, gravity enabled, interpolation enabled, and continuous dynamic collision detection. The existing mesh collider is convex.
- Board Inspector values added: Reference volume `0.00255`, reference yield `4`, minimum yield `4`, maximum yield `9`, board spacing `0.14`, and height offset `0.012`.
- Tray Inspector value added: Portion spacing `(0.17, 0.13)`.
- Behavior changed: A knife pass through fully flattened dough creates one batch of equal `CookieDoughPortion` objects. Yield is the rounded reference yield multiplied by rendered-volume ratio and clamped from four to nine. Portions preserve combined represented volume, retain pickup physics, and are arranged on the board.
- Tray behavior changed: The tray rejects whole flattened dough, requires parchment, accepts only matching portions from one batch, arranges all portions in a grid, and reports ready only when the batch is complete.
- Oven behavior changed: Baking requires a complete cookie batch and applies the selected underbaked, baked, or burnt cookie result to every loaded portion.
- Stability corrections: Added safe destroyed-object guards to board and tray trigger exits and stopped assigning velocity after Rigidbodies became kinematic in bowl, board, tray, and liner transitions. `FLOW-001` and `PHYS-001` are resolved.
- Project-settings cleanup: Retained `Kitchen Prototype` as the sole enabled build scene while removing incidental App UI config serialization and the `SENTIS_ANALYTICS_ENABLED` define from this work.
- Validation performed: Focused Play Mode tests for empty-board cutting, three-pass flattening, reference and scaled yields, equality and combined volume, required Rigidbody/collider components, parchment-first batch loading, oven acceptance, six-second perfect bake, and all-cookie result propagation; static serialized-scene and Build Settings inspection; temporary-test removal; final Unity compilation and assembly reload; repository whitespace and status checks.
- Validation result: Passed. The reference batch produced four equal cookies and the 1.25-times scaled batch produced eight. Both batches preserved rendered volume. A complete four-cookie batch loaded and all four became `BakedCookie`. Unity's final compilation and assembly reload succeeded without C# diagnostics, and the final Unity MCP Console query returned 0 errors and 0 warnings.
- Failed attempts or corrections: Background Play Mode initially did not advance enough frames. Explicit editor stepping corrected timing. The automated tray callback was late despite physical overlap, so the harness verified bounds overlap before deterministic trigger dispatch. A broad static component-name check initially confused `DoughPortioningTool` with saved runtime `DoughPortion`; exact identifier matching confirmed zero saved runtime portions. Two direct MCP probe calls used the display tool name instead of its registered transport name and logged tool-not-found errors; those self-created entries were cleared before the final successful Console query. Incidental Unity package-setting serialization and trailing whitespace on newly serialized scene components were removed.
- Known limitations: One knife pass currently auto-divides the full batch; yield is visual-volume-based rather than gram-based; the tray is capped at nine; the bowl still lacks the complete chocolate-chip-cookie ingredient set and validation; no persistent automated tests were added.
- Follow-up work: Complete chocolate-chip-cookie ingredient requirements and chocolate-chip inclusion, add a reliable sequence reset, and run a full player-driven start-to-finish acceptance test before generalizing recipe architecture or beginning another dish.
- Status: Complete.
- Performed by: Codex through Unity MCP, Play Mode verification, and local repository tooling.

## 2026-09-05 17:55 +08:00 Chocolate-chip ingredient container initiated

- Request: Create the chocolate-chip ingredient container and integrate it into the first cookie pastry sequence.
- Scene: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Initial inspection: Flour and sugar already use `MeasuredIngredientSource` and the shared `MeasuringScoop`. No chocolate-chip source or visual exists. The bowl currently completes with only flour and egg and silently counts other inputs. Initial live Unity Console: 0 errors, 0 warnings; active scene saved and outside Play Mode.
- Intended changes: Reuse an existing open kitchen container asset; configure a chocolate-chip measuring zone; add reusable modeled chip clusters and label; extend scoop and bowl visuals; require the five existing cookie ingredients at tunable prototype quantities; retain chocolate appearance through rolling, portioning, and baking.
- Intended files: Ingredient and interaction scripts, chip visual mesh/material/prefab, `Kitchen Prototype`, relevant recipe/system notes and development trackers. No package or baking-timing changes intended.
- Intended validation: Live Play Mode scoop pickup, fill and transfer; incorrect/duplicate inputs; missing-chip mix rejection; complete mixing, four-cookie portioning, tray loading, oven result; visual capture; scene references and final Console.
- Status: In progress. Responsible: Codex.

## 2026-09-05 23:03 +08:00 Chocolate-chip ingredient container completed

- Requested objective: Create the chocolate-chip container and finish the first pastry as one functional prototype sequence.
- Change type: Scene and prefab creation, measured-ingredient integration, exact recipe validation, visual-state preservation, Play Mode verification, and project documentation.
- Scene edited: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Files created: `Assets/BakeIT/Art/ChocolateChipCluster.asset`, `Assets/BakeIT/Materials/ChocolateChipMaterial.mat`, `Assets/BakeIT/Prefabs/ChocolateChipCluster.prefab`, `Assets/BakeIT/Prefabs/ChocolateChipContainer.prefab`, `Assets/BakeIT/Scripts/Ingredients/ChocolateChipVisual.cs`, all corresponding Unity `.meta` files, and `BakeIT_Notes/ingredient-measuring-system.md`.
- Files edited: `BowlReceiver.cs`, `MeasuringScoop.cs`, `DoughBoardController.cs`, `OvenController.cs`, `Kitchen Prototype.unity`, `chocolate-chip-cookies.md`, `Current Project Status.md`, `design-decisions.md`, `known-issues.md`, `oven-and-temperature-system.md`, `test-log.md`, and this change document.
- GameObject added: `ChocolateChipContainer` at world position `(-1.16, 1.1613476, 6.017256)`, instantiated from a reusable prefab whose root is normalized to zero position, identity rotation, and unit scale.
- Container configuration: Reused the licensed `Prop_Bowel_01` mesh; added a non-convex shell collider, `ChocolateChipScoopZone` trigger, `MeasuredIngredientSource` configured as `ChocolateChips`, three collider-free decorative chip layers, and a player-facing `CHOCOLATE CHIPS` label.
- Shared scoop configuration: Added a serialized `ChocolateChipContents` child reference. Chip measures use the modeled chip cluster while flour and sugar continue to use the existing powder visual.
- Bowl behavior changed: The cookie batch now requires exactly one flour scoop, one sugar scoop, one butter portion, one egg, and one chocolate-chip scoop. Unsupported, duplicate, and post-completion inputs are rejected before the scoop or whole ingredient is consumed. Four valid whisk passes create chip-bearing dough only after all five requirements are met.
- Portioning and oven behavior changed: Renderers marked with `ChocolateChipVisual` are excluded from dough-volume yield calculations and from dough/bake recoloring. Every portion therefore keeps one visible brown chip cluster without changing the four-cookie reference yield.
- Validation performed: Final prefab and scene-reference inspection; a fresh deterministic Play Mode run through the real source, scoop, bowl, board, portioning, parchment, tray, bake-zone, and oven callbacks; exact ingredient and rejection assertions; four-cookie yield and chip-marker assertions; perfect-temperature bake verification; post-Play saved-scene inspection; Editor-log C# diagnostic check; repository whitespace check; and final Unity Console query.
- Validation result: Passed. The source filled the scoop with the chip visual; the bowl accepted one measure and retained rejected inputs; the exact five-ingredient recipe mixed; the reference batch produced four chip-bearing portions; the prepared tray baked at `180 C`; every portion became `BakedCookie`; chip colors remained unchanged; the active scene was saved; the final Editor-log window contained no C# diagnostics; and the final Unity Console contained 0 errors and 0 warnings.
- Failed attempts or corrections: Initial setup commands were interrupted by expected domain reloads and resumed after MCP reconnection. A Unity 6.5 inspection call using obsolete `GetInstanceID` was changed to `GetEntityId`. A dynamic setup script's ambiguous `Mesh` type was changed to `UnityEngine.Mesh`. The first visual pass showed an oversized container and backwards label, so the scene placement, scale, and label rotation were corrected. The verifier could not use reflection in the dynamic-command sandbox, so it used `SendMessage` and serialized inspection to exercise the same callbacks. One malformed Console request created a tool-only conversion entry, which was superseded by the correct string-filtered query. The first baked-cookie screenshot used an unsuitable Game-camera angle; the final Edit Mode container view was used for visual QA. The prefab initially retained its scene transform, so its root was normalized while preserving the scene instance's intended size and position.
- Temporary files removed: All ignored `.codex` setup, inspection, layout, Play Mode verifier, mode-control, focus, normalization, and preview files created for this pass. No temporary verification code remains in the Unity project.
- Project settings or package changes: None intentionally made for this feature.
- Known limitations: Interaction quantities are prototype units rather than grams; sources do not deplete; feedback is Console-only; the sequence requires a fresh Play session to reset; and a final player-driven usability pass is still needed for reach, label readability, and gesture feel.
- Follow-up work: Add a reliable sequence reset and player-facing recipe feedback, then complete a manual start-to-finish usability pass before generalizing recipe architecture or beginning another pastry.
- Status: Complete.
- Performed by: Codex through Unity MCP, Play Mode verification, and local repository tooling.

## 2026-09-05 23:12 +08:00 First-pastry finalization audit correction initiated

- Requested objective: Continue finalizing the first pastry while following `Codex.md`.
- Scene being reviewed: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Audit finding: The container and source-to-bake integration passed, but the existing oven outcome model jumped directly from `Perfect` to `Burnt`. `Codex.md` requires a distinct `Overcooked` outcome and a complete recipe definition. The bowl's material changes also require a dedicated system note.
- Acceptance finding: The deterministic Play Mode workflow verifies the real runtime callbacks and state transitions, but it does not substitute for the required final player-facing pickup, reachability, and gesture-feel pass. Native Unity window control is not exposed to this session, so that acceptance item must remain open rather than being represented as complete.
- Intended changes: Add a tunable overcooked temperature band and visual result; test all four bake outcomes without altering the short prototype duration; complete the recipe/tool/outcome documentation; add the bowl-and-mixing system note; link resolved issues to this development log; and correct current-status wording.
- Project settings or package changes intended: None.
- Status: In progress.
- Performed by: Codex through Unity MCP, local repository tooling, and independent agent review.

## 2026-09-06 09:29 +08:00 Manuscript realignment and cookie acceptance work initiated

- Requested objective: Treat `Codex.md` as the hard-rule authority, realign `BakeITCapstoneManuscript.docx` with the implementation guide without reintroducing stale scope, then finish the chocolate-chip-cookie vertical slice.
- Current evidence: The repository is clean at `BakeITV0.0.2`. The cookie sequence is callback-verified through all four bake outcomes, but `FLOW-002` remains open, feedback is primarily Console-based, and player-facing usability acceptance remains incomplete. The earlier first-pastry audit entry was not closed even though its overcooked-result implementation and focused tests are reflected in code and system notes.
- Documentation work planned: Correct the implementation guide's stale Unity and recipe lines; align the manuscript's implementation-facing scope, recipe data, event logging, modes, architecture, and Appendix H tables with the corrected guide and the three current objectives; preserve the manuscript's academic structure and avoid inventing evaluation results.
- Unity work planned: Inspect the current scene and scripts, add a sequence-level reset that restores the cookie workflow without leaving Play Mode, add readable player-facing recipe feedback, preserve the existing interaction architecture, compile, run focused reset and regression checks, and prepare a short human usability checklist for subjective reachability and gesture-feel acceptance.
- Expected files and assets: `BakeITCapstoneManuscript.docx`, `BakeIT_Unity_Capstone_Guide.md`, cookie interaction scripts, `Kitchen Prototype`, and the applicable files in `BakeIT_Notes`.
- Pre-change verification: `Kitchen Prototype` is open in Unity 6000.5.0f1. The recent Editor log contains no project-script errors or warnings. Native Unity window automation is unavailable in this session, so technical verification will use project/editor tooling and the final subjective feel check will remain explicitly assigned to a human player.
- Status: In progress.
- Performed by: Codex through local document, repository, and Unity project tooling.

## 2026-09-06 10:03 +08:00 Manuscript realignment and cookie session completion

- Requested objective: Treat `Codex.md` as the hard-rule authority, realign `BakeITCapstoneManuscript.docx` to the implementation guide without reviving stale scope, and finish the remaining implementable work for the first chocolate-chip-cookie vertical slice.
- Change type: Manuscript and guide alignment, Appendix H table repair, runtime recipe feedback, scene-level workflow reset, input robustness, focused Play Mode verification, and project documentation.
- Scene edited: None. `Assets/BakeIT/Scenes/Kitchen Prototype.unity` was loaded for isolated verification but no serialized scene changes were made.
- Manuscript and guide changes: Updated the guide to Unity 6 and the approved chocolate-chip-cookie, brownie, and cupcake scope. Aligned the manuscript's recipe-data commitment, Meta XR wording, current three-objective research matrix, six-instrument summary, three-objective statistical treatment, and security-claim language. Repaired Appendix H's legacy overlapping table layout without changing the manuscript's academic structure or inventing study results.
- Files created: `Assets/BakeIT/Scripts/Recipes/RecipeFeedback.cs`, `Assets/BakeIT/Scripts/Recipes/CookieRecipeSessionController.cs`, their Unity `.meta` files, `BakeIT_Notes/recipe-session-and-feedback.md`, and `BakeIT_Notes/cookie-usability-acceptance-checklist.md`.
- Files edited: `BakeITCapstoneManuscript.docx`, `BakeIT_Unity_Capstone_Guide.md`, the cookie ingredient/board/tray/oven interaction scripts, `FirstPersonController.cs`, `PickupController.cs`, applicable BakeIT system notes, current status, issue tracker, design decisions, test log, and this change document.
- Prefabs and saved GameObjects changed: None. The session controller creates the transient `BakeIT Recipe Session` GameObject at runtime and persists it only across the in-Play scene reload.
- Components and Inspector values changed: No saved components or Inspector values changed. Existing serialized ingredient, mixing, portioning, tray, oven, and prototype timing values were preserved.
- Materials, models, audio, and other Unity assets changed: None.
- Project settings or package changes: None.
- Behavior changed: A persistent recipe-session controller now presents the latest cookie instruction or validation feedback in a player-facing HUD. Pressing `R` or its restart button reloads the saved active scene while remaining in Play Mode, which clears runtime cookie objects and restores every serialized ingredient, bowl, board, tray, and oven state consistently.
- Feedback integration: Expected player mistakes and progress messages now flow through `RecipeFeedback` for both the HUD and Console. Genuine configuration faults still use warning-level logging; expected invalid recipe actions do not pollute Unity warnings.
- Robustness correction: Keyboard and mouse access in the first-person and pickup controllers now tolerates unavailable input devices, resolving the null-reference failure exposed by headless verification.
- Validation performed: DOCX structural assertions; final 103-page PDF/PNG rendering; visual review of all pages and full-resolution review of changed Appendix H pages; blank-page and page-edge checks; isolated Play Mode reset/feedback verifier; temporary-verifier removal; two isolated production compiles; repository whitespace and diff checks.
- Validation result: Passed for manuscript structure/rendering and technical cookie behavior. The reset verifier confirmed runtime cleanup, saved-object restoration, cleared bowl and oven state, and restored initial feedback. The final isolated compile exited successfully with no project C# errors or warnings.
- Failed attempts and corrections: The first reset verifier exposed null input-device assumptions, which were fixed and verified in a second run. The first manuscript render exposed inherited negative cell indents and unformatted appended rows in Appendix H, so the affected tables were normalized and the complete document was rendered and reviewed again. An isolated Unity clone emitted one UnityEditor Search indexing exception during initialization; it was not a project-script compile diagnostic and the final production compilation exited successfully.
- Temporary files removed: The isolated Unity copies, verification scripts and logs, portable document-rendering runtime, DOCX backups, render pages, PDFs, and contact sheets created for this session. No temporary verification code remains in the production Unity project.
- Audit closure: The 2026-09-05 23:12 first-pastry audit's four-outcome implementation was already present and tested; this completion closes its remaining reset and player-feedback work while preserving the required human acceptance gate.
- Known limitation: Native Unity-window control was unavailable, so subjective reachability, pickup comfort, label readability, gesture feel, and HUD comfort were not claimed as tested. The first pastry must remain pending final human acceptance until `cookie-usability-acceptance-checklist.md` is completed.
- Remaining follow-up work: Run and record the human cookie usability checklist. If it passes, mark the first recipe complete and generalize the proven workflow into data-driven recipe definitions before implementing brownies.
- Status: Complete for authorized implementation and technical verification; human usability acceptance pending.
- Performed by: Codex through local document, repository, and isolated Unity verification tooling.

## 2026-09-06 10:17 +08:00 Cookie guide checklist redesign initiated

- Requested objective: Replace the confusing top-left action log with a compact recipe to-do guide that checks off ingredients and advances through mixing, board work, portioning, tray preparation, transfer, and baking without covering the upper half of the screen.
- Scene being edited: `Assets/BakeIT/Scenes/Kitchen Prototype.unity` only if the requested rolling-pass value requires a serialized change.
- Existing behavior: `CookieRecipeSessionController` displays the most recent `RecipeFeedback` message, so correct actions overwrite the panel with narration instead of showing the learner what remains.
- User-test finding: The panel behaves like an action log and is more confusing than helpful. Ingredient completion, current stage, and the next required action are not visible together.
- Intended implementation: Read authoritative state from the existing bowl, board, tray, oven-zone, oven, and oven-door components; show only the current phase; use checked/unchecked ingredient rows during preparation; show numeric progress for whisking, rolling, portion transfer, and baking; retain warnings briefly as a compact correction line; and keep restart controls in a small footer.
- Intended tuning change: Increase the saved rolling requirement from three to four passes, matching the requested guide progression while retaining the established four-cookie reference yield.
- Architecture decision: The guide will observe existing systems rather than introduce a second recipe-state manager or infer progress from action-log strings.
- Validation planned: Compile in an isolated Unity copy; run a focused Play Mode state-progression verifier; inspect the final panel layout where possible; update the test log, current status, affected system notes, issue tracker, and checklist.
- Status: In progress.
- Performed by: Codex through local project and isolated Unity verification tooling.

## 2026-09-06 21:12 +08:00 Spoon fill containment and dough-return recovery initiated

- User request: Correct sparse/overflowing chocolate-chip spoon visuals shown in the three 21:05-21:06 screenshots, restrict chocolate chips to `Small Measuring Spoon`, and prevent uncut dough becoming unusable after pickup from the board.
- Scene in scope: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Initial inspection: Unity 6000.5.0f1 is connected through Unity MCP in Edit Mode; the saved scene is clean and the Console contains no warnings/errors. `_01` has much smaller cup geometry than the prior fallback's fixed chip radius, height, and three offset layers. Both utensils currently accept chips. Existing dirty notes and the tracked prior review project will be preserved.
- Planned changes: Explicit chip-tool eligibility, a dense contained fill fitted to the small spoon's cup, clear wrong-tool feedback, and recovery of dough placement with preparation progress preserved. Extend existing measuring, board, and pickup systems.
- Validation planned: Actual saved-scene Play Mode interaction checks, spoon geometry/visual inspection, wrong-tool rejection, three post-whisk chip transfers, raw/flattened dough pickup and return, subsequent cutting/tray continuation, compilation and Console checks.
- Initial diagnostic failure: A read-only Unity MCP command used obsolete `GetInstanceID` and was rejected at compilation; retried without that call successfully. No scene change occurred.
- Work distribution: Main Codex agent owns measuring visuals/eligibility, scene configuration, Unity verification, and logs; a parallel Codex agent investigates and edits only dough/board/pickup recovery scripts.
- Assets/settings: No package, project-setting, model, or recipe-scope changes planned.
- Status: In progress; final values, test results, limitations, and changed files will be recorded on completion.
- Performed by: Codex agents through local source edits and Unity MCP.

## 2026-09-06 15:53 +08:00 Small-scoop chocolate-chip pickup correction initiated

- Requested objective: Fix the small measuring scoop failing to collect chocolate chips during the player test.
- Scene being edited: None expected; the reusable chocolate-chip source prefab and shared measuring scripts are the intended targets.
- Runtime evidence: The current Unity log contains an earlier successful `Scoop filled with ChocolateChips` event, followed in the latest base-dough sequence by repeated `The base dough is already mixed. Add chocolate chips next.` corrections and no new chip-fill event. This indicates the scoop retained a rejected base ingredient, while `MeasuringScoop.TryFill` silently refuses to fill any non-empty scoop.
- Intended correction: Let a different ingredient source replace stale scoop contents; retry source filling during trigger stay as well as initial entry; enlarge the chocolate-chip source trigger above the visible container opening; and clarify the base-dough correction.
- Validation planned: Focused Play Mode verification with a flour-filled scoop entering the chip source, retained overlap/refill behavior, visual mound activation, and three post-mix transfers; final isolated production compile and documentation checks.
- Status: In progress.
- Performed by: Codex through local project logs and isolated Unity verification tooling.

## 2026-09-06 20:57 +08:00 Small measuring spoon and chocolate-chip pickup correction completed

- Requested objective: Make the player's intended `MeasuringSpoon_01` collect chocolate chips and rename it `Small Measuring Spoon` to avoid confusion.
- Change type: Scene-tool correction, measured-source recovery, trigger usability tuning, runtime contents visual, guide wording, Play Mode verification, and documentation.
- Scene edited: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Files created in production: None.
- Files edited: `MeasuringScoop.cs`, `MeasuredIngredientSource.cs`, `BowlReceiver.cs`, `CookieRecipeSessionController.cs`, `ChocolateChipContainer.prefab`, `Kitchen Prototype.unity`, `ingredient-measuring-system.md`, `recipe-session-and-feedback.md`, `chocolate-chip-cookies.md`, `cookie-usability-acceptance-checklist.md`, `Current Project Status.md`, `known-issues.md`, `design-decisions.md`, `test-log.md`, and this change document.
- Prefabs and GameObjects changed: The existing `Prop_MeasuringSpoon_01` scene instance was renamed `Small Measuring Spoon` and received the existing `MeasuringScoop` component. The previously configured `_04` scene instance was renamed from `MeasuringScoop` to `Large Measuring Spoon`. The reusable `ChocolateChipContainer/ChocolateChipScoopZone` trigger was enlarged; no licensed vendor prefab was renamed or overwritten.
- Components added or removed: Added one `MeasuringScoop` component to the `_01` scene instance. No component was removed. Its existing Rigidbody, convex MeshCollider, and `PickupPose` were preserved.
- Inspector values changed: `ChocolateChipScoopZone` local BoxCollider size changed from `(0.22, 0.18, 0.22)` to `(0.30, 0.28, 0.30)` and center from `(0, 0.13, 0)` to `(0, 0.16, 0)`. The new small-spoon component starts empty and has no serialized contents references; it generates its visual-only contents at runtime.
- Code behavior changed: A measuring spoon now rejects only a duplicate fill from the same source. A different source replaces its existing ingredient and reports the change. Measured sources try on both `OnTriggerEnter` and `OnTriggerStay`, so a just-emptied spoon already inside a source can refill. The post-base correction explicitly tells the player to dip the small spoon into the chip container.
- Visual behavior changed: A compatible spoon without authored contents children receives a collider-free powder disk and an 18-piece chocolate-chip cluster at runtime. The existing three-layer mound logic duplicates that cluster into three active layers for a visibly full chip spoon. Generated visual colliders are disabled immediately and destroyed; pickup physics remain on the licensed spoon collider and Rigidbody.
- Guide behavior changed: The controls tutorial now says `Aim at small measuring spoon + press E`.
- Validation performed: Scene/prefab YAML inspection; isolated Unity compilation; focused Unity Play Mode test using the saved scene and real source callbacks; a verifier-free isolated compilation; repository whitespace and documentation review.
- Validation result: Passed technically. `BAKEIT_SCOOP_REFILL_VERIFIER_PASS` and the XML result recorded `1/1` passed. The final production-code compile exited with code `0` and no `error CS`, `warning CS`, or `Scripts have compiler errors` diagnostics.
- Failed attempts and corrections: A first execute-method Play Mode harness stalled after an unrelated UnityEditor Search index exception and its exact isolated process was stopped. Native computer-use access exposed no Unity window, so no UI action was attempted. The Unity Test Framework was then used; the first two invocations included `-quit` and compiled but exited before running tests, so the final supported invocation omitted `-quit` and passed. A concurrent user commit (`BakeITV0.0.3`) captured the isolated review project while the test was running; after detecting the new commit, Codex restored the review test files and its project-setting value instead of deleting or overwriting committed user state.
- Temporary files: The isolated test source now remains under the committed `CodexReview/UnityScoopRefill_20260906_1605` review project because it became part of the user's concurrent commit. Its generated `Library` remains Git-ignored, and it is separate from the production `BakeITCapstoneProject`. Recursive cleanup was not performed after the environment blocked the operation and the directory was found in the new commit.
- Project settings or package changes: None in production.
- Known limitation: Headless Play Mode verifies state and contents activation but cannot judge the spoon's exact visual seating, dip comfort, or pickup feel. The player must stop and re-enter Play Mode and physically retest `Small Measuring Spoon`.
- Remaining follow-up work: Player retest the small spoon through flour, sugar, four-pass base whisking, and three chocolate-chip transfers; report whether the pickup zone or visual needs further tuning.
- Status: Technical fix complete; physical player acceptance pending.
- Performed by: Codex through local repository tooling, isolated Unity Play Mode testing, and documented fallback after unavailable native UI control.

## 2026-09-06 15:46 +08:00 Tutorial, chip-order, and completion correction implemented

- Requested objective: Prevent post-bake guide regression; add a small desktop tutorial before ingredients; whisk base dough before adding chips; and make the chocolate-chip measure feel substantially fuller.
- Change type: Recipe ordering, tutorial guidance, completion-state correction, chip quantity/visual adjustment, focused Play Mode verification, and documentation.
- Scene edited: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Files created: None in the production project.
- Files edited: `CookieRecipeSessionController.cs`, `PickupController.cs`, `BowlReceiver.cs`, `MeasuringScoop.cs`, `Kitchen Prototype.unity`, `Current Project Status.md`, `chocolate-chip-cookies.md`, `bowl-and-mixing-system.md`, `ingredient-measuring-system.md`, `recipe-session-and-feedback.md`, `cookie-usability-acceptance-checklist.md`, `design-decisions.md`, `known-issues.md`, `test-log.md`, and this change document.
- Prefabs and saved GameObjects changed: None.
- Components and Inspector values changed: `BowlReceiver.requiredChocolateChipScoops` on `IngredientTrigger` changed from `1` to `3`. No other saved Inspector value changed.
- Materials, models, audio, project settings, and packages changed: None.
- Tutorial behavior: `PickupController` exposes its currently held Rigidbody read-only. The session controller observes `W A S D`, mouse movement, and pickup of the existing measuring scoop, presents them as three checked tutorial tasks, and advances to ingredients after all three. Tutorial completion remains learned across an in-Play recipe restart but resets on a new Play Mode session.
- Recipe-order behavior: The bowl now accepts flour, sugar, butter, and egg as the base. Four whisk passes form a visible base-dough mound without activating the finished dough object. Only then are chocolate chips accepted; the third chip scoop activates finished `Dough`. A premature chip scoop is emptied without incrementing the bowl so the only shared scoop cannot deadlock the recipe.
- Chip visual behavior: Filling the measuring scoop creates and displays three overlapping, collider-free instances of the existing `ChocolateChipContents` cluster. The in-bowl chip mound scales upward across the three accepted transfers. Pickup colliders and Rigidbody behavior are unchanged.
- Completion behavior: The session controller checks and latches `OvenController.BakeCompleted` before all ingredient, tray, and oven branches and stores the completed result. Tray movement or later live-oven state changes therefore cannot return the guide to `LOAD OVEN`; `R` or the restart button clears the latch.
- Secondary correction: `BowlReceiver` ignores its own `mixtureResult` when the finished dough activates inside the trigger, preventing a false post-completion ingredient warning.
- Guide progression: Ten stages—basic controls, prepare base, whisk base dough, add three chip scoops, move to board, flatten, portion, prepare tray, load oven, and bake—followed by a latched completion panel.
- Validation performed: Initial isolated Unity compile; focused isolated Play Mode verifier using the real bowl trigger callbacks and saved scene objects; follow-up verifier after the self-result guard; verifier removal; final verifier-free isolated compile; and repository/documentation checks.
- Validation result: Passed. `BAKEIT_TUTORIAL_CHIP_COMPLETION_VERIFIER_PASS` confirmed the three tutorial items, early-chip recovery, exact base requirements, fourth-pass base-dough threshold, three visible scoop layers, third-scoop completion, guide transitions, and persistent completion latch. The follow-up run produced no false self-result correction. The final compile exited successfully with no project C# errors or warnings.
- Failed attempts and corrections: The first passing verifier also captured a physics-trigger warning caused by the newly activated dough re-entering its own bowl. The explicit self-result guard removed it in the follow-up pass. The headless verifier completed its assertions but did not terminate after leaving Play Mode, so only the isolated verifier processes were stopped after their pass markers and logs were written; the subsequent verifier-free compile exited normally.
- Temporary files removed: The isolated Unity project, temporary Editor verifier and metadata, and compile/verifier logs were deleted after results were recorded. No temporary verifier remains in production.
- Known limitation: Automated verification cannot judge whether the fuller mound looks natural, whether the tutorial wording is comfortable at the current Game-view scale, or whether three manual chip transfers feel appropriate. These remain player-facing acceptance items.
- Remaining follow-up work: Stop and re-enter Play Mode, then test the tutorial, deliberately try chips early, complete four whisk passes and three chip transfers, finish baking, move the tray, and confirm the result remains on screen.
- Status: Implementation and technical verification complete; player usability retest pending.
- Performed by: Codex through local project and isolated Unity verification tooling.

## 2026-09-06 14:57 +08:00 Cookie guide checklist redesign implemented

- Requested objective: Replace the confusing action-log panel with an uncluttered recipe to-do list that checks off preparation ingredients and then guides whisking, board transfer, flattening, four-part portioning, parchment, tray transfer, and oven work.
- Change type: Player-guidance redesign, state exposure, one serialized tuning change, focused Play Mode verification, and documentation.
- Scene edited: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Files created: None in the production project.
- Files edited: `CookieRecipeSessionController.cs`, `BowlReceiver.cs`, `DoughBoardController.cs`, `Kitchen Prototype.unity`, `Current Project Status.md`, `recipe-session-and-feedback.md`, `chocolate-chip-cookies.md`, `cookie-usability-acceptance-checklist.md`, `design-decisions.md`, `known-issues.md`, `test-log.md`, and this change document.
- Prefabs and saved GameObjects changed: None.
- Components and Inspector values changed: `DoughBoardController.requiredRollingPasses` on `DoughWorkZone` changed from `3` to `4`. No other saved Inspector value changed.
- Materials, models, audio, and other Unity assets changed: None.
- Project settings or package changes: None.
- Code behavior changed: `BowlReceiver` and `DoughBoardController` expose read-only recipe progress. The session controller derives eight stages from the bowl, board, tray, oven zone, oven, and door instead of treating feedback text as recipe state. Successful actions no longer replace the guide. Warnings appear as a temporary 4.5-second correction while the current checklist remains visible.
- UI layout changed: The panel is capped at 410 pixels wide. Its largest normal state is the five-row ingredient checklist at approximately 218 pixels tall; a temporary correction increases it to approximately 260 pixels. Later stages progressively disclose only one to three immediate tasks.
- Guide progression: Prepare five ingredients; complete four whisk passes; move dough to the board; complete four rolling passes; portion the reference batch into four; place parchment and transfer every portion; load the oven; then set `170-190 C`, close the door, start baking, and view the result.
- Validation performed: Two isolated compilations, an eight-stage Play Mode guide-state verifier, successful-action suppression and corrective-message checks, a focused four-pass rolling verifier, temporary-verifier removal, and a final isolated production compile.
- Validation result: Passed technically. The guide verifier logged `BAKEIT_COOKIE_GUIDE_VERIFIER_PASS`; the rolling verifier logged `BAKEIT_FOUR_ROLLING_PASSES_VERIFIER_PASS`; and the final production compile exited without C# errors or warnings.
- Failed attempts and corrections: The first compile referenced the non-existent enum name `OvenBakeResult.Undercooked`; it was corrected to the existing `Underbaked` value. Unity's isolated headless verifier altered one temporary package-cache asset during shutdown, but that copy was disposable and no production package file changed.
- Temporary files removed: The isolated Unity project, verifier scripts and metadata, compilation logs, and runtime test objects created for this change. No temporary Editor test remains in the production project.
- Known limitation: Automated state verification cannot judge whether the panel is visually comfortable during real movement. `UI-001` remains open at “fix implemented” until the player confirms readability and that the panel does not cover too much of the Game view.
- Remaining follow-up work: Retest the guide from ingredient preparation through the oven phase and report any stage that advances too early, any unclear wording, or any remaining obstruction.
- Status: Implementation and technical verification complete; player retest pending.
- Performed by: Codex through local project and isolated Unity verification tooling.

## 2026-09-06 15:13 +08:00 Cookie guide readability enlargement

- Requested objective: Make the staged recipe guide approximately 30-40% larger after the player's screenshot showed that it remained too small at the QHD Game-view scale.
- Change type: Player-facing UI sizing adjustment and documentation.
- Scene edited: None.
- Files created: None in the production project.
- Files edited: `CookieRecipeSessionController.cs`, `Current Project Status.md`, `recipe-session-and-feedback.md`, `cookie-usability-acceptance-checklist.md`, `known-issues.md`, `test-log.md`, and this change document.
- Prefabs, saved GameObjects, components, and Inspector values changed: None.
- Materials, models, audio, project settings, and packages changed: None.
- Code behavior changed: Added a uniform `1.35` guide scale and applied it to panel placement, width, height, padding, row spacing, correction area, footer, button, and every font size. Recipe logic and stage progression are unchanged.
- UI layout changed: Maximum width increased from `410` to approximately `554` pixels. The five-item preparation stage increased from approximately `218` to `294` pixels tall normally and from approximately `260` to `351` pixels with a temporary correction. At the supplied 2560x1440 Game resolution, the normal panel remains well below half the screen height.
- Validation performed: Isolated Unity 6000.5.0f1 production compilation and source/dimension review.
- Validation result: Passed. Unity exited batch mode successfully with no project C# errors or warnings.
- Temporary files removed: The isolated Unity compilation project and its log were deleted after verification. No temporary project code remains.
- Known limitation: Headless compilation cannot judge final perceived size at the editor's `0.63x` Game-view display scale. Player visual confirmation remains required.
- Remaining follow-up work: Retest the opening ingredient stage at the same QHD Game-view scale and confirm that it is readable without obscuring important workstation content.
- Status: Sizing implementation and compilation complete; player visual retest pending.
- Performed by: Codex through local repository and isolated Unity verification tooling.

## 2026-09-06 15:31 +08:00 Tutorial, chip-order, and completion correction initiated

- Requested objective: Keep the guide on its completed result after baking; add a short desktop-control tutorial before ingredient work; whisk the flour, sugar, butter, and egg into base dough before accepting chocolate chips; and make the chip measure feel full through both a denser scoop visual and three small-scoop transfers.
- Scene being edited: `Assets/BakeIT/Scenes/Kitchen Prototype.unity` for the saved chocolate-chip requirement only.
- User-test findings: The completed guide can regress to the oven-loading instruction, the experience begins with recipe ingredients without teaching basic movement or pickup, the chip amount appears countable and sparse, and the current recipe order accepts chips before whisking.
- Intended code changes: Latch the oven result before tray-state branches; observe basic movement, mouse look, and measuring-scoop pickup for a compact tutorial; split bowl progression into base preparation, four-pass whisking, and post-mix chip addition; require three chip scoops; and create a three-layer runtime chip mound in the measuring scoop.
- Prefab and material plan: Preserve the source prefab and materials. Reuse its collider-free chip-cluster visual as overlapping runtime layers inside the existing measuring scoop.
- Validation planned: Isolated Unity compile; focused Play Mode verification of tutorial presentation, chip-order rejection, four-pass base mixing, three chip transfers, full scoop layers, guide progression, and completion latching; final diff and documentation checks; player visual/interaction retest remains required.
- Status: In progress.
- Performed by: Codex through local project and isolated Unity verification tooling.

## 2026-09-06 21:37 +08:00 Contained small-spoon fill and uncut-dough recovery implemented

- User objective: Correct sparse and overflowing chip fills, restrict chocolate chips to the small measuring spoon, and recover dough picked up before cutting. Evidence: the three supplied 21:05-21:06 screenshots.
- Scene edited: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Production files edited: `Assets/BakeIT/Scripts/Ingredients/MeasuringScoop.cs`, `Assets/BakeIT/Scripts/Interactions/DoughBoardController.cs`, and the scene above.
- Documentation edited: `Current Project Status.md`, `ingredient-measuring-system.md`, `chocolate-chip-cookies.md`, `recipe-session-and-feedback.md`, `cookie-usability-acceptance-checklist.md`, `known-issues.md`, `design-decisions.md`, `test-log.md`, and this append-only change log.
- Files created: `BakeIT_Notes/dough-board-and-portioning-system.md` documents the existing system; `BakeIT_Notes/evidence/small-spoon-contained-fill-2026-09-06.png` is the final Unity camera render. No production scripts, components, prefabs, or models were added or removed.
- Saved GameObjects configured: `Small Measuring Spoon` (`Prop_MeasuringSpoon_01`) and `Large Measuring Spoon` (`_04`). Their existing names, model scale, pickup colliders, and Rigidbodies were preserved.
- Inspector changes: Added explicit `acceptsChocolateChips=1` and `chocolateChipContentsPrefab=ChocolateChipCluster.prefab` on the small spoon; added explicit `acceptsChocolateChips=0` and null chip prefab on the large spoon. Added `cupInnerRadius=0.0205`, `cupFillBottom=0.005`, and `cupFillSurface=0.023`. New `chipMoundHeight` uses its `0.005` code default. Previously both spoons accepted chips without any eligibility setting.
- Visual changes: Replaced the small spoon's oversized 18-sphere fallback and three offset copies with eight instances of the existing 576-vertex chip-cluster mesh/material, paired across four heights. Normalize the mesh's non-centered pivot and actual radial footprint. Final chips fit local radius `0.0205` and Y `0.010-0.028`: a contained fill with tips about `0.0043` above the actual rim. Small powder fill changed from center Y `0.0375`, scale `(0.06,0.009,0.06)` to center Y `0.014`, scale `(0.041,0.009,0.041)`. The large spoon's old chip child stays hidden.
- Measuring behavior: Chip attempts on the large spoon give a throttled player correction and preserve existing flour/sugar. Existing stale-ingredient replacement, retained-overlap retry, bowl ordering, and three-chip-scoop requirement remain functional.
- Dough behavior: Board placement accepts both `Dough` and `FlattenedDough`; same-slab rolling count, partial distance, original scale, and target scale survive pickup. Returning restores the exact board-local target. Trigger stay catches released overlapping dough without stealing held dough. Board occupancy reflects actual parenting, off-board slabs cannot be worked by board tools, and portions are excluded from slab placement.
- Assets and project settings: Reused existing mesh and chocolate material without editing them. No vendor-prefab, package, project-setting, recipe-count, bake-timing, or input-binding changes. The scene file is saved; final active scene is clean in Edit Mode, with `PlayerSettings.runInBackground=false`, original spoon layer `0`, and no QA camera remaining.
- Tests: Connected Unity 6000.5.0f1 Play Mode tested actual ray pickup/source overlap, stale replacement, retained-overlap refill, wrong-tool rejection, exact visual bounds, eight-layer emptying, early-chip recovery, four-pass base mixing, three chip transfers, partial dough pickup/return at `2/4`, flattened return after rotation, three repeat return cycles without scale drift, actual knife portioning, parchment-first loading, full tray readiness, and successful baking of all four recovered portions at `180 C`.
- Results: Technical and rendered-visual checks passed; `FINAL_RECOVERED_COOKIE_BATCH_PERFECT_BAKE_PASS` confirms downstream recovery through baking. Final Unity Console query returned zero errors/warnings; `git diff --check` passed. See the matching 21:37 test-log entry for exact steps and method limits.
- Failed attempts/adjustments: An obsolete diagnostic API and unauthorized reflection import were rejected before execution. Stale in-memory scene data required explicit scene reimport/reopen. Background Play Mode initially did not advance until temporarily enabled. A Rigidbody-only test teleport was replaced with verified transform movement and physics synchronization. Four-cluster and vertically compressed visual trials were rejected after renders; the final eight-cluster packing preserved chip shape and looked full. Two connector camera captures failed; direct Unity RenderTexture capture succeeded. One multi-file documentation patch failed an exact-context check before making any changes and was reapplied with correct context.
- Temporary work: Runtime-only test objects, camera, layer changes, and test state were discarded by exiting Play Mode. Several preview PNGs remain in the OS temporary folder; only the final evidence render is added to the repository. No new review project or persistent verifier was created. The earlier tracked `CodexReview` copy and all pre-existing working-tree changes were preserved.
- Agent work: A parallel Codex agent implemented the board-only change; the main agent reviewed it and performed all unified Unity tests. An optional follow-up agent review hit a usage limit; the main agent completed inspection and verification directly.
- Known limitations/follow-up: The tests combine actual physics with deterministic calls to existing gameplay callbacks; they do not replace a full user-controlled pass. Player confirmation of apparent fill size, reach, rotation/scroll comfort, obstruction recovery, and guide readability remains required. Retest both spoons and pick up/return an uncut slab before continuing to bake.
- Status: Requested fixes implemented, saved, documented, and verified in Play Mode; final human usability acceptance remains open under `Codex.md`.
- Performed by: Codex through local source/scene edits, Unity MCP, and a parallel board-fix agent.

## 2026-09-06 21:50 +08:00 Kitchen layout, fridge storage, and mise en place initiated

- User objective: Modestly polish and declutter the existing kitchen, move chips clear of the board, give the board a brown wood appearance, make the fridge usable for ingredient/object storage, introduce practical mise en place before final cookie polish, and improve the oven's box-like controls while keeping a game-like presentation.
- Scene in scope: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`; existing licensed kitchen models and cookie systems are retained.
- Initial state: Repository and active scene are clean. Unity MCP reports no Console errors/warnings. The fridge has separate correctly pivoted door meshes but only one large solid root collider and no storage behavior. Ingredients/board overlap near the left of an otherwise mostly empty preparation counter. Oven controls already have dedicated marker components and can be reskinned without replacing bake logic.
- Planned implementation: Separate pantry/tool storage, mixing, rolling/cutting, and tray zones; use egg/butter fridge storage and preparation gathering; provide labeled shelves/slots and compact game-like cues; fit a wood-toned board material; redesign the existing oven control faces; preserve recipe quantities, counts, timings, and recovery paths.
- Work distribution: Main agent owns scene layout/materials/oven presentation, documentation, and all Unity tests. A parallel agent owns fridge/storage runtime scripts and scoped pickup integration. Additional independent guide work may be delegated after API inspection.
- Validation plan: Inspect actual geometry and materials, render the layout and fridge, test storage opening/retrieval/return and setup guidance, check object access/collider separation, run the cookie workflow and a restart, query final Console, and record before/after scene values. Player environment/handling acceptance remains required.
- Status: In progress.
- Performed by: Codex using local source edits and Unity MCP.

## 2026-09-07 09:20 +08:00 Kitchen layout and mise-en-place technical handoff

- Objective: Finish the user's modest kitchen declutter/wood-board/storage/oven-control request before final cookie polish. Continued the 2026-09-06 21:50 entry; no recipe-scope expansion.
- Scene edited/saved: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- New production scripts: `Interactions/FridgeDoorController.cs`, `Interactions/IngredientStorageSlot.cs`, `Recipes/MiseEnPlaceStation.cs`, with metadata. Edited `PickupController.cs` to dispatch targeted fridge use before pickup/release; edited `CookieRecipeSessionController.cs` to observe the four-item setup stage and number eleven phases.
- New authoring/art assets: `Editor/KitchenPolishSetup.cs` and Editor folder metadata; `Art/PrepWood.shader`, `Art/KitchenWorldText.shader`; seven `Materials/Kitchen_*.mat` assets (Slate, Cream, Amber, Sage, Plaster, PrepWood, WorldText), all with metadata. The editor menu is explicit/one-time and refuses to overwrite an existing authored layout. It is excluded from player builds.
- Hierarchy/layout: Repositioned existing bowl, flour, sugar, chip source, board, pin, knife, tray, parchment, spoons, whisk, sideboard, cup and lid. Moved the board away from chips and created pantry/MIX, ROLL & CUT, and LINE & LOAD homes. Added grouped station badges, low pantry labels, supported MIX mats, tool-home sign/mat, rear prep wall/trim/sign, and warm fill. Exact before/after root Transforms and active state: `evidence/kitchen-layout-transforms-2026-09-07.json`. Full child positions, component values, colors, and light settings: `kitchen-layout-and-storage.md`.
- Fridge changes: Disabled (retained) the old solid root BoxCollider, added a static hollow-shell MeshCollider, and added tight boxes/kinematic Rigidbodies/door controllers to the existing two door meshes. Configured +/-100-degree opening, 110 degrees/s, 3-degree maximum physics step, .002 obstruction tolerance, and 2s correction throttle. Added four shelf support boxes, typed egg/butter return slots and snap points, labels, and an interior light. Initial egg/butter bodies are kinematic/no-gravity at their shelf poses; no vendor-prefab edit.
- Storage behavior: E opens/closes/reverses the targeted fridge door even while carrying an ingredient. Moving doors check player/item obstruction instead of pushing. Closed solid doors block pickup. Retrieval clears slot ownership and cannot be immediately stolen back; a valid open-slot return preserves scale, releases safely, and stabilizes the food. Spare shelves support physical loose-object drop/pickup. This is not an unlimited inventory or temperature simulation.
- Mise en place: New observation region center `(-1.68,1.43,5.90)`, size `(1.92,.70,1.35)`, references the original egg/butter/spoon/whisk and bowl. Prepared flags latch on entry or accepted whole-food use, preventing early-consumption deadlock without duplicating recipe state. Guide adds phase 2/11 and an E interaction hint; existing 135% scale, recipe quantities, counts, and completion latch remain.
- Oven: Reused current buttons, marker components, controller, display, door and stove. Replaced tiny cube faces with rounded built-in sphere faces, enlarged hitboxes, cream minus/plus and amber BAKE labels, slate bezel and inset screen. Final player-facing order is minus then plus. The unused duplicate `Prop_Oven_01` is inactive rather than removed; the existing sideboard occupies its space and can be restored through the recorded original pose.
- Visual decisions/iterations: Board uses brown procedural fine grain, not a new model/bitmap. Initial world text bled through fridge doors; introduced depth-tested font material and verified closed/open renders. Initial .75 fridge light was reduced to .3. Added support colliders to MIX mats after noticing possible released-item clipping. Swapped the first plus/minus visual order. Default scene serialization reorders some records and explicitly saves the existing chip mound defaults; cookie tuning was not changed.
- Documentation created: `kitchen-layout-and-storage.md`; five kitchen evidence PNGs and transform-audit JSON. Updated current status, measuring, bowl/mixing, board, tray/parchment, oven, cookie, recipe-session, usability checklist, test log, known issues, design decisions, and this append-only log.
- Tests/results: Connected Unity 6000.5.0f1 Play Mode passed ray-operated fridge opening while empty-handed/holding egg; stored-food ray pickup and return after actual exit/reentry; wrong-slot rejection; closed-door pickup blocking; player-blocked close and automatic resume once clear; four gathered-object ray pickups; settled mats; spare-shelf large-spoon storage/retrieval; real input-state tutorial progression to mise en place; all setup flags and next phase; actual relocated chip/board/knife/parchment trigger contact; four-pass flattened pickup/rotation/return with exact scale/progress; four complete tray portions; new oven-control ray hit areas; 180 -> 190 -> 180 C; rejected empty start; successful four-cookie 180 C bake; completion latch after tray removal; and full scene restart with food restored/doors closed/setup cleared. See the dated test-log entry for precise method limits.
- Failed tooling attempts: This continuation had two diagnostic lookups mistakenly assuming a root Renderer/Collider on the chip container; corrected to its child components. A key delta-state test was rejected because keyboard keys are bitfields; whole KeyboardState events succeeded and were released afterward. A PowerShell brace-path query failed parsing and was replaced with an ordinary recursive rg path. None of these tool failures were treated as passing tests. No production runtime exception was observed; final Play/Edit Console queries returned zero errors/warnings and both new shaders had zero compiler messages.
- Cleanup/review: Temporary runtime placement, camera, input, and player-controller test states were discarded by exiting Play Mode. Restored `PlayerSettings.runInBackground=false`, saved assets/scene, checked missing scripts, and reviewed pending repository changes without committing. Only generated evidence previews remain; no new review project or permanent test harness was created. Unity-generated trailing scene whitespace was normalized mechanically before final diff review.
- Settings/packages: No net project settings, package, binding, Build Settings, recipe requirement, or licensed asset changes. No material user data was deleted. The inactive duplicate oven and original chip label remain recoverable in the scene.
- Limits/next task: This is a technical/visual implementation handoff, not final player acceptance under `Codex.md`. Several recipe transitions were driven by their real callbacks and the tray was placed into the oven zone by the test command. Full hand-carried door passage/tray insertion, all rotation/scroll/crouch/edge cases, small-label reading, low-resolution HUD comfort, subjective appearance, and VR remain to be accepted by the player. Prioritize that kitchen/mise-en-place pass, then final cookie polish.
- Performed by: Codex main agent through local edits and Unity MCP; earlier bounded agents supplied fridge/storage and setup-guide code, reviewed and tested by the main agent. No additional delegation was started during this continuation.

## 2026-09-07 09:45 +08:00 Preparation-board staging and dry storage correction initiated

- User correction: Store flour/sugar inside the existing sliding cabinet, chips in the fridge, label ingredients themselves, and prepare everything on the board before mixing. Stabilize near actual release position rather than teleporting to prescribed slots; replace arrow/MIX wording.
- Inspected: Cabinet has two overlapping door meshes and an internal shelf near local Y .403, but a solid root box; sources have no Rigidbodies; setup currently counts hover and early consumption. Scene is clean in Edit Mode; Console zero errors/warnings. Previous uncommitted changes are preserved.
- Planned: Sliding-door E dispatch, hollow cabinet collision, carryable existing containers, safe source colliders, release-only near-surface stabilization, board-based preparation, attached labels, guidance and pre-mixing validation. Preserve recipe counts, oven and models. Test storage, release accuracy, scooping, progression/reset and renders.
- Performed by: Codex via local edits/Unity MCP; in progress. Initial multi-file patch was rejected before any changes because it attempted delete/add on the same path; reapplied using an update.

## 2026-09-07 14:15 +08:00 Storage-first board preparation implemented

- User objective: Correct first-pass mise en place: dry ingredients in the side-sliding cabinet, chips in the fridge, all supplies prepared on the board before mixing, stable near-release placement and item-attached names. Work continued after the user's continue message.
- Scene saved: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`. Existing scene, source/container/board/furniture models, original recipe requirements, oven values and prior uncommitted work were preserved.
- New files/components: `Interactions/PreparationItem.cs`, `StablePlacementSurface.cs`, `SlidingCabinetDoor.cs`, `Editor/PreparationStorageSetup.cs`, with metadata. Edited `PickupController`, `MiseEnPlaceStation`, `CookieRecipeSessionController`, `BowlReceiver`, `DoughBoardController`, and the earlier `Editor/KitchenPolishSetup` to keep serialized field access consistent. No duplicate recipe manager.
- Cabinet scene changes: existing broad BoxCollider disabled; static hollow MeshCollider added. Existing `_p02`/`_p03` meshes receive tight BoxColliders, kinematic Rigidbodies and local slide offsets `(-.74,0,0)` / `(.74,0,0)` at speed .65. Added internal support at local `(0,.397,-.015)`, size `(1.48,.012,.56)`. Sources move from former counter positions to cabinet-local `(-.4,.408,.09)` / `(.4,.408,.09)` (world `(1.112,.408,3.09)` / `(1.688,.408,3.09)`). Added forward, cream-colored E/Ctrl cabinet hint.
- Supply scene changes: flour/sugar/chip source roots now have Rigidbodies plus PreparationItem; existing egg/butter/small-spoon/whisk also marked. Seven initial bodies kinematic/no-gravity, mass .2, speculative detection. Flour/sugar meshes are convex. Chip bowl moves from `(-1.43,1.16135,6.28)` to fridge `(-2.35,1.544,3.13)`; static bowl MeshCollider disabled, replaced by bottom plus eight low box walls so the moving bowl stays hollow/scoopable. Exact collider values are in `kitchen-layout-and-storage.md`.
- Board/rest changes: board scale `(2.5,2.5,2.5)` -> `(3.5,2.5,2.5)`; thickness/depth preserved and four-cookie yield verified. Eight StablePlacementSurfaces on board, two mats, four fridge supports and cabinet shelf. Release preserves X/Z/yaw, settles resting pitch/roll and height within .22 m, refuses incomplete footprint or >.002 penetration, then holds kinematically at that point. No horizontal snapping or reparenting; pickup remains reversible.
- Removed behavior, not data: both old IngredientStorageSlot components and their triggers are disabled, as is the broad MIX hover trigger. These scene objects remain recoverable. Counter ingredient labels and shelf egg/butter labels are inactive. New labels are children on both sides of egg/butter/bags/chip bowl, using the existing depth-tested font material. Pickup excludes text/empty-renderer bounds so labels do not move carry pivots.
- Preparation/recipe changes: all seven supplies must be physically resting on the prep board together before readiness latches. Before readiness, lifting an item unchecks it; hovering or early food consumption cannot bypass setup. Bowl rejects early additions non-destructively with a throttled correction. Guide phase 2 uses explicit board task plus five ingredient-location rows, then two tool rows; phase 6 explains clearing staged containers/tools before dough. Board defers placement until clear and retains its previous recovery logic. Existing amounts, four whisk/roll passes, three chip scoops, completion latch, reset and oven behavior remain.
- Tests: connected Play Mode verified closed/sliding access, ray pickups, exact zero-XZ-delta releases, occupied/high refusal, egg rotated return/scale, cabinet shelf return/obstruction, delayed actual carry motion/rotation/scroll, real transported-source filling for all three sources, whole-container/early-bowl rejection, all-seven guide progression, clear-board behavior, four-cookie Perfect bake and full storage/reset restoration. Final Console returned zero errors/warnings; carrier collider audit found no enabled non-convex meshes and no missing scripts. See the 14:15 test entry for exact methods/limits.
- Iteration/failed attempts: Initial patch format rejected atomically; corrected. Single-sided item labels were hidden from the prep viewpoint; reverse-side labels fixed that in the final render. Cabinet hint contrast/position improved. Initial broad collider audit also found the pre-existing stationary FreeStove non-convex/kinematic combination; documented for future physics/VR review without unrelated changes. Scene serializer whitespace normalized before final diff review.
- Notes updated: current status, kitchen/storage, ingredient measuring, bowl/mixing, board/portioning, cookie, recipe-session, acceptance checklist, known issues, design decisions, test log and this chronological log. New evidence: prepared-board and guide, dry cabinet and revised fridge previews; an earlier prepared-board-review PNG retains the label iteration evidence.
- Settings/packages/version control: no net project setting, package, input binding, build scene, vendor model/material or new recipe change. Scene/assets saved, runtime cameras/test placements discarded, runInBackground restored false. Pending changes reviewed; no commit or destructive reset. Disabled older slot/label data is retained and recoverable.
- Status: Requested implementation and technical/visual checks complete; full player acceptance remains pending. Retest natural cabinet/fridge carrying paths, free placement/rotation/edge drops, small label readability, board clearing, dough recovery and the full cookie bake before further polish. No VR support claim.
- Performed by: Codex through local source edits and Unity MCP; no subagents used for this correction.
- Final repository review: HEAD is now `e431fa0` (`BakeITV0.2.3`), which already includes part of the earlier source work; Codex did not create that commit. Remaining scene/ready-state/pivot/documentation edits, authoring script, generated metadata and evidence are left as pending changes. No baseline changes were reset or overwritten. Final whitespace-normalized `git diff --check` passed and there is no net ProjectSettings diff.

## 2026-09-07 19:21 +08:00 Separate preparation/mixing workflow initiated

- User request: prep → mix → chopping board; ingredients on Cold Ingredient Rest, tools on Mix Tool Rest; carryable/placeable bowl starting on a holder; three further whisk passes after chips. Record cleaning first, as future work only.
- Inspected current scripts, notes, scene and asset catalog. Preserve unsaved user mat edits: Cold Ingredient Rest position `(-1.902,1.168,6.008)`, scale `(1.277,.012,1.207)`; Mix Tool Rest position `(-.995,1.167,6.022)`, scale `(.412,.010,1.151)`. Scene starts dirty; do not reload/discard these edits.
- Partial existing functionality: reversible stable placement and seven-supply preparation, but readiness and HUD incorrectly target the chopping board; bowl is static and third chip scoop immediately creates dough.
- Console baseline: six Unity Connect/AI account token-exchange errors; unrelated service stack traces, no game script error observed. Do not log token contents or change account settings.
- Added `cleaning-and-workstation-readiness.md` as planned-only design notes before gameplay edits. Next: extend existing placement/preparation/mixing systems, reuse a suitable holder asset, save and test in Play Mode including incorrect placements and delayed final dough. No packages/new recipes planned.
- Performed by Codex via local edits/Unity MCP; in progress.

## 2026-09-07 19:40 +08:00 Prep → mix → chopping-board implementation and technical handoff

- Objective: latest user correction; continue the 19:21 initiation. Cleaning documented first as planned-only; no cleaning control or readiness gate implemented.
- Files: edited BowlReceiver.cs, MiseEnPlaceStation.cs, CookieRecipeSessionController.cs; added Editor/WorkstationFlowSetup.cs + Unity metadata. Kitchen Prototype scene saved. Prior user/agent changes preserved; no vendor prefab/model/material file edited.
- User transforms preserved exactly: Cold Ingredient Rest position(-1.90150,1.168,6.00840), scale(1.27701,.012,1.20682); Mix Tool Rest position(-.99520,1.167,6.02170), scale(.41184,.01,1.15099). They were initially unsaved and are now included in the saved scene.
- Layout: new slate Mix Bowl Rest center(-.4,1.167,5.91), size(.56,.01,.92), from existing supported mat. Board X.05→.45; pin X.04→.45; knife X.57→1.02; other axes/scales unchanged. Existing station labels/backplates adjusted to separate ingredient/MIX/board homes; tool hint now says TOOLS TO MIX TOOL REST. Exact geometry and widths are in kitchen-layout-and-storage.md.
- Holder assets: reused Prop_TrayHolder at(-3.5,0,4.12), unit scale with static hollow mesh collision; reused Prop_Tray as shelf at(-3.5,1.045,4.12), scale(1.4,1,1.7). Bowl initial pose moves from(-1.72,1.16135,5.70) to(-3.5,1.056,4.12), unit scale. Original shelf BoxCollider now supplies a thin stable plane.
- Bowl physics: disabled original static MeshCollider, added .35 kg kinematic/no-gravity speculative Rigidbody and PreparationItem; .13/.016/.13 base plus twelve .052/.076/.013 wall boxes at radius.091/Y.05/30-degree steps. Hollow opening remains accessible. Added PreparationItem to large spoon. Nine carriers, ten supports; old fixed slots remain disabled.
- Preparation: existing manager now references ingredientRest/toolRest/bowlRest and largeSpoon/bowl. Five ingredients on ingredient mat, three mixing tools on tool mat, bowl in MIX must coexist before latch. Correct pickup/consumption afterward does not undo prep. Bowl lifting pauses additions/whisking; contents and scale persist. No duplicate manager or automatic horizontal teleport.
- Recipe/UI: requiredChipMixPasses3 and separate count; final scoop resets whisk tracking, not dough completion. Bowl-local whisk tracking; third final pass spawns dough. Four base passes/three chip scoops/four rolls/oven requirements unchanged. Guide now12 phases with ingredient→tools→bowl setup substeps and final chip-mixing count. Board is for dough, with clearing only as accidental-obstruction recovery. Existing135% HUD and completion latch retained.
- Iterations/failures: corrected duplicate shelf support to reuse original disabled box; removed only the newly added redundant component. Aligned badge backgrounds after first render. Test-only no-held drop callbacks and wrong dough hierarchy lookup failed and were corrected; over-high/edge release attempts correctly declined and were repositioned. See known-issues/test-log. No material user data deleted or overwritten.
- Tests: wrong-board/early-consumption rejection, all correct rests and bowl readiness, rack pickup/return/re-pick, delayed held motion/rotation/scroll, occupied/high refusal, filled-bowl relocation, actual three-step final whisk movement, final dough pickup, relocated board/flattened recovery, four-cookie Perfect bake and reset passed. Final fresh Console query0 errors/warnings; nine-carrier collider audit0 unsafe meshes and saved-scene audit0 missing scripts. External Unity account errors at session start remain documented, not claimed fixed.
- Notes: created cleaning-and-workstation-readiness.md; updated current status, kitchen/storage, measuring, bowl/mixing, board/portioning, cookies, session/feedback, acceptance checklist, decisions, issues, tests and this append-only log. Five evidence images added (asset preview, initial layout iteration, prepared-rests guide, bowl-placement guide, rack render).
- Save/settings/review: Edit Mode, dirty=false; PlayerSettings.runInBackground restored false. No net ProjectSettings/package changes. Unity-generated whitespace normalized mechanically and saved scene reimported/reopened; git diff --check passed. No commit/destructive reset; pending previous changes remain intact.
- Status/next: requested implementation and technical checks complete, not final cookie/environment/player acceptance. Retest natural storage/rack carrying, rest placement, both mixing phases and full cookie workflow. Cleaning remains a future requested implementation task. No VR support claim.
- Performed by: Codex main agent through local edits/Unity MCP; no subagents used.

## 2026-09-07 19:51 +08:00 Reported flour leaking through bowl — investigation

- User reports flour appears to leak after scooping into the bowl. Inspect presentation versus collision before changing physics. Existing user/layout edits remain untouched.
- Initial Console: zero errors/warnings; Kitchen Prototype in clean Edit Mode. Bowl mesh spans local Y approximately0–.10; IngredientTrigger localY.25, centerY-.1241, sizeX.2155. Current visual root subtracts half the trigger width, followed by another negative flour offset: predicted flour bottom below bowl base.
- Plan: reproduce the current presentation in Play Mode; fit visible contents inside the bowl without changing ingredient quantities, scoop logic or bowl physics; verify flour and related ingredient/mixing stages, carried bowl and reset. Codex via Unity MCP/local edits.

## 2026-09-08 11:20 +08:00 Flour-through-bowl visual correction completed

- Continued Sep7 19:51 investigation after user continue. Diagnosed decorative visual clipping, not physical leakage: flour bottom was bowl-localY-.03148 because presentation was lowered from the offset receiver trigger.
- Source affected: BowlReceiver.cs, adding ingredientVisualCenter(0,.06,0) in bowl-parent space, independently positioned within the existing trigger-child hierarchy. Flour width.72→.62D, X.08→.02D, Y-.13→-.12D; sugar X-.18→-.08D and Z.08→.04D. Final flour minimumY.01258 and inner-shell clearance.00115. D is existing .2155 trigger diameter; no ingredient count, collider, Rigidbody, material, model, mixing threshold, source or scoop behavior changed.
- Scene: Kitchen Prototype; explicitly saved new receiver visual-center field. No kitchen layout/transforms changed. Prior code and image work is already in the user's current HEAD20ef8a7 (BakeITV0.2.3.1), observed clean at continuation; Codex did not create that commit and did not reset it.
- Iterations: first raised flour fit hovered14mm above interior, then seated closer; initial sugar recenter left9 vertex intersections and was moved inward until all passed. No physics workaround, new manager or spill simulation. No destructive action or permanent test helper.
- Tests: proper-rest preparation and real scoop/bowl callbacks accepted flour1 and emptied spoon; base ingredient and four-base/three-chip/three-final mixing callbacks passed. Actual mesh containment checked all active ingredient/mixing meshes. Fresh final-source filled-bowl ray pickup/rotation/return preserved flour and scale; final fresh Play/reset and render passed. Detailed methods/limitations in dated test log; user appearance retest remains.
- Console: original start0 errors/warnings; Sep8 start2 external executable-signature metadata warnings; final0. No account/security setting changes. Test background execution temporarily enabled Sep7, restored false; no net ProjectSettings/package change.
- Documentation: updated current status, bowl/mixing, issues, test log, design decisions and this log. Evidence includes baseline leak, fitted iteration and final saved-code render. Final scene serialization whitespace normalized; saved scene reloaded clean and pending changes reviewed, no commit.
- Next: user retests scooping flour/sugar and viewing/moving the bowl. Broader environment/cookie acceptance and planned cleaning remain unchanged. Performed by Codex main agent through local edits/Unity MCP.

## 2026-09-08 11:28 +08:00 Cookie appearance, six portions, parchment and smooth release initiated

- User requests reference-inspired pale cookie dough/random chips, golden baked cookies, parchment pulled from a roll/dispenser, and universal near-drop smooth placement on rests/board/tray/cookies. Clarification: six cookies total, not ten.
- Inspected existing board/liner/tray/pickup/oven/visual scripts, related notes, actual meshes/colliders and scene relationships. Console zero errors/warnings, Edit Mode clean. Preserve prior pending flour-fix scene/notes edits.
- Existing mismatch: board and tray receivers capture held objects immediately and teleport to fixed centers/indexed slots; tray LateUpdate reasserts positions. Flour visuals now fit correctly and must remain unaffected. Current chip cluster is repeated across portions; baked color is dark orange-brown. No paper-roll/tissue asset found; existing parchment is a flat primitive.
- Plan: shared short release-settle motion and common fit/obstruction checks, retaining chosen release X/Z; extend existing receivers without duplicate managers. Six smaller equal portions from unchanged batch. Reuse existing dough mesh with subtle shape/grain, deterministic varied chip distribution and lighter successful-bake palette. Add a simple clearly labeled roll around the existing single recipe sheet. Test normal/invalid/held/re-pick/moving-tray paths, complete six-cookie bake and reset, with rendered QA. No recipe, cleaning, package or account expansion. Codex main agent, no delegation.

## 2026-09-08 12:05 +08:00 Added player feedback: forgiving tool placement and whisk tutorial

- User reports tools physically on the green mat sometimes do not count after a high release; requests a higher threshold, a specific whisk tutorial including right-mouse rotation and scroll distance, Utensils Area naming, and separate fridge/cabinet preparation guidance.
- Inspected current StablePlacementSurface, PreparationItem, session tutorial, MiseEnPlaceStation and existing rest-specific notes. Existing release assistance only runs at E-release with .22m maximum; a later physical landing has no registration callback. Plan .50m tool-rest allowance plus released collision registration (held hovering still does not count), without enlarging the user-authored mat or changing other surface reach.
- Tutorial will use the whisk only, require rotation/scroll practice and actual placement on Utensils Area before advancing. Split phase 2 into cold-food, dry-food, remaining utensils and bowl substeps, retaining compact current-task presentation and completion latch.
- Intermediate polish checks found unreadable vendor dough mesh (Read/Write enabled), repeated decoration during same-frame clone rebuild (remove all prior children), and reversed roll text (rotation corrected). Initial fixture used Rigidbody.position before transform synchronization and was corrected; editing source during Play reloaded runtime tray collections, so that run cannot certify post-bake removal. Full verification will restart on final source. These are retained as failed attempts, not hidden.

- Additional player report: held cookies sit below the crosshair; aiming at parchment can prevent E-release. Inspection confirmed HoldPoint localY=-.15 and dispenser ray handling before DropObject. Plan center cookie portions on the camera ray while preserving other tool offsets and collision recovery; dispenser ray is ignored when holding an item so E releases it. Preserve the player's temporary roll relocation if authored in Edit Mode.

## 2026-09-08 13:20 +08:00 Cookie polish and follow-up usability changes — verified handoff

- Scope: user's two food references, six cookies total clarification, roll dispenser and near-drop smooth placement, followed by forgiving tool-mat credit, whisk/rotation/scroll tutorial, separate cold/dry prep and cookie crosshair/release fix. Kitchen Prototype only; Codex main agent through apply_patch and Unity MCP, no delegation.
- Runtime files added: Ingredients/CookieAppearance.cs; Interactions/SmoothPlacement.cs, PlacementGeometry.cs, ParchmentDispenser.cs. Edited: BowlReceiver, DoughBoardController, TrayReceiver, TrayLinerReceiver, PickupController, StablePlacementSurface, OvenController, MiseEnPlaceStation and CookieRecipeSessionController. Added Editor/CookiePolishSetup.cs as an explicit one-time Undo-aware migration, guarded against rerunning over manual layout edits.
- Assets: CookieSurface.shader and Cookie_Dough.mat; existing Food_Dough.fbx importer Read/Write false→true (Unity also upgraded its serialized importer metadata). Source FBX/model and existing chip material are reused, not replaced. No generated/imported bitmap used. Automatic Unity .meta files accompany new assets. Reference-inspired low-poly treatment, not a photoreal replica.
- Food values: raw base (.93,.80,.57); Perfect (.78,.42,.14)→(.88,.67,.36). Shader grain and baked-edge tint, base mesh variation, one deterministic scattered chip mesh per food body. Raw slab has24 chips, portions8/9/10/8/9/10. Board reference/minimum/maximum yield4/4/9→6/6/6, portionHeightOffset .012→.003. Ingredient amounts, four base/four rolling/three chip/three combining counts and oven thresholds/timer unchanged.
- Placement: shared .24s smooth motion, near-release X/Z/yaw preservation, upright support/obstruction checks, collision suppression during settle with restoration, pickup cancellation and moving-tray local anchoring. Board/liner/tray no longer capture held overlap or force indexed positions. Six fully settled same-batch cookies and settled parchment are required. Loaded paper is protected; cooked cookies remain replaceable. Tray gains PreparationItem and an invisible .95/.006/.90 worktop support at(1.68,1.15835,5.85). Board support is reused for returning portions.
- Paper: existing ParchmentPaper starts inactive; roll contains dispenser/outlet/short paper lip/core/E label and dispenses that one sheet. Initial authored roll(1.68,1.208,6.36), scale(.09,.29,.09), Z90. User relocated it during the session; final observed roll(1.99,1.21,6.57) was preserved. No layout reset. Label reversed initially; corrected to Euler(30,0,0). Repeat requests use the existing sheet; scene restart replenishes it.
- Tools/tutorial: Mix Tool Rest renamed Utensils Area with existing Transform and station references preserved. Its maximumSettleDistance .22→.50; tool-only edge allowance0→.035, center still inside mat. Collision enter/stay counts a released physical landing; held hovering does not count. Synchronized Rigidbody/Transform preview poses fix stale rotation bounds. Related world labels and preparation corrections now use Utensils Area. Whisk-specific two-screen tutorial observes WASD/look/E pickup, right-click mouse movement, scroll, and final resting surface. Cold food, dry food, remaining utensils and bowl have separate compact phase2 substeps. Existing135% panel size and completion latch retained.
- Cookie pickup: HoldPoint was(0,-.15,1.50) in saved scene. Cookie portions now target the camera ray at adjustable holding distance; other items retain their old offset. Roll interaction returns false with a held item so E reaches DropObject, even if the crosshair hits the roll behind the cookie. Existing sweep/recovery/player-collision logic retained.
- Iterations/failures: unreadable mesh errors fixed by original importer flag; same-frame clone rebuild produced three active chip meshes, fixed by disabling/removing all previous decoration children; roll label mirrored, corrected. Initial Rigidbody-only fixture movement gave stale poses; synchronized transforms and reran. High physical spoon drop slipped slightly across mat edge and initially did not count; tool-only .035 allowance plus pose synchronization corrected it. Four new deprecated enumeration warnings were removed using the installed Unity overload. A direct diagnostic FBX mesh lookup and one note-patch heading match failed without changing gameplay data; corrected. Source edits during an intermediate Play run cleared runtime collections on domain reload; only fresh final-source results certify membership and reset. Earlier intermediate evidence is retained, not passed off as final.
- Verification: fresh final-source Play Mode 13:10–13:19 passed tutorial input/ray pickup/rotation/scroll/settled completion, .45m-high whisk release, natural high spoon fall and mat credit, cold→dry screen progression, all supplies/bowl, measured ingredient callbacks and both mixing gates, uncut2/4 recovery, six portions with single unique decoration meshes, actual roll ray, crosshair viewport(.50,.50,.75), E-drop while aiming at roll, paper protection, five/six and overlap gates, moving full tray, actual five-second180C Perfect bake, cooked-cookie replacement and latched completion after tray removal. Alternate160/200/230/180C outcome callbacks verified under/over/burnt/Perfect names/colors with unchanged chip material. Restart restored0 portions, empty bowl, incomplete prep, replenished roll and yield6. Detailed methods in test-log; manual traversal/gesture acceptance remains open.
- Saved evidence: cookie-raw-dough-final, cookie-six-raw-tray and cookie-six-golden-final PNGs (2026-09-08) are actual Unity renders. Earlier blank/missing-chip/reversed-label images are diagnostic iterations. No permanent verification cameras, input scripts or fixture objects remain in the scene.
- Repository: external/user HEAD advanced from20ef8a7 to4f46361 (BakeITV0.2.3.2) while testing. Codex did not make that commit or reset it; later changes are preserved on top. Temporary background testing setting restored false (the user's intermediate commit captured it true). No package/account/security changes. Current status, system notes, decisions, issues and tests updated. Save/reload and pending-diff review performed; no Codex commit.
- Remaining: player acceptance at normal resolution, full natural carry/oven-door insertion/gesture route, final performance/device testing and VR integration. Cleaning remains notes-only. Cookie slice is technically verified, not declared fully accepted or feature-complete.

## 2026-09-08 21:08 +08:00 Measure return / tilt pour / deliberate discard initiated

- User got stuck holding unwanted sugar; requests put-back/emptying, explicit cup versus spoon instructions, and tilting or discarding with future waste tracking in mind. Inspected Codex.md, source/scoop/bowl/session code and related notes, existing model/contents geometry and scene references. Edit Mode clean, Console0; preserve all prior pending changes and user's roll position.
- Baseline: touching bowl auto-empties accepted measures; rejected sugar remains indefinitely. Different sources silently replace contents, and early chips are emptied without accounting. No return or deliberate-discard action. Actual _04 model is cup-sized (radius~.04), _01 small spoon radius.0205; both currently named spoons and both accept powders.
- Plan: name _04 Measuring Cup and use it for flour/sugar; retain Small Measuring Spoon for chips. Reuse existing components, add held tilt-pour into bowl and matching-source return, prevent silent replacement/refill loops, and hold-Q discard recovery. Compact contextual instructions identify contents/tool and pour/return/discard controls. Record ingredient, tool/unit and disposition events per session for future grading; no score, gram conversion, cloud service or full spill/cleaning simulation.
- Preserve ingredient-unit quantities, six cookies, current mixing/bake thresholds and existing environment. Short collider-free transfer visuals only. No suitable bin asset found in bounded prefab-name search; explicit hold-Q discard does not require adding furniture. Test rejection/return/discard/duplicate prevention, real tilted transfers, full recipe and reset in Play Mode. Codex main agent, no delegation.

## 2026-09-08 21:28 +08:00 Recoverable measuring handoff

- Implemented in MeasuringScoop, MeasuredIngredientSource, BowlReceiver and CookieRecipeSessionController; added MeasureTransfer and MeasureTransferVisual (Unity-generated meta files). Existing cup object renamed Measuring Cup, preserving references/model/powder visual. Cup mouth radius .0205→.0365 and fill surface .023→.041 match its larger model; Small Measuring Spoon remains chip-only. No furniture/roll relocation or ingredient quantity changes.
- Before: bowl contact auto-consumed, different sources overwrote contents, rejected sugar had no recovery and early chips could vanish. After: only a held measure tilted at least75 degrees for .35s over a destination transfers. Matching-source return is non-waste and blocks refill until lifted .07m clear; wrong source and rejected bowl contents remain recoverable. Upright sources fill below55 degrees; full measures never silently overwrite.
- Hold Q for .60s deliberately discards one measure, release early cancels. Contextual HUD identifies cup/spoon and contents with tilt/return/discard controls; no permanent waste panel. Short collider-free transfer grains provide feedback. Per-session events record ingredient/tool/unit/destination/disposition/time; only Discarded increments waste, scene restart resets. No grade, grams, stock depletion, persistent spill/cleaning or backend added.
- Verified roles, physics fill, upright non-pour, tilt timer, duplicate rejection, matching return/refill lock, wrong-source rejection, input-backed discard threshold/cancellation/empty-repeat, early-chip retention, three chip additions and combining gate, six-cookie placement/bake flow, and empty restart ledger/tools/portions. See evening test-log for direct callbacks versus actual physics/timers. Background Q injection failed to sustain reliably; manual focused Game-view Q acceptance remains open and is explicitly recorded in known-issues.
- Added measured-waste design note and acceptance checklist; updated status, measuring/bowl/recipe notes, decisions and issues. Evidence measure-tilt-transfer-2026-09-08.png is an actual Unity render. All previous pending changes preserved; no commit/reset. Play stopped, temporary background=true restored false, scene/assets saved and final diffs/Console reviewed.

## 2026-09-09 09:31 +08:00 Underbaked no-visual-change and parchment-size correction initiated

- User discarded the previous broader change set and explicitly limited this pass to two items: underbaked temperatures must make zero cookie visual changes, and parchment must cover at least 95% of the tray. No equipment-storage, recipe-flow, pickup, workstation, or unrelated scene change is authorized.
- Baseline after the user's discard: Git worktree clean; `OvenController` still blends underbaked bases toward white, changes smoothness, and writes bake amount; saved `ParchmentPaper` scale is `(.55,.002,.32)` against tray support approximately `(.7211077,.4330797)` on X/Z. Unity Console has zero errors/warnings.
- The active Editor scene is dirty after the external discard even though Git is clean, so it will be reloaded from the saved disk scene before editing to prevent stale prior-session state from being saved. Planned files are only `OvenController.cs`, `Kitchen Prototype.unity`, and mandatory affected notes. No package, ProjectSettings, material, shader, prefab, model, audio, recipe quantity, oven threshold, timer, or tray interaction change is planned.
- Performed by Codex main agent through local patches and Unity MCP; no delegation.

## 2026-09-09 09:39 +08:00 Underbaked no-visual-change and parchment-size correction completed

- Objective/result: completed only the two explicitly retained requests. Temperatures below `170 C` keep the logical `Underbaked` / `UnderbakedCookie` outcome and feedback but perform zero cookie material changes. Existing parchment is now slightly above 95% of the actual tray support on X and Z.
- Files/assets: edited `Assets/BakeIT/Scripts/Interactions/OvenController.cs`, `Assets/BakeIT/Scenes/Kitchen Prototype.unity`, and mandatory affected BakeIT notes. No file created/renamed/removed; no prefab, material, shader, model, audio, package, ProjectSettings, storage, workstation, pickup, recipe, threshold, timer, roll, tray transform, or receiver behavior changed.
- Code/Inspector values: removed obsolete `underbakedColor` and `underbakedSmoothness` serialization. Added a branch-local visual-change gate: underbaked skips renderer material access; Perfect, Overcooked, and Burnt still set bake amount/color/smoothness. `ParchmentPaper` scale changed `(.55,.002,.32)` → `(.6851,.002,.4115)`; tray support remains approximately `.7211077 x .4330797`, for `95.0066%` / `95.01715%` coverage.
- Testing: synchronous Unity compilation succeeded without C# diagnostics. Focused Play Mode placed the existing paper, loaded six same-batch portions through the actual receiver, registered the actual oven zone, and passed a `160 C` logical-underbaked result with unchanged deliberately non-default `_BaseColor`, `_BakeAmount`, and `_Smoothness`. A following `180 C` Perfect regression applied expected values. Successful-run Console query zero errors/warnings.
- Failed attempts: first fixture lacked physics-transform synchronization and could not place the moved paper; restarted clean and corrected. Exact 95% serialization rounded X to `94.99999%`, so the final sheet uses a tiny positive margin. One intermediate Perfect check observed the wrong material reference; final renderer-instance check passed. No failed runtime object was saved.
- Remaining: human Game-view confirmation that the enlarged sheet looks correct inside the tray rim and that low-temperature cookies show no perceptible visual change. Final Edit Mode reload/audit passed with no temporary objects and a clean saved scene; Console returned zero errors/warnings; pending diff contains only the two requested implementation changes plus mandatory documentation. Performed by Codex main agent via `apply_patch` and Unity MCP; no delegation or commit.

## 2026-09-09 09:45 +08:00 Mixing and rolling pass cooldown initiated

- User request: prevent one whisk or rolling gesture from instantly counting as multiple mixing/flattening passes by adding cooldowns to both interactions.
- Baseline: `BowlReceiver` already has a `0.15 s` minimum between passes, but subtracts only one threshold from accumulated travel, so excess movement remains banked and can count later without a fresh stroke. `DoughBoardController` has no cooldown and a `while` loop can consume several `0.2 m` thresholds from one movement sample. Required counts remain four base-whisk, three chip-combining, and four rolling passes.
- Plan: use a tunable `0.35 s` minimum for both systems, discard movement accumulated during the cooldown, reset travel to zero after each accepted pass, and allow at most one pass per movement sample. Preserve recipe counts, dough scaling, visual progression, tool physics, prior oven/parchment work, and all unrelated scene values.
- Baseline Unity Console: zero errors/warnings. Planned runtime files are `BowlReceiver.cs` and `DoughBoardController.cs`, with their two serialized cooldown values in `Kitchen Prototype.unity` and mandatory notes. Performed by Codex main agent via local patches and Unity MCP; no delegation.

## 2026-09-09 13:35 +08:00 Mixing and rolling pass cooldown completed

- Objective/result: one whisk or rolling gesture can no longer count as multiple instances. Both systems now require fresh distance after a `.35 s` cooldown; movement during cooldown is discarded and one movement sample can register at most once.
- Files/scene: edited `Assets/BakeIT/Scripts/Ingredients/BowlReceiver.cs`, `Assets/BakeIT/Scripts/Interactions/DoughBoardController.cs`, `Assets/BakeIT/Scenes/Kitchen Prototype.unity`, and mandatory affected notes. No files/assets/components were created, removed, or renamed; no prefab, material, shader, model, audio, package, ProjectSettings, recipe quantity, ingredient logic, workstation, pickup, oven, parchment, or tray change.
- Before/after: bowl `minimumTimeBetweenPasses .15 → .35 s`; accumulated whisk travel now resets instead of subtracting one threshold. Board adds `minimumTimeBetweenRollingPasses=.35 s`; the multi-count `while` loop is replaced by a single gated count and reset. Existing `.06/.2 m` distance thresholds and base/chip/rolling counts `4/3/4` remain.
- Verification: synchronous Unity compilation passed. Disposable Play Mode fixtures used actual receiver callbacks. Immediate whisk stayed `1 → 1`; a `.65 m` roll counted one and its immediate reverse stayed one. Fresh strokes after each cooldown progressed base mixing to `4/4`, rolling to `4/4`/`FlattenedDough`, and post-chip combining to `3/3`/recipe complete. Successful Play Console query zero errors/warnings.
- Failed attempts: none. Runtime background processing was temporarily enabled only to let the real clock cross cooldown intervals and restored false. No verifier or QA object is saved.
- Remaining: player should tune subjective `.35 s` pacing in the focused Game view and later VR controllers. Final Edit Mode reload/audit passed with `dirty=false`, background processing restored false, and zero temporary QA objects. Final Console had zero errors and one already-documented external executable-signature warning (`SERVICE-002`); no project warning. Final diff review passed with no whitespace errors, saved QA objects, package changes, or ProjectSettings changes. Performed by Codex main agent via `apply_patch` and Unity MCP; no delegation or commit.

## 2026-09-09 13:45 +08:00 Parchment fit, knife pose, and cooldown retune initiated

- User report: the 95% parchment is too large and cannot be placed naturally; the held knife points up toward the crosshair instead of forward; `.35 s` whisk/rolling pacing still counts too quickly, with `1.2 s` suggested for testing.
- Measured baseline: paper `.6851 x .4115` against support `.7211077 x .4330797` leaves only about `2.4 mm` valid off-center tolerance on Z after the shared `.003 m` inset. Knife mesh is `.3075 m` along local Z and its longer/blade side extends toward local `-Z`; held pose is `(90,0,0)`. Saved cooldowns are `.35 s`; required counts remain `4/3/4`.
- Plan: reduce the existing paper to approximately 90% (`.649 x .3898`), change only the knife pickup pose to `(0,180,0)`, and set both saved/default cooldowns to `1.2 s`. Preserve free-position placement, recipe counts, distance thresholds, cutting behavior, rotation controls, oven logic, and all unrelated scene/project settings. Verify compilation, off-center paper placement plus six portions, held knife forward alignment/cutting path, immediate and pre-interval cooldown blocking, and full post-interval completion.
- Baseline Console: zero errors and one existing external executable-signature warning (`SERVICE-002`), unrelated to gameplay. Performed by Codex main agent via `apply_patch` and Unity MCP; no delegation.

## 2026-09-09 14:05 +08:00 Parchment fit, knife pose, and cooldown retune completed

- Objective/result: parchment now fits and is materially easier to release on the tray; the held knife's blade points forward; whisk and rolling counts use the requested `1.2 s` pacing. Fresh-distance and one-count-per-sample behavior remain unchanged.
- Files/values: `ParchmentPaper` scale `.6851,.002,.4115 → .649,.002,.3898` (approximately `90.000%/90.007%` tray coverage); knife `PickupPose (90,0,0) → (0,180,0)`; bowl and board cooldown defaults plus saved scene values `.35 → 1.2 s`. Existing counts `4/3/4`, distance thresholds `.06/.2 m`, tray/free-placement logic, cutting code, pickup rotation, oven behavior, packages, and ProjectSettings are unchanged.
- Play verification: paper released successfully with deliberate `25 mm` X / `12 mm` Z offset and accepted six non-overlapping same-batch portions. Held knife local `-Z` blade direction aligned exactly with camera forward (`dot=1`) and produced six portions after rolling. Immediate repeats stayed at one; explicit `0.65 s` whisk/roll attempts stayed `1/1`, and fresh post-`1.2 s` strokes advanced both to `2`. Full stages completed base `4/4`, chips `3/3`/recipe complete, and rolling `4/4` before knife portioning. Play Console had zero errors/warnings.
- Failed attempts/corrections: an audit command using reflection was rejected by the Unity bridge security validator, so the successful audit used `SerializedObject`; no product or scene mutation occurred. One disposable chip stroke was ignored after the QA whisk drifted outside its synthetic trigger; repositioning/re-entering the same callback completed `3/3`. This was fixture geometry, not a cooldown failure. An initial knife assertion checked the wrong model-axis sign; direct `+Z/-Z` comparison established local `-Z` as the forward blade direction.
- Final audit: exited Play Mode with runtime background processing restored false. Edit Mode has zero QA objects, `dirty=false`, paper/knife/cooldowns serialize correctly, and Console has zero errors/warnings. Human Game-view judgment remains required for parchment appearance, knife presentation, and whether `1.2 s` is the best fluidity value. Performed by Codex main agent via `apply_patch` and Unity MCP; no delegation or commit.

## 2026-09-09 14:15 +08:00 Parchment sensitivity and four-point reduction initiated

- User report: the approximately 90% parchment remains difficult to add; requested greater placement sensitivity and about another 4% size reduction. User also requested recommendations for the next project focus.
- Baseline: saved paper `.649 x .3898` is `90.000%/90.007%` of tray support. `TrayLinerReceiver` preserves exact X/Z/yaw, uses `PlacementGeometry`'s default `.22 m` maximum height, and has no misalignment fallback. Previous `25 mm/12 mm` offset passed technically, but the user experience shows that precise footprint fitting remains too demanding.
- Plan: interpret 4% as four tray-percentage points and use approximately 86% (`.6202 x .3725`). Keep exact placement first, then add a parchment-only tray-center/alignment fallback within serialized `.15 m` horizontal and `.35 m` vertical reach, with `.12/.35 m` detection padding. Verify precise preservation, assisted misaligned/high release, far/below rejection, six-cookie support, tray movement, compilation, cleanup, and Console. Preserve cooldown, knife, oven, recipe, and unrelated placement behavior.
- Baseline Console: zero errors/warnings. Performed by Codex main agent via `apply_patch` and Unity MCP; no delegation.

## 2026-09-09 14:35 +08:00 Parchment sensitivity and four-point reduction completed

- Objective/result: reduced paper from approximately 90% to 86% coverage and made nearby releases substantially easier without weakening shared placement for other objects. Exact valid poses remain untouched; only a failed parchment pose within the configured reach receives tray-centered, tray-aligned assistance.
- Files/values: edited `TrayLinerReceiver.cs`, `Kitchen Prototype.unity`, and mandatory notes. Paper `.649,.002,.3898 → .6202,.002,.3725`, measuring `86.007%/86.012%`. New serialized assist values are `.35 m` maximum vertical distance, `.12 m` horizontal detection padding, `.35 m` vertical padding, and `.15 m` magnetic radius. No prefab, package, ProjectSettings, cookie quantity, cooldown, knife, oven, or shared `PlacementGeometry` change.
- Play verification: exact placement preserved a `.020/.015 m` player offset. A deliberately failed pose `.10/.08 m` off-center, `.30 m` above, and yawed `45°` was accepted by the fallback and settled with zero center/yaw delta. A `.18 m`-far rotated pose and `.04 m`-below pose were rejected. Six same-batch portions loaded successfully; the complete paper/cookie hierarchy retained local positions after tray movement. Fresh Play Console zero errors/warnings.
- Failed attempt/correction: first fallback test rejected even though a separately centered pose validated. Cause was stale Transform bounds when the temporary preview assigned only Rigidbody pose. The corrected implementation synchronizes both Transform and Rigidbody before and after the preview, matching the shared geometry utility. The downstream zero-cookie/movement failures in that discarded run were expected consequences of the absent liner, not separate product defects. Script was recompiled and all tests rerun from fresh Play Mode.
- Final audit: Edit Mode `dirty=false`, zero QA objects, runtime background false, saved paper/assist values correct, and Console zero errors/warnings. Human placement feel remains the final acceptance check. Performed by Codex main agent via `apply_patch` and Unity MCP; no delegation or commit.
## 2026-09-09 14:51 +08:00 Data-driven recipe foundation initiated

- User approved making the existing cookie mechanics reusable before starting brownies because those recipes share much of the same base flow. This pass is limited to a recipe-data foundation and migration of the current chocolate-chip-cookie values; it must not change the player-facing cookie workflow.
- The manuscript/implementation guide is explicitly provisional: its three-recipe scope remains the working boundary, but feature details may change. Any new mechanic, ingredient, workflow stage, scoring rule, or other improvement that departs from the user's approved request or steers beyond the guide must be discussed with the user before implementation.
- Planned implementation: one reusable `RecipeDefinition` ScriptableObject model, one chocolate-chip-cookie definition asset, and connections from the existing bowl, measuring, shaping, tray, oven, and guide systems to those values while preserving existing fallback values and scene behavior. Brownie/cupcake assets and behavior, cupcake icing, yeast/leavening decisions, scoring, modes, and cloud work are not part of this pass.
- Baseline: `Kitchen Prototype` is the scene in scope; Unity Console reported zero errors and warnings before the recipe-data work. Existing pending parchment, cooldown, knife, oven, scene, and documentation changes are user-approved work from the current session and will be preserved. No package or ProjectSettings changes are planned.
- A source-only `Assets/BakeIT/Scripts/Recipes/RecipeDefinition.cs` draft has been added. Its future-facing pan/shaping/finish values are configuration vocabulary only; they do not implement new gameplay. Further code/asset/scene changes and Play Mode verification remain in progress.
- Performed by Codex main agent through `apply_patch` and Unity Editor verification; no delegation.

## 2026-09-09 15:14 +08:00 Data-driven recipe foundation completed

- Objective/result: added the approved reusable recipe configuration and migrated the existing chocolate-chip-cookie facts without changing its quantities or player workflow. The manuscript/guide is recorded as provisional; improvements or deviations beyond an approved request require user consultation.
- Created `Assets/BakeIT/Scripts/Recipes/RecipeDefinition.cs` and meta; `Assets/BakeIT/RecipeDefinitions/` folder/meta; `ChocolateChipCookies.asset` and meta; and dedicated `BakeIT_Notes/recipe-data-system.md`. Edited `BowlReceiver.cs`, `MeasuringScoop.cs`, `DoughBoardController.cs`, `TrayReceiver.cs`, `OvenController.cs`, `CookieRecipeSessionController.cs`, `Kitchen Prototype.unity`, and mandatory relevant notes.
- Scene/component change: `IngredientTrigger/BowlReceiver.recipeDefinition` changed None → `ChocolateChipCookies.asset`. No GameObject, prefab, material, shader, model, audio, package, ProjectSettings, parchment, knife, collider, Rigidbody, Transform, cooldown, recipe amount, or scoring value was added/removed/changed in this architecture pass.
- Asset values: five cookie requirements and tool roles; base/finishing stages; mix4+3; RollAndPortion/roll4; target/min/max6/6/9; Parchment; post-finish None; existing runtime names; bake5s, perfect170–190C, burnt>=220C. Future shaping/pan/icing enum options are dormant vocabulary only, not implemented features or approved recipe formulations.
- Behavior: bowl validates listed requirements/stages and whole-item roles; measuring tools use exact configured roles and reject unlisted ingredients; board/tray/oven use configured names/counts/profile; guide uses configured title/checklist/labels/range. All retain current fallback values when no asset is assigned. Existing cookie visuals and twelve-stage orchestration remain deliberately cookie-specific.
- Verification: compilation passed; Edit Mode asset/reference audit passed; Play Mode consumers shared one definition; actual measuring roles and absent-Cocoa rejection passed; runtime receiver callbacks passed early/unlisted/duplicate/extra rejection and exact 4+3 completion; final-source runtime regression passed. Scene reloaded from saved disk after disposable tests; final `dirty=false`, zero QA objects, Console zero errors/warnings.
- Failed attempt: Unity bridge rejected one reflection-based read-only gate script before execution. Replaced with ordinary receiver callbacks; no asset or scene effect.
- Limitations/follow-up: only cookies are migrated. A data asset alone does not implement brownies/cupcakes; their ingredients, flow, pan actions, bake profile, and cupcake leavening/icing require consultation. Persistent step data, generic stage sequencing, and repeatable project tests remain later work. Performed by Codex main agent via `apply_patch` and Unity Editor commands; no delegation or commit.

## 2026-09-09 15:21 +08:00 Oven preheat and retained-light workflow initiated

- User requested a kitchen-essential preheat function and for the oven light to remain on once heated. This pass interprets “kitchen essentials” only as that explicit oven behavior; it does not authorize additional props, ingredients, safety/scoring rules, or unrelated improvements.
- Existing architecture inspected: one `OvenController` owns temperature/baking; the physical BAKE marker routes through it; `OvenDisplay` shows set temperature/countdown; `OvenInteriorLight` currently lights only while baking or when the door is open; the cookie guide currently loads the tray before starting the oven. Active scene `Kitchen Prototype` was clean and baseline Console had zero errors/warnings.
- Plan: reuse the existing start control for context-sensitive PREHEAT then BAKE, add a short serialized prototype preheat duration/current-temperature state, require preheat before timed baking, keep the interior light/emissive surface on while preheating and after readiness, update the display and cookie guide with a visible preheat stage, and preserve the data-driven recipe temperature/bake outcome rules.
- Intended scene change is limited to assigning/updating the existing start-button label if needed and serializing preheat tuning on the current oven. No new GameObject, model, material asset, audio, package, ProjectSettings, recipe ingredient/quantity, bake threshold/duration, scoring, or extra kitchen object is planned.
- One read-only Unity inspection command failed compilation because Unity 6 marks `GetInstanceID()` obsolete-as-error; it executed nothing and was immediately replaced with an ID-free inspection. Performed by Codex main agent via `apply_patch` and Unity Editor commands; no delegation.

## 2026-09-09 18:43 +08:00 Oven preheat and retained-light workflow completed

- Objective/result: added the explicitly requested preheat function to the existing oven and kept its light/emissive interior on after reaching temperature. The intended order is now set temperature → close empty oven → PREHEAT → READY/light on → load complete tray → BAKE.
- Source changes: `OvenController.cs` now owns current temperature, preheating/readiness state and gate logic; `OvenInteriorLight.cs` follows preheating, heated, baking, and configured door-open states; `OvenDisplay.cs` shows current temperature plus PREHEAT/READY or the bake countdown; `OvenStartButton.cs` changes its existing label between PREHEAT, HEATING, BAKE, and BAKING; `CookieRecipeSessionController.cs` adds a separate preheat phase before tray loading.
- Scene values: the existing `OvenBakeZoned` controller serializes ambient `25 C` and preheat duration `5 s`; the existing start-button component references the existing oven and label, whose saved text is `PREHEAT`. No GameObject, model, material, audio, recipe asset/value, package, or ProjectSettings change was added.
- Behavior: preheat requires a closed, empty oven; an open door pauses it; baking requires readiness plus the existing complete-batch/closed-door gates; changing the target clears readiness; bake completion retains current temperature, readiness, and the oven light. Existing 5-second bake profile, temperature outcomes, cookie visuals, and underbaked zero-material-change rule are preserved.
- Verification: a disposable Play Mode verifier exercised the actual liner, tray receiver, bake zone, controller, display, label, and light. It passed initial cold state, bake-before-preheat rejection, loaded-tray preheat rejection, intermediate heating UI/light, 180 C READY UI/light, loaded-tray baking UI, six `BakedCookie` / Perfect outcomes, and retained READY/light after baking. Marker: `OVEN_PREHEAT_QA_COMPLETE: PASS=True; initial=True; bakeBlocked=True; trayPrepared=True; loadedBlocks=True; removed=True; started=True; heatingUI=True; readyUI=True; reloaded=True; bakingUI=True; retainedAfterBake=True`.
- Test corrections: the first read-only inspection used Unity 6-obsolete `GetInstanceID()` and did not run. An early cross-command timed test lost transient Play state when the dynamic command assembly refreshed, so it was replaced with the single lifecycle verifier. Two `CS0618` warnings from a deprecated `FindObjectsByType` overload were corrected to the Unity 6 inactive-object overload. The first command immediately after recompilation hit a transient Unity bridge `RunCommandMacroEvaluatorEntryPoint` lookup error; a retry compiled successfully and the project Console was clean.
- Cleanup/final state: the temporary verifier source/meta and every runtime QA object were removed, background execution restored false, and the saved scene reloaded clean with `dirty=false`. The final project compile had zero project errors/warnings; the Console contained one already documented external Codex executable-signature warning (`SERVICE-002`) and no gameplay/project warning. Player-facing five-second timing, label fit, and light readability remain for human acceptance. Performed by Codex main agent via `apply_patch` and Unity Editor commands; no delegation or new commit.

## 2026-09-09 19:01 +08:00 Brownie direction and metallic cookie tray initiated

- User selected Allrecipes `Quick and Easy Brownies` as the brownie basis, explicitly excluded walnuts and deferred additions such as walnuts/marshmallows until all three base dishes work. Approved direction includes a deep rectangular pan, a rubber/plastic spatula for spreading/leveling batter, and playable slicing. Cooling may remain optional; parchment may replace the source recipe's baking-spray greasing, pending final workflow confirmation.
- User explicitly requested a metallic finish for the existing cookie tray matching the future brownie pan and asked how players will change between pans. This pass changes only the current cookie tray appearance and records a recommended future physical-pan selection design; it does not create the brownie pan/spatula, add a recipe selector, implement brownie ingredients/batter/spreading/cooling/slicing, or alter cookie gameplay.
- Baseline inspection: `Prop_Tray` has one renderer using shared `Panda Mat` (`metallic=0`, `smoothness=.5`), which is also used by 27 other scene renderers. It must not be edited globally. No suitable project-owned metallic bakeware material exists; package-only Editor template materials are not appropriate runtime dependencies. Plan: create one neutral project-owned `Bakeware Metal` material for this tray and the future approved brownie pan, assign it only to `Prop_Tray`, then verify appearance/receiver behavior in Play Mode.
- Baseline scene was clean. Console had zero errors and one already documented external Codex executable-signature warning (`SERVICE-002`). Performed by Codex main agent through Unity Editor commands and `apply_patch`; no delegation.

## 2026-09-09 19:05 +08:00 Metallic cookie tray completed; brownie pan selection remains consultative

- Created project-owned `Assets/BakeIT/Art/Bakeware Metal.mat` and meta using URP Lit with neutral silver base color `(.62,.65,.68,1)`, metallic `.85`, and smoothness `.35`. Assigned it only to the existing `Prop_Tray` renderer in `Kitchen Prototype`; its previous material was shared `Panda Mat` (`metallic=0`, `smoothness=.5`) and remains untouched on all 27 other scene users.
- No Transform, mesh, collider, Rigidbody, parchment size/assist, tray receiver, cookie quantity, bake logic, ingredient, package, or ProjectSettings value changed. The new material is intentionally reusable by the future approved deep brownie pan for consistent bakeware appearance.
- Play Mode regression on the actual scene tray passed: material/name/metallic value correct; Rigidbody present; three colliders present; existing `TrayLinerReceiver` and `TrayReceiver` present; and `PickupController.TryPickupBody` successfully held the tray. Result: `METALLIC_TRAY_PLAY_QA PASS=True`. Exiting Play Mode restored the saved tray pose/state.
- Visual inspection of a framed Scene view confirmed the tray reads as reflective neutral metal while the parchment and surrounding kitchen remain distinguishable. No screenshot asset was saved. Final saved-scene/material audit and Console review follow in the test log.
- Final Edit Mode audit passed with the new material assigned, exact saved values, tray physics/receivers intact, `dirty=false`, and Play Mode stopped. The Scene-view capture tool emitted one transient `Releasing render texture that is set to be RenderTexture.active!` warning after returning the preview; it is an Editor capture-tool cleanup warning, not a project/gameplay fault, and no render texture or asset was retained.
- Future recommendation recorded, not implemented: keep cookie and brownie pans as separate physical objects in storage; future recipe selection sets the active `RecipeDefinition`; the guide names the required pan; and a recipe-aware receiver rejects the wrong pan rather than swapping or morphing it. This requires explicit approval before adding recipe selection, pan-type data, or the new brownie equipment.

## 2026-09-09 19:14 +08:00 Physical brownie equipment and bakeware storage initiated

- User approved the proposed physical pan-selection direction and parchment-only brownie preparation. This pass is authorized to add a stored deep rectangular brownie pan and stored rubber spatula and to relocate the existing shallow cookie tray into the same accessible bakeware storage area.
- Scope remains equipment-only: separate grabbable pans with a shared metallic finish and a grabbable leveling spatula. No recipe-selection UI/state, pan-type validation, brownie asset/ingredients, batter, spreading counter, cooling, slicing, bake profile, guide stages, optional toppings, or cupcake work is authorized in this pass.
- Existing asset inventory found vendor spatula prefabs (`Prop_Spatula_01` through `_09` plus Free Kitchen variants) and the current vendor `Prop_Tray`; no dedicated deep rectangular brownie-pan prefab was found. Plan: reuse the most appropriate rubber-spatula mesh, reuse/deepen the licensed rectangular tray mesh rather than generating an unrelated asset, give moving objects safe Rigidbody/collider/pickup configuration, and fit all three tools on/within the existing bowl-holder/bakeware rack without changing the user's workstation sizes.
- Baseline scene is saved/clean. Existing Console has zero errors and one documented Editor-only render-texture cleanup warning from the prior Scene-view capture. Performed by Codex main agent through Unity Editor commands and `apply_patch`; no delegation.

## 2026-09-09 19:26 +08:00 Physical brownie equipment and bakeware storage completed

- Result: the existing rack beside cold storage now holds the existing shallow cookie tray on a lower rail and a separate deep rectangular brownie pan on the next usable rail; the existing bowl remains untouched on its middle shelf. A green rubber spatula hangs on the rack's accessible side. The three objects remain separate physical Rigidbody items rather than visual variants of one object.
- Cookie tray: moved only its saved Transform from `(1.68,1.16135,5.76)`, rotation `(0,0,0)`, to `(-3.5,.425,4.12)`, rotation `(0,90,0)`. Its scale, `Bakeware Metal` material, Rigidbody, three colliders, parchment/cookie receivers, worktop return surface, and all cookie behavior remain unchanged.
- Brownie pan: instantiated the existing licensed `Prop_Tray` mesh as scene object `Brownie Pan` at `(-3.5,.613,4.12)`, rotation `(0,90,0)`, scale `(1.65,7,1.63)`. This produces an approximately `.40 x .08 x .56 m` stored footprint and a visibly deep pan. It uses `Bakeware Metal`, a `.8 kg` initially kinematic/gravity-off Rigidbody, `PreparationItem`, `PickupPose`, and five BoxColliders (thin bottom plus four walls) so its interior is open for later parchment/batter work.
- Rubber spatula: reused licensed solid-head `Prop_Spatula_01` as `Rubber Spatula` at `(-3.15,1.23,4.12)`, rotation `(90,0,0)`, with a `.15 kg` initially kinematic/gravity-off Rigidbody, convex source MeshCollider, `PreparationItem`, and `PickupPose`. Created project-owned `Rubber Spatula.mat` with URP Lit dark green `(.12,.38,.34,1)`, metallic `0`, smoothness `.3`.
- Play Mode regression: both pans and the spatula resolved their expected materials/components. The pan's center ray reached its bottom `.0655 m` below the rim, confirming an open usable interior. The real pickup controller successfully picked up and released the cookie tray and brownie pan onto `Tray Worktop Rest` and the spatula onto `Utensils Area`; marker `BAKEWARE_EQUIPMENT_QA PASS=True`.
- Failed audit/correction: the first oven-fit check compared the pan's sideways rack-storage bounds (`.40 x .08 x .56 m`) directly with the oven zone (`.92 x .52 x .51 m`) and correctly failed on depth. This was an orientation test error, not a saved geometry defect. A fresh Play Mode test rotated the physical pan to oven orientation, producing `.56 x .08 x .40 m`, centered it in the actual zone, and passed complete bounds containment: `BROWNIE_PAN_OVEN_FIT PASS=True`.
- Visual inspection confirmed the two distinct metal pans read clearly on separate rack rails and the green spatula is visible beside them. Scene-view captures emitted the already documented Editor-only active-render-texture cleanup warning and saved no screenshot asset. No recipe-selection, pan validation, brownie ingredients/batter/spreading/slicing, cooling, topping, guide, package, or ProjectSettings change was made. Performed by Codex main agent via Unity Editor commands and `apply_patch`; no delegation or commit.
- Visual correction before handoff: the first selected vendor `Prop_Spatula_05` proved to be a slotted turner in the rack view, unsuitable for leveling batter. It was removed and replaced with the solid spoon-style `Prop_Spatula_01` while preserving the approved material, pose, mass, and interaction configuration. Fresh Play Mode pickup/release passed: `SOLID_RUBBER_SPATULA_QA PASS=True`.
- Final disk reload found exactly one cookie tray, one Brownie Pan, one Rubber Spatula, zero preview/QA objects, exact saved positions/materials/components, `dirty=false`, Play Mode stopped, background execution false, and zero Console errors/warnings.

## 2026-09-09 19:52 +08:00 User-authored rack correction and brownie-cut consultation

- The user reported replacing/re-leveling the holder arrangement, correcting both pans so they lie on the rack instead of floating, and manually positioning/orienting the rubber spatula. Live Unity inspection confirmed the current objects and transforms. These user edits supersede the earlier Codex-authored storage rotations and are now the placement authority.
- Inspected live transforms: cookie tray `(-3.5,.425,4.12)`, rotation `(0,0,0)`; Brownie Pan `(-3.5,.613,4.12)`, rotation `(0,0,0)`; bowl `(-3.4939,.9979,4.1394)`; Rubber Spatula `(-3.2303,1.5447,4.4190)`, rotation `(270,80,0)`. The scene was dirty/unsaved. Codex deliberately did not save, reload, or mutate it.
- Brownie portion decision: use 12 true-square pieces in a four-column by three-row grid. The eventual cuttable batter/slab area should be 4:3; applying the same grid edge-to-edge to a literal 9-by-13-inch footprint would produce equal near-squares, not exact squares.
- The user identified the need for a larger brownie bowl. This is recorded as a separate bowl so the cookie bowl remains unchanged; prefab selection, placement, physics, and recipe binding remain unimplemented pending the next approved equipment pass.
- Clarified that the source recipe requires already-melted butter. Solid butter does not melt from entering a room-temperature bowl. Pre-melted supply versus a playable heating mechanic remains a pending user decision. No runtime code, scene object, asset, package, ProjectSettings, or gameplay value was changed in this consultation. The only Console entry was an unrelated Unity AI account-access warning; zero project/game errors were present. Performed by Codex main agent; no delegation or commit.

## 2026-09-09 20:13 +08:00 Larger brownie bowl and pre-melted butter pour initiated

- User approved supplying the brownie butter already melted and transferring it by deliberately tilting its container. This also confirms the previously requested separate larger brownie mixing bowl.
- Scope: add one distinct larger bowl without resizing/replacing the cookie bowl; add one carryable pre-melted-butter container with a visible liquid fill; require it to be held, tilted over the larger bowl, and maintained briefly before one complete transfer; reject upright contact, the cookie bowl, and duplicate pours. Keep this as an equipment/interaction foundation only—do not add the remaining brownie ingredients, batter, spreading, slicing, recipe selection, cooling, optional toppings, or cupcake work.
- Architecture plan: extend the existing pickup/held-rotation vocabulary with a dedicated pourable-ingredient component and a receiver owned by the brownie bowl. Do not add a second `BowlReceiver` or disturb the active cookie recipe, because current cookie systems resolve that component as their sole authority.
- Scene baseline: the user's live rack/tray/spatula corrections remain dirty/unsaved and are authoritative. Implementation will preserve and save them rather than reload the scene. Existing Console has zero project/game errors and one unrelated Unity AI account-access warning. Performed by Codex main agent through `apply_patch` and Unity Editor commands; no delegation or commit.

## 2026-09-09 20:36 +08:00 Larger brownie bowl and pre-melted butter pour completed

- Added `PourableIngredient.cs` and `PourableIngredientReceiver.cs`. The first tracks a single visible liquid portion, held state, 75-degree tilt, `.35 s` hold, `.012 m` mouth detection, and `.35 m` downward destination search. The second accepts only `MeltedButter`, requires its owning bowl's `PreparationItem` to rest on `Mix Bowl Rest`, blocks duplicates, and owns accepted-state presentation. Extended `MeasureTransferVisual.cs` only to render melted-butter transfers golden; cookie powder/chip behavior is unchanged.
- Added `Brownie Mixing Bowl` from licensed `Prop_Bowel_02` at initial `(-3.5,1.19,4.12)`, scale `(1.4,1.4,1.4)`, approximately `.306 x .131 x .310 m`. Configuration: `.5 kg` initially kinematic/gravity-off Rigidbody, `PreparationItem`, `PickupPose`, disabled vendor MeshCollider, thin base plus 12 BoxCollider walls, and one dedicated trigger. It is separate from the existing cookie bowl and does not add a second `BowlReceiver`.
- Added `Melted Butter Cup` from licensed Free Kitchen Version03 `FreeCup` at `(-2.35,1.19,5.60)`, scale `.65`. Configuration: `.22 kg` initially kinematic/gravity-off Rigidbody, existing BoxCollider, `PreparationItem`, `PickupPose`, two-sided `MELTED BUTTER` TextMesh label, visible liquid cylinder, pour point, and the new pourable component. Added project-owned URP Lit `Melted Butter.mat`: golden `(.96,.68,.16,1)`, metallic `0`, smoothness `.62`.
- Preserved and saved the user's previously dirty rack/holder, flat cookie tray, flat brownie pan, cookie bowl, and realistically oriented spatula edits. No existing user Transform was changed during this pass. The new bowl's rack pose is provisional because the user intends to do final manual leveling.
- Play Mode result: `BROWNIE_BUTTER_QA_COMPLETE: PASS=True; structure=True; larger=True ((0.306, 0.131, 0.310) vs (0.218, 0.122, 0.222)); visuals=True; cupPickup=True; uprightRejected=True; tilt=True; storageRejected=True; transfer=True; duplicateRejected=True; cookieRejected=True; cupRelease=True; bowlPickup=True; bowlRelease=True`. This exercised the real pickup controller, held/tilted/destination gates, MIX requirement, one successful transfer, visual state change, duplicate rejection, cookie-bowl rejection, and both object release paths.
- Verification corrections: the first verifier lookup used `GameObject.Find` for an intentionally inactive received-liquid child, so it reported that test reference missing; the object/configuration was present. The lookup was corrected to the bowl hierarchy. A second run reached its timed release wait, but this background Editor session did not advance `Time.time`; it was stopped and replaced with immediate `PickupController.ReleaseForPlacement` assertions. The final run passed. Temporary verifier source/meta and preview camera were removed.
- Final audit: exactly one cookie bowl and one cookie `BowlReceiver` remain; exactly one new brownie bowl/cup pair; pour hold `.35 s`; tilt 75 degrees; zero QA/preview scene objects; scene saved/clean; Play Mode stopped; final compilation succeeded; Console had zero errors/warnings. No remaining brownie ingredients, combined batter, pan liner receiver, spreading, baking, cooling, slicing, recipe selector/guide, optional toppings, package, ProjectSettings, or commit was added. Performed by Codex main agent through Unity Editor commands and `apply_patch`; no delegation.

## 2026-09-10 16:19 +08:00 Post-crash recovery audit

- The user reported that Unity had crashed after the brownie bowl/butter work, then reopened successfully. No reproduction steps or crash diagnostic were available, so no project code was changed in response.
- Reopened-scene audit passed: active scene `Assets/BakeIT/Scenes/Kitchen Prototype.unity`; `Brownie Mixing Bowl`, `Melted Butter Cup`, `PourableIngredient`, and `PourableIngredientReceiver` all present; scene `dirty=false`; Play Mode stopped. The user's authoritative cookie tray `(-3.5,.425,4.12)` rotation zero, brownie pan `(-3.5,.613,4.12)` rotation zero, and spatula `(-3.2303,1.5447,4.4190)` rotation `(270,80,0)` also matched the saved state.
- Console after reopening had zero errors/warnings. Result: `CRASH_RECOVERY_AUDIT PASS=True`. No restoration was required and no user-authored object was moved. Recorded as a one-time Editor incident for monitoring.

## 2026-09-10 16:33 +08:00 User-authored bowl-holder naming recorded

- The user renamed the two `Prop_Tray` support instances beneath `Kitchen Environment/Bowl Holder Rack` to match their functions: `Cookie Bowl Holder` at `(-3.5,.9899,4.12)` and `Brownie Bowl Holder` at `(-3.5,1.1779,4.12)`. The user also finalized `Brownie Mixing Bowl` at `(-3.4973,1.1859,4.1133)`, rotation zero, scale `1.4`.
- Read-only dependency search found no runtime reference to the former `Bowl Holder Shelf` name. The historical one-time `WorkstationFlowSetup` authoring helper still contains that original creation label but refuses repeat full application in the current scene; it was not changed or run.
- Focused Play Mode check confirmed the renamed objects are unique children of `Bowl Holder Rack`, both retain colliders, and the cookie bowl, brownie bowl receiver, and melted-butter cup components remain present. Marker: `HOLDER_RENAME_PLAY_QA PASS=True; names=True; supports=True; systems=True`.
- Scene was saved/clean before inspection. Codex made no scene, code, prefab, asset, package, or ProjectSettings mutation; only project notes were updated. Performed by Codex main agent; no delegation or commit.

## 2026-09-10 16:49 +08:00 Recipe clipboard and guide-readability pass initiated

- User request: add a physical clipboard/paper beneath the existing `BakeIT` wall sign so a player can choose which scoped dish to bake at the start; make the guide's yellow heading name the current learning part (for example `Movement Tutorial` and `Let's Practice Mise En Place`); and increase the guide size slightly for readability.
- Scope guard: the clipboard will list the three approved dishes—chocolate-chip cookies, brownies, and cupcakes—but only cookies will be selectable because the other two workflows are not complete. Brownies and cupcakes will be visibly marked `IN DEVELOPMENT`; this avoids presenting unfinished recipes as playable and does not invent new recipe steps.
- Planned implementation: add project-owned selector scripts, connect the existing `E` interaction ray to the paper rows, gate cookie tutorial tracking until the available recipe is chosen, give each existing cookie phase a plain-language yellow section heading while retaining its numbered detail line, raise the existing guide scale modestly, and add one clipboard/paper assembly below `Kitchen Identity Panel` without moving user-authored kitchen objects.
- Baseline: `Kitchen Prototype` was saved/clean, Play Mode was stopped, and Unity Console contained zero errors/warnings. No package, ProjectSettings, existing recipe value, ingredient amount, bake threshold, cooldown, equipment placement, or manuscript change is planned. The attached screenshot is treated only as a visual placement reference; the user's written request is the behavioral authority. Performed by Codex main agent; no delegation or commit.

## 2026-09-10 17:04 +08:00 Recipe clipboard and guide-readability pass completed

- Added `RecipeSelectionClipboard.cs` as the scene start-choice marker and `RecipeSelectionButton.cs` for explicit row identity/availability. Extended `PickupController` so its existing sorted three-metre `E` ray recognizes a recipe row before a backing solid; when an item is held, `E` remains release. No new input binding was introduced.
- Added `Kitchen Environment/Recipe Clipboard` at `(0,1.56,6.95)` below `Kitchen Identity Panel`: project-native wood backing, cream paper, metal clip, title/instruction text, and three trigger rows. Cookies are the sole `READY` row; Brownies and Cupcakes visibly say `IN DEVELOPMENT` and warn instead of starting. Existing kitchen/rack/equipment transforms were not changed.
- `CookieRecipeSessionController` now waits at `Choose a Recipe / RECIPE SELECTION`, prevents movement/look/tool credit before selection, and preserves the selected cookie across its existing in-session restart. Guide scale changed `1.35` → `1.5`. The yellow line now uses friendly current-part headings; the cream line retains recipe plus numbered action detail.
- Visual preview found the initial cupcake row too close to the worktop edge; Brownie/Cupcake local Y positions were raised to `-.075`/`-.235` before final save. The final render showed all three lines readable. The Library-only PNG is not a project asset and the preview camera was destroyed without saving.
- Play verification: locked Brownie ray, available Cookie ray, initial gate, row structure, wall placement, and transition to `Movement Tutorial / 1 / 13 BASIC CONTROLS` passed. Marker: `RECIPE_CLIPBOARD_QA PASS=True; structure=True; initialGate=True; lockedRay=True; cookieRay=True; position=True`.
- Restart regression passed: selecting cookies then invoking production `RestartRecipe()` retained `chocolate-chip-cookies` and returned the reloaded scene to `Movement Tutorial`; marker `RECIPE_SELECTION_RESTART_QA PASS=True`.
- One preliminary dynamic QA command imported disallowed `System.Reflection`; Unity MCP rejected it before compilation/execution, so it changed no state. Public-state replacement passed. A requested `ScreenCapture.CaptureScreenshot` did not emit its Library PNG in the background Editor; the already successful direct scene render was retained as visual evidence and no project asset/state changed. Final audit: clipboard true; rows 3; ready 1; preview/QA objects 0; scene `dirty=false`; Play Mode stopped; Console zero errors/warnings. Brownie/cupcake runtime activation, dynamic definition switching, and their guide branches remain deliberately unimplemented. Performed by Codex main agent through `apply_patch` and Unity Editor commands; no delegation or commit.

## 2026-09-10 17:11 +08:00 Separate framed recipe-sheet revision initiated

- User clarification: the supplied image is a visual reference for several individual framed recipe sheets. Replace the one large shared clipboard with separate wall pieces—one per approved dish—under the BakeIT sign.
- Planned scene-only revision: remove exactly `Kitchen Environment/Recipe Clipboard` and replace it with a parent grouping three individually framed cream papers for Chocolate-Chip Cookies, Brownies, and Cupcakes. Preserve the existing selector components, recipe IDs, one-ready/two-in-development availability, `E` interaction behavior, guide gate, UI scale, and all kitchen/equipment objects.
- Baseline: old clipboard present with eight children and three recipe rows; Play Mode stopped; Console zero errors/warnings. The live scene reports `dirty=true`, so it will not be reloaded; existing unsaved user state will be preserved when the requested replacement is saved. No recipe ingredients, steps, status, gameplay code, package, ProjectSettings, manuscript, or unrelated transform change is planned. Performed by Codex main agent; no delegation or commit.

## 2026-09-10 17:16 +08:00 Separate framed recipe-sheet revision completed

- Removed exactly `Kitchen Environment/Recipe Clipboard` and replaced it at the same center with `Kitchen Environment/Recipe Selection Posters`. Three independent poster roots are spaced at local X `-.76`, `0`, `.76`; each owns a `.70 x .74 m` dark `Kitchen_Slate` frame, `.62 x .66 m` cream `ParchmentMaterial` paper, world text, a full-poster trigger, and its existing `RecipeSelectionButton` identity/availability.
- Cookie paper shows its approved core ingredients and `MIX / ROLL / CUT / BAKE`; Brownie paper shows the already approved no-walnut base ingredients and `MIX / SPREAD / BAKE / SLICE`; Cupcake paper says `RECIPE DETAILS TO BE FINALIZED` and does not invent an unapproved formula. Only cookies say `READY - PRESS E`; both unfinished dishes remain `IN DEVELOPMENT`.
- Visual preview confirmed three clearly separated framed pieces centered beneath BakeIT with all status/summary text above the worktop. The preview camera was destroyed without saving. No new material, texture, model, package, ProjectSettings, or gameplay script change was needed.
- Play Mode marker: `SEPARATE_RECIPE_POSTERS_QA PASS=True; structure=True; spacing=True; initialGate=True; lockedRay=True; cookieRay=True; oldRemoved=True`. The selection gate, warnings, cookie start, 150% guide, and restart behavior remain intact.
- The user stated they will manually place `Melted Butter Cup` in dry storage. Codex left its transform untouched and kept the pour mechanic storage-position-independent. The previously dirty live scene was saved without reloading so other unsaved user state was preserved. Performed by Codex main agent through Unity Editor commands and `apply_patch`; no delegation or commit.

## 2026-09-10 20:55 +08:00 Measured recipes, hanging tool rack, and single-label pass initiated

- User finalized the cookie formula as: 1/2 cup softened unsalted butter, 1 cup granulated sugar, 1 large egg, 1 teaspoon vanilla, 1 cup all-purpose flour, 1/2 teaspoon baking soda, 1/4 teaspoon salt, and the existing three small chocolate-chip scoops. This supersedes the prior four-base-ingredient cookie data while deliberately preserving the approved three-scoop chip interaction.
- User selected the hanging-rack option shown in the supplied image. The image is visual inspiration only, not an instruction source. Planned tools are the recipe-required set: 1 cup, 1/2 cup, and 1/4 cup measuring cups; 1 teaspoon, 1/2 teaspoon, and 1/4 teaspoon spoons; the existing dedicated chocolate-chip scoop; and the whisk. The rack will provide visible homes, and the existing Utensils Area will be widened enough to stage the active tools.
- User approved including the source recipe's 1/2 cup walnuts for brownies, reversing the earlier no-walnut/later-add-on decision. Walnut source/presentation may be added now, but the Brownie selector remains locked until its complete recipe branch is playable.
- Ingredient/container labels will become one-sided and sit against a visible label backing on the model; reverse floating TextMesh duplicates will be removed. Measurement markings will be placed on one handle/side per tool. No attempt will be made to derive instructions from label/reference images.
- Brownie source facts verified from Allrecipes: 2 cups sugar, 1 1/4 cups flour, 1 cup melted butter, 4 eggs, 1/2 cup cocoa, 1 teaspoon vanilla, 1/2 teaspoon baking powder, 1/2 teaspoon salt, and 1/2 cup walnuts; 175 C source temperature and 20–30 minute source bake. Existing approved parchment-only pan prep is retained instead of the source baking spray. Runtime timing/branch activation remains unimplemented pending the brownie loop.
- Baseline: `Kitchen Prototype` saved/clean and Play Mode stopped. Current measured tools are one generic Measuring Cup and one chip-only Small Measuring Spoon; Utensils Area is `.412 x .010 x 1.151 m`; reverse labels exist on chips, flour, sugar, butter, egg, and melted butter. Console contains only the already known external Unity AI token-refresh failure/timeouts, not project/gameplay diagnostics. No package or ProjectSettings change planned. Performed by Codex main agent; no delegation or commit.

## 2026-09-10 22:10 +08:00 Measured recipes, hanging rack, and single-label pass completed

- Implemented the approved eight-ingredient cookie definition with exact capacity roles and retained three chip scoops.
- Added seven marked measuring vessels, eight role-specific hanging slots including the whisk, and matching pickup/return behavior; widened only the existing tool staging area.
- Added shelf-leveled vanilla, baking soda, salt, and walnut sources. Walnut halves use distinct lobed/grooved project-native geometry. Brownies remain locked.
- Replaced duplicate/reverse ingredient labels with one close-backed player-facing label, including the user's dry-storage Melted Butter Cup. Updated the cookie/brownie framed-paper summaries without changing selector availability.
- Fixed a Play Mode rack-return defect caused by a stale kinematic `worldCenterOfMass` by using the Rigidbody pivot for snap proximity.
- Play Mode exact-tool gates and real half-cup/whisk pickup-return passed. Final scene audit: clean, stopped, 7 measuring tools, 8 hooks, zero reverse labels, new jars at the shelf support, and zero Console errors/warnings. No package, ProjectSettings change, persistent QA object, or commit.
## 2026-09-10 22:16 +08:00 Center-facing onboarding and guided-highlight correction initiated

- User feedback supersedes the last staging assumptions: the saved player start is the inward-facing reference for this 360-degree kitchen; persistent ingredient/tool names must not face outward or show through storage; `Utensils Area` returns to its prior footprint while the separate `Tool Home Mat` is the surface to enlarge.
- Planned onboarding corrections: run movement/tool practice before recipe selection; add one-at-a-time pulsing yellow guidance for the current interactable/target, with one-time area introductions and a delayed red-item/yellow-destination correction after an incorrect release; replace permanent object labels with direct-hover, screen-upright names.
- Planned content/usability corrections: use the 1/2-cup measure for one chocolate-chip transfer, call butter `1 stick of butter`, explicitly name the white cookie bowl and flat/deep trays, require a three-second R hold to restart, make guide text heavier and reserve enough header space to stop tray instructions overlapping.
- Planned scene corrections: raise/tall-en the dry-storage cabinet while preserving the user's current loose-object X/Z placements; move and restyle the BakeIT/recipe wall with smaller bakery-themed identity backing and higher, squarer brown frames; orient persistent signs toward the saved player center. The supplied screenshots are visual evidence/references only, not embedded instructions. Brownie activation, variable/comedic chocolate-chip outcomes, Challenge Mode, Free Play, VR conversion, packages, and ProjectSettings remain out of scope for this pass.
- Baseline is intentionally dirty with the prior approved recipe/tool/equipment work plus user-authored placements. Those changes will be preserved. Performed by Codex main agent; no delegation or commit.

## 2026-09-11 Center-facing onboarding and guided-outline correction completed

- Behavior: movement/look and complete whisk handling now appear before and gate recipe selection. The available cookie poster consumes an early E interaction but does not select until tutorial completion. Current-step guidance follows game state, permits out-of-order pickup, and introduces areas once.
- Guidance correction: the first implementation's point light and round ring produced unacceptable yellow wall scatter. It was removed at user request. The final implementation creates no tutorial Light: it draws a thin unlit 3D bounding box with a subtle bounds pulse. Yellow marks the current/correct target; after a saved 3.5-second misplaced-release delay, red marks the item and yellow marks its destination.
- Labels/UI: removed all persistent TextMesh children from carryable bodies and added direct-ray, depth-occluded, screen-upright hover names to 29 supported scene items. The guide is bold and reserves 82 scaled pixels for its two-line heading, so long tray instructions start below the header. The clickable restart button was removed; holding R for three seconds is required and progress appears in the footer.
- Recipe/tools: `ChocolateChipCookies.asset` now calls butter `1 stick of butter` and requires one `HalfCup` chocolate-chip transfer. The scene `1/2 Cup Measuring Cup` accepts and renders chips. The dedicated chip scoop and hook were removed; six measuring vessels plus whisk remain on seven exact hooks. Mise-en-place and guide requirements were updated accordingly.
- Scene/layout: restored `Utensils Area` X scale `.86 -> .412`; enlarged `Tool Home Mat` to `.76 x .68`; raised dry storage to Y scale `1.18` and moved its loose shelf contents upward `.073 m` without changing X/Z; raised and squared the three brown-framed recipe papers; made the BakeIT panel smaller/higher/warm brown with simple cookie emblems. Cookie wording explicitly names `WHITE COOKIE BOWL` and `FLAT COOKIE TRAY`; the locked Brownie poster names `DEEP TRAY`.
- User continuation: the user manually fixed remaining world-text orientation. Those saved rotations are authoritative. The continuation performed no later batch rotation or replacement of fixed text.
- Validation: Edit audit passed exact recipe data, six vessels/seven hooks, zero carryable-body TextMesh, hover count, cabinet/tool/poster layout, and clean scene. Clean Play Mode passed movement-first guide, early-selection gate, one yellow Whisk outline, zero tutorial Lights, direct Whisk hover, 1/2-cup chip fill, red-item/yellow-destination correction, and `.2 / 2.99 / 3.01 s` restart threshold cases. Play Mode stopped cleanly with zero preview roots and Console 0 errors / 0 warnings.
- Scope intentionally unchanged: Brownie/Cupcake runtime activation, variable/comedic chip quantity results, Challenge/Free Play behavior, VR controls, scoring, packages, ProjectSettings, cloud, dashboard, and manuscript remain untouched. Performed by Codex main agent; no delegation or commit.

## 2026-09-11 Held-item hover-name suppression

- Updated the player hover-label controller so it clears and withholds every hover name whenever `PickupController` reports a held object. Names remain available for direct crosshair inspection only while the player's hands are empty.
- This is a script-only interaction correction. The user's saved world-text rotations and all scene placements were left untouched.
- Focused Play Mode verification showed `Whisk` with empty hands and an empty hover label immediately after the real pickup controller held it. Unity compilation passed.

## 2026-09-11 Solid-black recipe typography

- Changed the nine TextMesh elements beneath `Recipe Selection Posters`—dish titles, availability statuses, and recipe details—to solid black for stronger contrast on the cream paper.
- Scope was limited to the three framed recipe sheets. The BakeIT sign, tutorial UI, object-hover labels, saved text rotations, and gameplay logic were not modified.
- Saved-scene verification found 9/9 recipe labels black, 19 TextMesh elements outside the poster root untouched, Play Mode stopped, and the scene clean.

## 2026-09-11 Recipe title and difficulty refinement

- Restored the three dish titles to the existing bakery-orange accent while retaining solid-black detail text.
- Replaced the poster status copy with difficulty labels: Cookies `EASY`, Brownies `MODERATE`, and Cupcakes `INTERMEDIATE`. The repeated “brownies” in the request was treated as the apparent Cookies/Brownies typo; Cupcakes remains Intermediate because the planned icing interaction adds coordination without yet constituting a hard recipe.
- Selector availability was not changed: Cookies remain playable, while Brownies and Cupcakes remain locked in runtime behavior. The longest new line measures `.384 m`, within the `.62 m` recipe paper width.

## 2026-09-11 Brownie workflow implementation initiated

- Objective: begin the approved Brownie branch using the Allrecipes `Quick and Easy Brownies` basis and the already approved large bowl, melted-butter pour, exact measures, walnuts, parchment-lined deep pan, spatula spreading, 175 C oven target, and 4-by-3 playable slicing into 12 equal squares.
- First milestone: add a Brownie `RecipeDefinition` and extend the current recipe/bowl foundation without duplicating managers or changing the working Cookie path. Brownie selection will remain locked until its complete mixing, pan, bake, slicing, guide, and restart path passes.
- Cooling remains optional and no required cooling gate will be added in this milestone. Prototype bake duration remains to be selected before poster activation. No toppings beyond the approved walnuts, scoring, modes, VR bindings, packages, or ProjectSettings changes are authorized.
- Baseline: `Kitchen Prototype` saved and clean, Play Mode stopped, Console 0 errors / 0 warnings. Existing user-authored equipment and text rotations are authoritative and will be preserved. Performed by Codex main agent; no delegation or commit.

## 2026-09-11 Full recipe-poster wording

- Replaced abbreviated, slash-separated Cookie and Brownie summaries with one fully written ingredient per line and one process step per line. Cookie wording includes `1 stick softened unsalted butter`; Brownie wording names the `deep brownie pan` and `12 squares`.
- Cupcakes continue to say `Recipe details to be finalized`; no cupcake ingredients were inferred. Its two approved high-level actions remain on separate `Bake` and `Decorate` lines.
- Changed only the three `Recipe Summary` TextMesh elements: character size `.0065/.0065/.0037 -> .0043`, line spacing retained at `.9`, and initially set local Y to `-.10`. The user subsequently adjusted the live Cookie/Brownie summary layout to Y `.126`; Codex detected the dirty scene and saved those user-authored values before Brownie implementation. Titles, difficulty labels, user-authored rotations, and selection logic were preserved.
- Saved-scene bounds: Cookie `.310 x .279 m`, Brownie `.296 x .301 m`, and Cupcake `.378 x .116 m`, each inside its `.620 x .620 m` paper. No summary contains the previous ` / ` separator.

## 2026-09-11 Brownie recipe-data foundation completed

- Created `Assets/BakeIT/RecipeDefinitions/Brownies.asset` and bound it to the existing `PourableIngredientReceiver` beneath `Brownie Mixing Bowl`; no duplicate scene manager or second melted-butter receiver was added.
- Recipe data: 2 one-cup Sugar measures; Flour as one 1-cup plus one 1/4-cup measure; 1 cup MeltedButter; 4 Eggs; 1/2 cup Cocoa; 1 teaspoon Vanilla; 1/2 teaspoon BakingPowder; 1/2 teaspoon Salt; and 1/2 cup Walnuts. Runtime result names use Brownie-specific batter/portion/doneness identifiers.
- Process data: 4 base mix passes, 2 finishing walnut passes, pour-into-pan shaping with 4 spread passes, exactly 12 portions, parchment preparation, no required post-bake finish, 8-second prototype bake, 170–180 C success range centered on 175 C, and burnt at 210 C or above. Cooling remains optional and un-gated.
- Extended `RecipeDefinition` with tool-specific lookup across repeated ingredient IDs. `MeasuringScoop.CanMeasure` now delegates to that shared lookup, allowing Brownie Flour to validly require both the 1 Cup and 1/4 Cup without weakening Cookie tool restrictions.
- Extended the existing `PourableIngredientReceiver` with a recipe reference. Its original held/tilted/surface/duplicate gates remain; recipe data now validates the melted-butter ingredient instead of relying only on a disconnected string.
- User-authored poster layout was saved before these changes. Brownie/Cupcake selection remains locked; no measured Brownie intake, batter completion, pan/spatula behavior, slicing, guide branch, or runtime activation was claimed.

## 2026-09-11 Brownie measured-intake and mixing milestone completed

- Extended the existing `PourableIngredientReceiver` into the Brownie bowl's measured intake/mixing owner instead of creating a second manager. It tracks requirements by ingredient plus exact tool, so the 1-cup and 1/4-cup Flour transfers cannot satisfy each other. It accepts four whole Eggs and the existing deliberate melted-butter pour.
- Added `MeasuringScoop` routing to the Brownie receiver plus an active-recipe setter for future selector wiring. Brownie tools accept Sugar, both Flour measures, Cocoa, Vanilla, Baking Powder, Salt, and Walnuts according to `Brownies.asset`; the Cookie definition continues to reject Brownie-only ingredients.
- Brownie mixing uses `.06 m` fresh travel and the existing `1.2 s` cooldown: 4 base passes, then the 1/2-cup Walnut finishing measure, then 2 finishing passes. Early Walnuts, late base ingredients, excess measures, wrong tools, and mixing before all base requirements are rejected without consuming a valid held measure.
- Added scene objects `Cocoa Powder Container` at `(1.25,.478,3.08)`, `Baking Powder Container` at `(1.36,.478,3.08)`, and `Food_Egg 2` through `Food_Egg 4` beside the original fridge egg at `.10 m` X intervals. Both jars reuse the existing carryable measured-source jar structure; Cocoa has a project-owned brown fill material and both use hover names rather than persistent text.
- Added collider-free `Brownie Batter In Bowl` beneath the user's large bowl at local `(0,.032,0)`, base scale `(.074,.0035,.074)`, using new project-owned `Brownie Batter.mat`. It replaces the butter-only surface once other ingredients arrive, increases fill depth with measured progress, and darkens through base/finishing mixing.
- Preserved the user's saved recipe-poster transforms and all existing equipment placements. Brownie selection stays locked. Pan pouring/spreading, oven integration, 4-by-3 slicing, guide flow, and restart are the next milestone.
- Two QA-script corrections were required: an unavailable `Transform.GetHierarchyPath` helper caused a compile-only audit failure, and two pickup audits initially used `GameObject.Find` for an inactive batter/consumed egg. No production code or saved scene state changed in those failed audits; corrected scene-inclusive lookups passed.

## 2026-09-11 Brownie development branch made playable

- At the user's request, the Brownie recipe paper is now selectable after the existing movement-and-whisk tutorial even though the downstream pan workflow is unfinished. Cupcakes remain locked and Cookies remain fully playable.
- `Recipe Selection Posters` now catalogs both `ChocolateChipCookies.asset` and `Brownies.asset`. Selecting a paper changes the active recipe used by every measuring vessel, so Brownie-only Cocoa, Walnut, and compound Flour measures work without weakening Cookie restrictions.
- Added a Brownie-specific guide branch for placing the large bowl, completing every exact base measure and four Eggs, performing four base whisk passes, adding the half-cup Walnut measure, and performing two finishing passes.
- Once the batter is complete, the guide stops at the explicit boundary `NEXT PART STILL IN DEVELOPMENT` with `Pan spreading and baking will be added next`. It does not route Brownie state into the Cookie tray or oven workflow and does not claim that pan spreading, baking, or slicing is implemented.
- Play Mode passed the real tutorial gate and recipe switch (`BROWNIE_SELECTION_QA pass=True`) and the complete measured-intake/mixing path through the boundary (`BROWNIE_BOUNDARY_QA pass=True`). A first QA-only reflection attempt was rejected by the Unity command sandbox, and a second QA-only input attempt used an unsupported bitfield state call; neither changed production code or saved scene state. The successful gate test used queued Input System device states and the normal public pickup/placement paths.

## 2026-09-11 Brownie mise-en-place and highlight correction

- Player testing exposed a contradictory first Brownie screen: the UI asked for the large bowl while the Cookie-oriented highlight director outlined the fridge, and the flow skipped preparation entirely.
- Added a separately latched Brownie mise-en-place path to the existing station. It requires four large Eggs from the fridge; Flour, Sugar, melted Butter cup, Cocoa, Vanilla, Baking Powder, Salt, and Walnuts from dry storage; the 1-cup, 1/2-cup, 1/4-cup, 1-teaspoon, and 1/2-teaspoon measures plus Whisk; then the Brownie Mixing Bowl in MIX.
- The guide and yellow outline now advance together through those Brownie-specific groups. After preparation, the outline follows the active requirement: correct empty measuring tool, ingredient source, filled-tool destination, Whisk, or Brownie bowl. Cookie-only Butter, Chocolate Chips, Baking Soda, 1/4-teaspoon spoon, and white bowl are not part of the Brownie preparation checklist.
- Fridge/cabinet introduction latches as soon as a door starts moving, so the next item can be highlighted without waiting for the full door animation. Corrected the quantity copy from `2 1 cups` to `2 cups` while retaining fractional measurements such as `1/4 cup`.
- Clean Play Mode regression passed the full UI/outline sequence from Brownie selection through first measuring target (`BROWNIE_TUTORIAL_FINAL_QA pass=True`) and confirmed the Cookie tutorial still starts unchanged (`COOKIE_TUTORIAL_REGRESSION pass=True`).

## 2026-09-11 Contour guidance, held eggs, and Brownie sugar presentation

- Replaced the tutorial's rectangular `LineRenderer` cages with a close mesh-silhouette outline using `BakeIT/Tutorial Silhouette Outline`. Yellow and red guidance now follows visible object shapes, uses no point light or colliders, and does not scatter light across nearby walls.
- A highlighted pickup stops outlining as soon as it is held. During mise en place, guidance moves to the item's intended rest instead.
- Eggs now opt into crosshair-centered holding. Kinematic held bodies also synchronize their rendered Transform immediately with their Rigidbody target, preventing a newly picked-up Egg from visually remaining beside or below the crosshair.
- Enlarged the HUD crosshair from `6 x 6` to `10 x 10` pixels.
- Corrected the Brownie bowl contents presentation. Before Cocoa is added, measured dry ingredients render off-white rather than chocolate brown. The contents height now scales from the bowl's saved `.0035` base height instead of replacing it with a world-sized `.45` to `1.0` value, removing the tall brown column.
- Ingredient container meshes and the final Brownie batter model were deliberately left unchanged pending the user's references.

## 2026-09-11 Outline visibility adjustment

- Increased the close tutorial contour from a `5.5 ± .35` pixel pulse to `7.25 ± .6` pixels and shifted the yellow toward a brighter warm tone. This makes the guided object easier to notice while retaining the non-scattering silhouette design.

## 2026-09-11 Teacher playtest usability pass

- The first external teacher playtest found that held objects were difficult to recover after snagging, especially after the scroll wheel had moved them far from the player. Reduced the saved/default recovery threshold from `.15 m` to `.075 m`. A successful safety teleport now resets the object to the nearest `.60 m` hold distance instead of retaining the accidental far-scroll distance.
- The same playtest found measured ingredient collection too precise for a first-time player. Added a `.045 m` cup-mouth assistance radius. It only considers compatible nearby ingredient sources while the correct cup is held, empty, and upright; recipe quantities, tool selection, pouring, and waste rules are unchanged.
- Fixed the reported flat-pad highlight bug. `StablePlacementSurface` targets now receive a four-edge, non-filled yellow perimeter rather than a silhouette shell that could expose and color the underside of a thin mat.
- Strengthened object contours to a brighter yellow `9.75 ± 1.25` pixel pulse. Placement-pad perimeters pulse between `.015 m` and `.021 m`. Both remain unlit guidance with zero scene lights.

## 2026-09-11 Sugar-jar reference asset pass initiated

- Objective: replace only the `SugarContainer` milk-bottle placeholder with a reusable sugar-jar visual based on the supplied clamp-lid glass-jar reference. Preserve the existing Sugar ingredient identity, measuring source, scoop trigger, pickup/physics components, saved scene position, and recipe behavior.
- Scope is deliberately one asset at a time at the user's request. The flour tin and the remaining referenced ingredient assets will not be started in this pass. Existing Egg and melted-butter models remain approved and unchanged.
- Planned presentation: realistically scaled clear square jar, visible off-white granulated sugar, open hinged lid, metal bail/clasp details, and no persistent world-space item text. Target closed jar body is approximately `0.13 m` wide and `0.20 m` tall in world space; the open lid may extend above it.
- Baseline inspection: `SugarContainer` retains `Rigidbody`, `PreparationItem`, `HoverDisplayName`, a convex root `MeshCollider`, and child `SugarScoopZone` with its `MeasuredIngredientSource`. Its saved Transform is `(1.69, 0.48, 3.09)`, rotation `(0,0,0)`, scale `(.69,.69,.69)`.
- Failed generation attempts: Unity's image-conditioned Tripo P1 entry rejected both the imported Texture2D and Sprite instance IDs as nonexistent, while prompt-only validation rejected the missing mandatory image. The failures occurred before generation began, so no model-generation usage was consumed. The approved fallback is a local procedural Unity asset.
- Baseline Unity Console contains only the six recorded asset-generator validation errors above and no warnings; these are not gameplay or compilation errors. Performed by Codex main agent; no delegation or commit.

## 2026-09-11 Sugar-jar reference asset completed

- Created the reusable visual prefab `Assets/BakeIT/Generated/Sugar Jar/Sugar Jar Visual.prefab` from local Unity geometry after the image-conditioned generator rejected its reference IDs. Supporting assets include three rounded meshes plus separate transparent glass, off-white granulated sugar, metallic clamp, and warm seal materials.
- The final asset resembles the supplied reference: a softly rounded square glass jar with a genuinely open mouth, visible white sugar, four-part rim, an open hinged glass lid, rear hinge hardware, and a front wire clamp/bail. It uses 20 renderer parts and deliberately contains zero colliders.
- Replaced only the `SugarContainer` placeholder presentation. Disabled its old root milk-bottle renderer and unnamed placeholder child; replaced the moving convex `MeshCollider` with a simple local `BoxCollider` at center `(0,.125,0)` and size `(.184,.250,.184)`. The saved root Transform, `Rigidbody`, `PreparationItem`, `HoverDisplayName`, `SugarScoopZone`, and `MeasuredIngredientSource` were preserved.
- At saved root scale `.69`, the rounded body is approximately `.123 x .156 x .123 m`; the open-lid visual bounds are approximately `.12 x .27 x .18 m`. This pass establishes the relative-size baseline for later pantry assets without changing those assets yet.
- Added `Assets/BakeIT/Editor/SugarJarAssetSetup.cs` as an explicit rerunnable builder. It writes only to the dedicated Sugar Jar generated folder and reapplies only `SugarContainer`.
- Play Mode verification passed real pickup/release for the jar and the 1-cup measuring tool's Sugar compatibility/fill/empty path. Final runtime structure reported `renderers=20`, `visualColliders=0`, and source name `Sugar`.
- One first QA cleanup attempt wrote velocity to an already kinematic body, producing two QA-only warnings; the corrected final Play Mode run made no such writes and passed. A compile-only audit also used deprecated `GetInstanceID`, and a later console-clear attempt imported a disallowed reflection namespace; neither executed nor changed the project. These tooling diagnostics are documented and are not production warnings.
- No flour/cocoa/baking-powder/salt/vanilla/chip/walnut/butter asset was started. Existing Egg and melted-butter visuals remain unchanged as approved. Performed by Codex main agent; no delegation or commit.

## 2026-09-12 Three pantry-model pass initiated

- User approved three additional models. The next supplied references are being handled in their established order: plain/all-purpose flour tin, cocoa powder tin, and baking powder can.
- Each will be built and verified as its own reusable local Unity prefab before moving to the next. The existing `FlourContainer`, `Cocoa Powder Container`, and `Baking Powder Container` roots, saved positions, Rigidbody/pickup behavior, hover labels, preparation state, measured-source triggers, and exact recipe identifiers will remain authoritative.
- Relative size targets use the accepted Sugar Jar as baseline: flour tin approximately `.14 x .20 x .11 m`; cocoa tin approximately `.10 x .16 x .10 m`; baking powder can approximately `.095 x .15 x .095 m`. Openings must remain accessible to the existing measuring system.
- Salt, vanilla, chocolate-chip bowl, chopped-walnut bowl, and wrapped butter remain outside this pass. Existing Egg and melted-butter visuals stay unchanged.
- Baseline scene is saved/clean and Play Mode is stopped. The Console has zero errors and one external Unity AI MCP executable-signature warning; it is unrelated to game code or these objects and is covered by the existing service/tooling limitation. Performed by Codex main agent; no delegation or commit.

## 2026-09-12 Three pantry-model pass completed

- Created three independent reusable local prefabs from the supplied references: `Generated/Flour Tin/Flour Tin Visual.prefab`, `Generated/Cocoa Powder Tin/Cocoa Powder Tin Visual.prefab`, and `Generated/Baking Powder Can/Baking Powder Can Visual.prefab`. The three source images were retained under `Assets/BakeIT/References/`.
- Flour: replaced the milk-bottle placeholder with an ivory rounded rectangular metal tin, open visible flour, red raised lid, metal rim/hinges/knob, bordered geometric label, and wheat emblem. The closed body is approximately `.138 x .200 x .110 m` in world scale. Its open-lid visual bounds are approximately `.14 x .32 x .17 m`.
- Cocoa: replaced the generic capsule jar children with a red `10.4 cm`-diameter, `16 cm`-high tin, silver rims and open lid, visible dark cocoa, cream bordered label, and cocoa-bean emblem. Open-lid bounds are approximately `.11 x .27 x .16 m`.
- Baking powder: replaced the generic capsule jar children with a cream/red `9.4 cm`-diameter, `15 cm`-high can, silver rims, open translucent lid, visible off-white powder, and navy geometric badge. Open-lid bounds are approximately `.10 x .25 x .15 m`.
- All three visual prefabs are collider-free. The authoritative roots retain their saved Transforms, Rigidbodies, `PreparationItem`, `HoverDisplayName`, and measured-source trigger/ingredient IDs. Flour now uses a simple local body `BoxCollider` center `(0,.145,0)`, size `(.205,.292,.165)`; Cocoa uses `(0,.080,0)` / `(.108,.160,.108)`; Baking Powder uses `(0,.075,0)` / `(.100,.150,.100)`.
- Added `Assets/BakeIT/Editor/PantryContainerAssetSetup.cs` with separate rerunnable menu actions for each prefab. Product identity is suggested through modeled color, borders, and emblems rather than persistent carried-object TextMesh labels; hover names remain the readable interaction labels.
- Play Mode: all three real roots passed pickup/release. With `Brownies.asset` active, 1 Cup accepted Flour, 1/2 Cup accepted Cocoa, and 1/2 Teaspoon accepted BakingPowder. 1 Cup correctly rejected Cocoa and 1 Teaspoon correctly rejected BakingPowder. All tools emptied/released after the test and all three visual hierarchies reported zero colliders.
- Corrections/failed attempts: a first flour preview was misread as backface/transparency, so triangle winding was reversed; the next preview proved that reversed winding hid the rounded meshes, and the change was reverted. The final opaque Flour material was explicitly verified at Surface `0`, alpha `1`, render queue `2000`. A first QA command failed compilation because its output variable reused the name `emptied`; the corrected command compiled and passed. Neither failed attempt saved runtime state or altered gameplay logic.
- No hosted model generation was called, so these three models consumed no generation usage. Salt, vanilla, chocolate-chip bowl, chopped-walnut bowl, and butter were not changed; Egg and melted butter remain unchanged. Performed by Codex main agent; no delegation or commit.

## 2026-09-12 Salt, vanilla, and chocolate-chip model pass initiated

- User approved the next three reference-driven models in sequence: rustic Salt Cellar, Vanilla Extract bottle, and clear/white Chocolate-Chip prep bowl.
- Preserve `Salt Container`, `Vanilla Extract`, and `ChocolateChipContainer` saved Transforms, Rigidbody/pickup behavior, `PreparationItem`, hover labels, measured-source triggers, ingredient IDs, and existing recipe quantities. The chip bowl's established hollow compound colliders remain authoritative.
- Relative-size direction follows the user's explicit note that Salt should not appear larger than Vanilla: target Salt body about `.06 x .08 x .06 m`, Vanilla bottle about `.045 x .15 x .045 m`, and chip bowl about `.17 x .075 x .17 m`. Open lids/spoon hardware may extend presentation bounds without enlarging body colliders.
- Walnut bowl and wrapped Butter remain outside this pass; Egg and melted Butter remain unchanged. Baseline scene is saved/clean, Play Mode stopped, and Console has zero errors/warnings. Performed by Codex main agent; no delegation or commit.

## 2026-09-12 Salt, vanilla, and chocolate-chip model pass completed

- Created three independently rerunnable, reference-driven local Unity prefabs: `Generated/Salt Cellar/Salt Cellar Visual.prefab`, `Generated/Vanilla Extract Bottle/Vanilla Extract Bottle Visual.prefab`, and `Generated/Chocolate Chip Bowl/Chocolate Chip Bowl Visual.prefab`. Their supplied reference images are retained under `Assets/BakeIT/References/`.
- Salt: replaced the capsule placeholder with a compact ivory ceramic cellar, visible salt surface, raised open lid and knob, rustic front plaque, and wooden spoon. The physical body collider is `.066 x .082 x .066 m`; decorative spoon/lid hardware intentionally extends the total visual bounds to approximately `.12 x .14 x .10 m`.
- Vanilla: replaced the capsule placeholder with a tall clear bottle, amber contents, modeled shoulder/neck/lip, cream-and-gold flower label, and detached green cap. The physical body collider is `.050 x .154 x .050 m`; visual bounds are approximately `.08 x .16 x .06 m`, keeping its bottle body visibly taller than the Salt cellar.
- Chocolate chips: replaced the old bowl rendering with a reusable white ceramic hollow bowl and three interleaved layers of the existing dark-chocolate chip cluster. Visual bounds are approximately `.18 x .11 x .18 m`. The existing `Chip Bowl Base` plus eight `Chip Bowl Rim` colliders remain active, preserving the established hollow compound collision.
- All three new visual hierarchies contain zero colliders and no persistent item-name text. The authoritative roots retain saved Transforms, Rigidbodies, preparation state, hover labels, and measured triggers. Source IDs remain exact: `Salt`, `Vanilla`, and `ChocolateChips`.
- Play Mode pickup/release passed for all three roots. With `Brownies.asset`, 1/2 Teaspoon accepted Salt and 1 Teaspoon accepted Vanilla; the inverse wrong-tool tests were rejected. With `ChocolateChipCookies.asset`, 1/2 Cup accepted Chocolate Chips while 1 Cup rejected them. Every successful measure filled, emptied, and reset correctly.
- Corrections/failed attempts: the first builder compile exposed a missing rotated-cube helper overload and was fixed before any scene mutation. A later Console-clear-only command failed compilation because `Editor` resolved to the command namespace, and the corrected reflection attempt was rejected by the command sandbox; neither affected saved assets, scene state, or gameplay. The normal validation commands compiled and passed.
- No hosted model generation was used, so this pass consumed no model-generation usage. Walnut bowl and wrapped Butter remain unchanged and are the only supplied-reference models left in this sequence; existing Egg and melted Butter remain approved as-is. Play Mode is stopped, the scene is saved/clean, and no temporary preview objects remain. Performed by Codex main agent; no delegation or commit.

## 2026-09-12 Flour-fill and chocolate-chip fullness correction

- The player's close-up screenshot exposed that the Flour Tin could read as completely empty and its wheat-only front panel did not identify the product. Raised its off-white flour surface to sit immediately below the open rim and added a fitted, modeled `FLOUR` wordmark to the package label. This is geometry rather than a floating TextMesh name, so held-item hover suppression remains unchanged.
- Reworked the Chocolate-Chip Bowl from three sparse-looking cluster layers to a contained packed chocolate bed with six overlapping chip-cluster layers. The white ceramic exterior remains visible and the established nine-part hollow compound collider remains authoritative.
- Made the two rerunnable asset builders robust against Unity's stale custom-mesh cache: custom mesh assets are updated in place to preserve GUIDs, and saved visual prefabs are force-reimported before their scene instances are created.
- Final Play Mode regression passed Brownie 1 Cup Flour fill/empty, Cookie 1/2 Cup ChocolateChips fill/empty, and Cookie 1 Cup chip rejection. Both presentation hierarchies remain collider-free; the scene is stopped, saved, clean, and the Console reports zero errors/warnings. No hosted generation was used.

## 2026-09-12 Grounded Chocolate-Chip Bowl correction

- Removed the vertically stacked chip presentation that could read as loose chips floating above the bowl.
- The bowl now uses six differently scaled and rotated clusters. Each cluster's Y position is calculated from the shared mesh's actual lowest vertex and embedded into the packed chocolate bed; only scale creates the shallow mound.
- The two former upper layers are disabled in the current prefab and are omitted entirely whenever the rerunnable builder is next applied. Ingredient identity, scoop trigger, root physics, and the nine-part hollow bowl collider are unchanged.

## 2026-09-12 Walnut Bowl and Wrapped Butter models completed

- Added `Assets/BakeIT/Editor/RemainingIngredientAssetSetup.cs` with independent rerunnable menu actions for the chopped-walnut bowl and wrapped Butter stick.
- Added reusable collider-free prefabs and supporting materials under `Assets/BakeIT/Generated/Walnut Bowl/` and `Assets/BakeIT/Generated/Wrapped Butter/`; copied the supplied turnarounds to `Assets/BakeIT/References/Walnut Bowl Reference.png` and `Wrapped Butter Reference.png`.
- Replaced the walnut jar presentation with a clear `.145 m`-wide prep bowl containing a grounded, uneven walnut bed plus modeled chopped kernels and walnut halves. The prior tall jar, fill, lid, and decorative walnut children are disabled; the `Walnuts` measured source remains unchanged.
- Replaced the Butter root's plain primitive renderer with a realistically proportioned 120 x 30 x 30 mm wrapped stick. Wax-paper panels, folded end seams, top crease, and readable modeled blue `BUTTER` marks are geometry rather than carried-object TextMesh labels.
- Fitted the walnut root collider to `.145 x .070 x .145 m` and the non-uniform Butter root collider to local `(1,.60,.4286)`, producing a world-space `.120 x .030 x .030 m` shell without changing the saved Butter root scale.
- Final live audit passed exact identities `Walnuts` and `Butter`, preserved root poses and interaction components, zero visual-prefab colliders, zero active walnut placeholders, a saved/clean scene, and zero temporary preview objects. Existing Egg and melted Butter remain unchanged as requested.

## 2026-09-12 Teacher-test tool and workstation visibility polish

- Refinished the six measuring vessels with a shared warm-copper URP Lit material so a dropped cup or teaspoon contrasts clearly against the gray floor. The material is metallic but explicitly non-emissive, so it does not create glow or bloom.
- The copper finish is assigned only to each measuring tool's root shell renderer. Flour/chocolate-chip fill visuals, tool capacities, pickup components, colliders, hanging slots, and measuring behavior remain unchanged.
- Added large permanent surface identities for `INGREDIENTS`, `TOOLS`, `MIXING`, `ROLL + CUT`, and `LINE + LOAD`; replaced the duplicate tiny tool-mat caption and enlarged the existing fascia labels. These labels remain present independently of the tutorial outline.
- Added the explicit rerunnable builder `Assets/BakeIT/Editor/TeacherTestVisibilityPolish.cs` and the material `Assets/BakeIT/Materials/MeasuringTool_Copper.mat`. The scene was saved after structural and rendered visual review.

## 2026-09-12 Baking-tool rack naming and return assistance

- Renamed the mixed wall rack from `MEASURING TOOLS` to `BAKING TOOLS`, since it stores the Whisk as well as the six measuring vessels.
- Added the permanent rack instruction `AIM AT RACK + PRESS E TO RETURN` so first-time players can discover that tools may be rehung.
- Expanded each matching hook's close-range return radius from `.34 m` to `.55 m` and added crosshair-assisted return within `2.5 m`, using a `.22 m` aim tolerance around the exact hook.
- Exact tool-to-hook matching remains mandatory. Aiming at a different hook does not accept or release the held tool.
- Runtime behavior is implemented in `Assets/BakeIT/Scripts/Interactions/ToolHangingSlot.cs`; saved scene values and labels are reapplied by `Assets/BakeIT/Editor/TeacherTestVisibilityPolish.cs`.

## 2026-09-12 Cookie usability and guide consistency pass initiated

- User reported inverted/default held poses across carryable items, a missing Baking Soda presentation, confusing numbered guide headers and compressed tool copy, reversed/skewed Butter lettering, nine generated cookies despite six-cookie copy, absent current-step guidance, overly aggressive stuck-object distance reset, and an oven that begins at 180 C.
- Intended scope: audit existing `PickupPose` data and make held fronts upright/readable; add a local Baking Soda visual without changing its recipe key or measuring trigger; remove phase counters from player-facing guide labels and split tool preparation into progressive screens; repair modeled Butter lettering on both sides; enforce the approved six-cookie yield; restore the correct tray-stage outline; recover held distance by one scroll step instead of jumping to the minimum; and start the oven selection/current display at 0 C.
- Preserve the existing recipe definitions, exact measuring capacities, pickup/right-click rotation controls, physical placement systems, five-second prototype preheat/bake duration, and Brownie development boundary except where the requested consistency fixes require serialized value changes.
- Scene: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`. Work performed by Codex main agent through local file edits and the connected Unity Editor; no delegation or commit.

## 2026-09-12 Cookie usability and guide consistency pass completed

- Changed the no-override pickup pose to face package fronts toward the player while preserving every explicit `PickupPose`; the live sweep passed all 27 active preparation items. Stuck recovery now retreats exactly one `.20 m` scroll step instead of jumping to the `.60 m` minimum.
- Replaced the Baking Soda cylinder placeholder with a collider-free open carton visual containing visible white powder, opened flaps, blue/orange package details, and readable front/back `BAKING SODA` labels. The authoritative `BakingSoda` source trigger and root interaction components remain unchanged.
- Removed recipe-phase fractions/counters from Cookie and Brownie stage headers. Cookie tool preparation now introduces the 1 Cup first, then uses a second screen with separate rows for the 1/2 Cup, 1 Teaspoon, 1/2 Teaspoon, 1/4 Teaspoon, and Whisk.
- Extended current-step guidance beyond mise en place through measuring, mixing, dough transfer, rolling, cutting, tray collection, parchment, portion loading, oven preheat/loading, and baking. Held targets are still suppressed so outlines do not remain on the object in hand.
- Corrected wrapped Butter lettering on both long sides and made the `R` leg vertical. Enforced six portions by setting the Cookie recipe and board minimum/target/maximum yield to six.
- Oven selected and current temperatures now begin at `0 C`. Preheat is rejected until a target is selected; the first `+` input chooses the minimum `100 C`, after which normal 10-degree steps apply.
- Edit Mode structure passed: Baking Soda has 12 renderer parts, zero visual colliders, exact `BakingSoda` source identity, board/recipe maximum six, and saved oven target/ambient zero. Final Console query reported zero errors and zero warnings.

## 2026-09-12 Brownie pan and oven stages initiated

- User approved implementing the next two Brownie stages while raw-batter and completed-brownie appearance references are still being gathered: pour the completed batter into the parchment-lined deep pan, spread it with the Rubber Spatula, then preheat/load/bake it in the existing oven.
- Gameplay will use clearly labeled temporary batter and baked-slab geometry. The later reference pass must be able to replace those visuals without rewriting the pan, spreading, oven, guide, or result-state logic.
- Preserve the approved `Brownies.asset` values: four spreading passes, an 8-second prototype bake, 170-180 C successful range, and underbaked/perfect/overcooked/burnt outcome names. Preserve the existing Cookie tray path and oven behavior.
- Extend the Brownie guide and yellow contour beyond mixing through parchment, batter transfer, spreading, preheating, oven loading, and baking. Slicing remains the next explicit development boundary.
- Scene: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`. Work performed by Codex main agent through local file edits and the connected Unity Editor; no delegation or commit.

## 2026-09-12 Brownie pan and oven stages completed

- Added `BrowniePanReceiver` to the existing deep pan with a dedicated parchment anchor, batter intake volume, four-pass Rubber Spatula tracking, and a replaceable temporary Brownie slab. Added the marker-only `BrownieSpreadingTool` to the existing spatula.
- Completed Brownie batter can transfer only from the held, sufficiently tilted Brownie Mixing Bowl and is consumed once. Parchment and batter remain separate prerequisites; the pan is oven-ready only after all four spreading passes.
- Generalized the existing bake zone/controller to support complete Cookie trays and ready Brownie pans. The active recipe supplies its own duration, temperature band, burnt threshold, and result names. Brownies use 8 seconds and 170-180 C; the Cookie recipe remains 5 seconds and 170-190 C.
- Extended the Brownie guide and yellow contour through line, pour, spread, preheat, load, and bake. After any Brownie bake result, the outline clears and the guide stops at `NEXT PART STILL IN DEVELOPMENT` for cooling/slicing instead of jumping backward to oven loading.
- Added rerunnable scene setup at `Assets/BakeIT/Editor/BrowniePanStageSetup.cs` and the replaceable `Assets/BakeIT/Materials/Brownie_Placeholder.mat`. Existing pan/spatula transforms and Cookie objects were preserved.
- Focused runtime QA passed parchment, batter, spread, oven registration/start, the 8-second Perfect result, all four Brownie result names, and Cookie recipe-data regression (`BROWNIE_RUNTIME_QA PASS=True`). Final Edit Mode audit compiled cleanly and reported Play Mode stopped, receiver/marker present, and scene saved/clean.
- Final Unity Console queries reported zero errors and zero warnings.
- Acceptance limitation: deterministic QA called the production batter-transfer endpoint and set the completed four-pass state directly after validating prerequisites. A normal player still needs to validate the continuous bowl-tilt hold, the feel of four physical spatula strokes, oven carrying, guide clarity, and placeholder appearance.

## 2026-09-13 Brownie food-art direction and bowl-volume pass initiated

- User supplied cooked Brownie, thick raw batter, and full mixing-bowl photo references after a suitable model sheet could not be found. The existing deep Brownie pan is approved and must remain unchanged.
- Intended work: create a preview-only three-option BakeIT-style concept sheet from the supplied photos; enlarge the existing Brownie bowl contents so the completed mixture reads as a substantial batch; preserve ingredient quantities, mixing progression, bowl/pan physics, and all newly implemented pan/oven logic.
- The concept sheet is for user selection and will not be imported as final game art. Pan batter/slab geometry remains replaceable until the user chooses option A, B, C, or a combination.
- Scene: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`. Work performed by Codex main agent through local edits, the built-in image-generation workflow, and the connected Unity Editor; no delegation or commit.

## 2026-09-13 Brownie food-art direction and bowl-volume pass completed

- Generated one preview-only three-column concept sheet from the user's three reference photos using the built-in image-generation workflow. Each A/B/C option shows the fuller green bowl, raw batter in the unchanged silver pan, and an unsliced baked slab; the concepts vary smoothness, walnut density, and crack strength.
- No concept was imported as final Unity art because user selection is still pending. The existing Brownie pan and its gameplay receiver remain unchanged.
- Increased `Brownie Batter In Bowl` saved local position from `(0,.032,0)` to `(0,.062,0)` and local scale from `(.074,.0035,.074)` to `(.135,.02,.135)`. World-space presentation is approximately `.19 x .056 x .19 m` inside the approximately `.31 x .13 x .31 m` green bowl.
- Play Mode retained the new transform after `PourableIngredientReceiver.Awake`; the focused render confirmed the mound remains inside the bowl. Edit Mode returned with the scene clean and zero temporary preview objects.
- Failed preview attempt: Unity's `Camera.Capture` helper rounded/rejected the Unity 6 entity ID and logged one tool-side error. A direct temporary-camera RenderTexture capture succeeded instead. The temporary runtime camera, lights, floor, and moved bowl were discarded on Play Mode exit; the diagnostic was cleared from Unity's Console afterward.
- Final Unity Console query reported zero errors and zero warnings. Evidence render: `BakeIT_Notes/evidence/brownie-bowl-fuller-2026-09-13.png`.
- Remaining follow-up: user selects A, B, C, or a combination; then build the matching lightweight raw-pan and baked-slab geometry and replace the placeholder without changing pan/spreading/oven logic.
## 2026-09-13 Selected Brownie food-art implementation initiated

- Requested objective: Implement the user's selected hybrid direction: B1 fuller rustic bowl batter, B2 thick rustic raw pan batter, and A3 smoother baked Brownie surface.
- Scene: `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- Intended work: preserve the approved Brownie Pan and all recipe/interaction state; add replaceable procedural food-surface meshes, walnut accents, and a presentation-only state bridge so baking swaps raw and baked art without changing gameplay requirements.
- Explicit constraints: no chocolate chips, no pan replacement, no ingredient-count changes, and no new recipe stages.
- Responsible agent: Codex through Unity MCP and repository edits.
- Failed attempt: the first idempotent setup run stopped before scene mutation because `Assets/BakeIT/Models` did not exist and the folder bootstrap began one level too deep. The bootstrap was corrected to create `Models`, then `Procedural`, then `Brownies`; already-created material assets are intentionally reused on retry.
- Visual-QA correction: the first isolated Play Mode render revealed that B1's circular surface triangles faced downward, hiding the batter while leaving the walnut details visible. The circular mesh winding was reversed before acceptance testing; B2 and A3 state selection already passed.
- Asset-import correction: the first corrected regeneration completed but Unity warned that the three mesh object names did not match their `.asset` filenames. Internal mesh names were aligned to `Brownie_B1_Bowl`, `Brownie_B2_RawPan`, and `Brownie_A3_BakedPan` before the final regeneration.
- B1 containment correction: runtime bounds showed the first custom mound at approximately `.38 m` wide inside an approximately `.31 m` bowl and slightly below the opaque inner lip. The selected mesh keeps its normalized radius, while the saved visual transform changes to local position `(0,.075,0)` and scale `(.105,.03,.105)`, producing an approximately `.294 m` opening fill with the center crest only slightly above the rim.
- B1 visibility adjustment: the corrected-size render still showed depth competition with the bowl asset's opaque inner insert. The final local position/scale is `(0,.088,0)` / `(.105,.032,.105)`, lifting the surface edge above that insert while preserving the `.294 m` horizontal footprint.
- B1 face-rendering correction: an isolated render proved the radial mesh was culled by Unity despite upward recalculated normals. The shared raw Brownie food material now explicitly renders both faces (`_Cull = 0`); this fixes B1 and is safe for the small non-colliding B1/B2 presentation meshes.

## 2026-09-13 Selected Brownie food-art implementation completed

- Implemented the approved hybrid: B1 fuller rustic bowl, B2 thick raw pan batter, and A3 smoother baked top. The existing deep silver pan was not replaced.
- Created procedural mesh assets `Brownie_B1_Bowl.asset`, `Brownie_B2_RawPan.asset`, and `Brownie_A3_BakedPan.asset`; created raw, baked, walnut, and crack materials under `Assets/BakeIT/Materials`.
- Added collider-free detail hierarchies: 12 walnut clusters for B1, 10 for B2, and 8 walnuts plus 3 crack groups for A3.
- Added `BrownieFoodVisual.cs`. `PourableIngredientReceiver` tints only the batter surface, leaving walnuts distinct; `BrowniePanReceiver` swaps the B2 mesh/details to A3 for successful/overcooked/burnt visual updates while preserving the existing outcome state.
- Inspector changes: B1 final local position/scale `(0,.088,0)` / `(.105,.032,.105)`; pan unspread/spread scales changed from `(.16,.0035,.10)` / `(.27,.0025,.17)` to `(.18,.018,.12)` / `(.27,.012,.17)`.
- Validation: Edit Mode structural audit passed and saved cleanly. Final Play Mode result: `BROWNIE_ART_ACCEPTANCE pass=True; B1=True; B2raw=True; A3baked=True`.
- Failed attempts/corrections: fixed a missing parent-folder bootstrap, mesh asset-name warnings, B1 triangle/culling visibility, and a misleading QA capture that left the mutually exclusive butter surface active. All are documented above.
- Evidence: `BakeIT_Notes/evidence/brownie-B1-bowl-final.png` and `BakeIT_Notes/evidence/brownie-B1-B2-A3-playmode.png`.
- Cleanup note: two unreferenced diagnostic captures, `b1-isolated-debug.png` and `b1-cull-debug.png`, remain under `BakeIT_Notes/evidence`; shell deletion was blocked by the workspace safety layer and `apply_patch` cannot decode binary PNGs. They are not imported by Unity or used by gameplay.
- Remaining work: human acceptance of the selected surfaces in the full player recipe, later mesh polish if requested, then the still-unimplemented cooling and slicing stages.
- Responsible agent: Codex through repository edits and Unity MCP.
## 2026-09-13 — Brownie ingredient readability, batter progression, and pan-liner correction

- **Requested objective:** Add readable names to Brownie ingredients, repair the malformed Baking Soda presentation, restore staged batter progression, delay walnuts until all six whisk passes, make the batter fuller and visibly dark brown rather than black, replace heart-like walnuts, stop transferred batter and parchment from floating, and extend parchment coverage up the deep pan sides.
- **Scene in scope:** `Assets/BakeIT/Scenes/Kitchen Prototype.unity`.
- **Planned implementation:** Extend the existing ingredient, `PourableIngredientReceiver`, `BrownieFoodVisual`, `BrowniePanReceiver`, and `BrowniePanStageSetup` systems; preserve recipe quantities and the existing four base plus two finishing whisk-pass requirement.
- **Initial inspection:** The current bowl art exposes its full-width raw surface and walnut detail root as soon as any non-butter ingredient is accepted; the procedural batter meshes are open surfaces; the parchment is parented as a visible rigidbody with only a flat scale; and the Baking Soda root already has a dedicated carton builder that needs its scene instance audited/reapplied.
- **Pre-change Console:** 12 repeated `OvenBakeZone.OnTriggerExit` `NullReferenceException` entries from earlier play-session teardown were present. They will be reproduced/investigated during this correction and must not be hidden.
- **Files changed:** `IngredientNameLabelSetup.cs` (new), `BakingSodaAssetSetup.cs`, `BrowniePanStageSetup.cs`, `PourableIngredientReceiver.cs`, `BrownieFoodVisual.cs`, `BrowniePanReceiver.cs`, `OvenBakeZone.cs`, the generated Baking Soda prefab, raw Brownie material/three procedural Brownie meshes, and `Kitchen Prototype.unity`.
- **Ingredient readability:** Added small, depth-tested, cream-backed front nameplates to Sugar, Melted Butter, Cocoa, Vanilla, Baking Powder, Salt, Walnuts, and Chocolate Chips. Flour retains its existing modeled wordmark; Baking Soda uses its corrected dedicated carton label; Eggs remain visually identifiable and retain hover names rather than carrying artificial text.
- **Baking Soda correction:** Rebuilt the carton label with a `.00235` TextMesh character size and corrected front/back rotations. The isolated render showed one centered `BAKING SODA` label rather than the prior stacked/through-wall lettering.
- **Batter progression:** `BrownieFoodVisual` now scales from a saved minimum fill to the full B1 pose while keeping the lower surface anchored. `PourableIngredientReceiver` supplies ingredient progress and does not reveal raw walnut details until the completed-batter gate. The approved four base plus two finishing whisk passes remain unchanged.
- **Color and geometry:** Unmixed batter is warm brown `(.46,.17,.055,1)` and mixed batter is dark brown `(.34,.105,.035,1)`. B1 now includes a closed side skirt/bottom; B2/A3 include perimeter walls and a bottom, preventing hollow silhouettes. Walnut clusters use three asymmetric lobes with varied pose rather than paired heart-shaped lobes.
- **Pan/liner behavior:** Added `Fitted Parchment Liner Visual` with one bottom and four raised side pieces. On acceptance, the physical sheet is frozen, collision-disabled, renderer-hidden, and parented exactly to `Brownie Parchment Anchor`; reset restores its original hierarchy, physics, colliders, renderer states, and transform. Raw batter sits `0.0024 m` above the fitted liner instead of floating.
- **Incidental defect fixed:** `OvenBakeZone.OnTriggerExit` now null-guards tray and pan references, removing the repeated teardown `NullReferenceException` observed before this pass.
- **Validation:** Final Play Mode assertion: `FINAL_BROWNIE_ACCEPTANCE_PASS early=0.153 full=0.294 color=RGBA(0.340,0.105,0.035,1.000) linerParts=5 anchorError=0.0000 batterGap=0.0024`. It also verified no early walnuts, three-lobe walnut clusters, closed bowl/pan meshes, parchment acceptance, pan-relative stability, and complete reset restoration.
- **Failed attempts/corrections:** Renamed a duplicate local `outerStart` variable after one compile failure; corrected the receiver lookup from parent-only to the bowl's sibling hierarchy; corrected diagnostic scripts that initially used an ambiguous `Mesh` type and the same incorrect lookup. A Unity camera helper attempt also failed, so visual QA used isolated scene views instead. None remained in the final product state.
- **Final state:** Play Mode stopped, scene saved, and the final Console query reported zero errors and zero warnings.
- **Known limitation:** Full natural mouse-driven traversal and aesthetic acceptance still belong to the player's next in-game retest. Cooling and slicing into 12 Brownie squares remain later milestones.
- **Status:** Complete for the reported defects.
- **Performed by:** Codex via filesystem and Unity MCP.

## 2026-09-15 08:59 +08:00 — Documentation reconciliation started

- Objective: User selected D (reconcile notes), will perform A personally and send feedback, then wants C (finish Brownies) before B (Cupcake planning).
- Planned files: Current Project Status.md, cookie-usability-acceptance-checklist.md, brownies.md, known-issues.md, design-decisions.md, test-log.md, change-documentation.md, ingredient-measuring-system.md, recipe-selection-and-guide-ui.md, recipe-data-system.md, grading-and-waste-tracking.md.
- Scope: documentation only; preserve historical content and existing uncommitted work. No scenes, GameObjects, components, Inspector values, assets, packages, settings, or runtime behavior will change.
- Evidence: September 14 test/development entries, current recipe assets, BrowniePanReceiver and session guide, September 6 CodexReview test XML.
- Validation planned: documentation diff, historical preservation, links and current formula/control consistency. No Play Mode/Console check for this documentation-only change.
- Responsible: Codex; no delegation.

## 2026-09-15 09:01 +08:00 — Documentation reconciliation completed

- Objective/result: completed D. User performs A and sends feedback; agreed C finishing work for Brownies precedes B Cupcake planning.
- Files changed in this pass: Current Project Status.md, cookie-usability-acceptance-checklist.md, brownies.md, known-issues.md, ingredient-measuring-system.md, recipe-selection-and-guide-ui.md, recipe-data-system.md, grading-and-waste-tracking.md, design-decisions.md, test-log.md, change-documentation.md.
- Current status, Brownie recipe and checklist now place reconciled information first with original content preserved in explicit historical sections. The existing cookie-checklist filename now hosts one active checklist for both recipes.
- Corrected current interpretation: Brownies selectable through five cuts/twelve squares; user acceptance outstanding; September 14 perfect-bake presentation was injected; automatic scoop replacement and small-chip-spoon expectations are superseded; tutorial precedes selection; restart is a three-second R hold.
- Scope decisions: cooling and individual serving are still specific follow-up choices. Broader September 30 scheduling discrepancy is explicit, without inventing new commitments. Existing BakeIT_Notes remains authoritative; no duplicate note tree.
- Scene, GameObjects, prefabs, components, Inspector values, materials/models/audio, code behavior, packages/settings: none changed. Existing uncommitted evidence images and notes preserved.
- Validation: source/data consistency review, seven active local links, original-history preservation and Git diff/whitespace checks passed. See matching test-log entry for tool diagnostics, line-ending notices and unavailable cm CLI.
- Failed attempts: two tool orchestration syntax/interpolation failures before execution, corrected without project effects. No rollback.
- Limits: no new Play Mode/Console/device checks; no new runtime bug claims. User playtest and art acceptance pending; Unity Version Control CLI review unavailable.
- Next: await user's A observations, record actual results/issues, then agree and execute C before B.
- Responsible: Codex through filesystem tooling; no delegation, commit or check-in.

## 2026-09-15 09:20 +08:00 — User playtest correction and serving follow-up initiated

- User findings: flipped Butter; cramped dry storage, tilted/wall-clipping Vanilla and intermittent Baking Soda pickup; chip-transfer instruction omits ingredient; melted-butter cup disliked with backwards/below-ground name; Brownie batter overflow; redundant return-bowl step; overly regular baked walnuts.
- Requested extensions: slightly taller organized dry storage; return to recipe selection distinct from restart; exact-ingredient green poster text only in guided/tutorial practice; final plating stage before grading. Free/Challenge modes are future work, not requested for full implementation now.
- Plan: inspect/fix existing objects, colliders, poses, guide/session, Brownie presentation and food handling. Extend existing systems; retain recipe quantities and user layout outside requested storage/prop changes. Clarifying serving quantity and approved grading rubric asynchronously while independent fixes proceed.
- Baseline: Kitchen Prototype, Edit Mode, clean saved scene; Console zero errors/warnings. Existing filesystem changes from earlier work preserved. Eight user screenshots are retained in evidence with this date.
- Files/components/Inspector changes: to be recorded with actual before/after audit. No new recipe, VR package, cloud system or unrelated mode implementation planned.
- Testing: reproduce pickup/visual/guide defects; verify normal/incorrect input, physics, reset and recipe return in Play Mode; inspect renders and Console. Full human feel remains for user retest.
- Responsible: Codex; no delegation.

## 2026-09-15 10:00 +08 — Player feedback implementation (Codex)

Scene: Kitchen Prototype; Unity 6000.5.0f1. Objective: address the eight screenshot reports, add smaller serving and recipe-selection return, and defer grading per the user's answer. The initial scope entry above preceded implementation.

### Files and scene changes

- New runtime scripts and Unity metas: ServingPortion.cs and ServingPlateReceiver.cs. New Editor-only authoring/test helpers and metas: PlayerFeedbackSetup.cs and PlayerFeedbackQA.cs. All under Assets/BakeIT. Added Generated/Serving and Pouring with Pouring Jug mesh, Ivory Ceramic and Golden Melted Butter materials and metas. Added serving-and-plating-system.md plus new evidence PNGs; retained all original screenshots under evidence/player-feedback-2026-09-15-1.png through -8.png.
- Extended CookieRecipeSessionController, RecipeSelectionButton and TutorialHighlightDirector for exact guided-paper progress, unambiguous chip row, finished-batter guidance, M hold and serving-based completion. Extended BrownieFoodVisual for individual pieces and per-batch walnut variation. BrowniePanStageSetup uses the contained bowl fill. Replaced deprecated unsorted FindObjectsByType overloads in touched Editor helpers with their equivalent supported overload.
- Kitchen Prototype saved with taller cabinet, aligned pantry visuals/colliders, explicit butter pose and corrected lettering, replacement melted-butter jug, contained batter fill, poster bindings and new serving plate. The original wrapped-butter scene prefab instance was unpacked to group/mirror 44 letter strokes; its generated prefab asset was not overwritten. Old melted-butter renderer/name remain disabled. Its pour receiver and ingredient identity are reused.
- Cabinet local scale (.72,1.18,1) -> (.72,1.40,1). Pantry positions and collider policy are recorded in kitchen-layout-and-storage.md; sources remain kinematic until picked up. Baking Soda visual local X +.10 -> 0; Vanilla +.05 -> 0, with upright roots and fitted body colliders. Open lids are excluded from the pickup bounds.
- Butter held Euler fallback (0,180,0) -> explicit (0,0,0), with corrected label geometry on both faces. Jug held Euler (0,180,0); fill local position (0,.128,0), scale (.13,.004,.13); spout point (0,.178,.096). Label center (0,.085,.083), character size .0032; label plate center Z .081. Hollow vessel profile separates inner/outer wall and spout; regenerated mesh buffers are explicitly updated so the saved asset and renderer agree.
- Brownie full-fill local position (0,.088,0) -> (0,.065,0); scale (.105,.032,.105) -> (.088,.025,.088). Guide/highlight MIX gate stops once batter is complete.
- Serving Plate Root at (2.42,1.172,5.90), unit scale; licensed plate visual normalized to .36 m width. Rigidbody mass .4, PreparationItem, PickupPose, hover name, support collider and acceptance trigger added. Small servings default to 3 Cookies/4 Brownie squares. Final slot geometry and collider values are in serving-and-plating-system.md. Placement was moved inward from the initial X=2.70 to avoid the countertop edge.
- No project settings or package changes. Application.runInBackground was enabled only in the Play Mode test driver. No rubric, score, new recipe, cooling stage or future mode UI was added. Existing shader/material and old Brownie screenshot changes present before this request were preserved.

### Failures, corrections and limits

The test fixture initially used the wrong input-state buffer and too-low pour target; corrected to queued input/update and receiver collider center. An initial knife fixture forced a bare tool kinematic and prevented later pickup; corrected the fixture rather than changing normal pickup rules. Some dynamic commands failed compilation (Mesh namespace ambiguity, unavailable input overload, trying to concatenate a void pickup return) or raced a domain reload; corrected/retried with no saved test state. An initial butter regrouping could not reparent prefab children; unpacking only the scene visual fixed this. Initial plate spacing/edge position and jug-label renders needed refinement; final renders were rechecked. CopySerialized mesh replacement left stale visual buffers during the jug iterations; explicit mesh buffer updates resolved the rendered clipping.

Final focused recipe runs passed; separate visual checks confirmed readable held butter and jug. Full natural mouse/keyboard acceptance, plate-carry feel and subjective art approval remain open. Keep C ahead of B; grading remains deferred until every dish and VR base-game completion/data collection. Git changes reviewed; no commit or remote write performed. Unity Version Control CLI cm is not on PATH, so native pending-change review was unavailable; Git review was used. Unity-generated blank m_Name fields produce trailing-space notices in the scene YAML; source/doc diff checks are separate.

### Final verification clarification

The final standalone serving guard fixture had no selected recipe; it confirms the pre-selection guard only. Active-recipe raw/wrong-recipe branches were source reviewed, not independently exercised by that fixture. See the test-log clarification. Final saved scene is clean in Edit Mode with Console errors=0/warnings=0.

## 2026-09-15 follow-up playtest — work initiated (Codex)

User reached a successful Brownie serving and reported abrupt/small-looking melted-butter pour, jug/bowl clipping, no visible walnut fall, bright/lingering bowl glow after transfer, and plated brownies clipping through the dish. User approved replacing four Brownies with a choice of two or three, arranged as a small stack like the supplied photograph. Implement smoother full-cup pouring, visible walnut transfer, appropriate empty-bowl guidance/material, and bounds-based stacked plating. Preserve Cookies and deferred grading. Baseline: Kitchen Prototype was in Play Mode, scene clean, Console errors=0/warnings=0; inspected current completed state before exiting Play Mode. Existing pending changes belong to prior work and are preserved. Reference screenshots are evidence only.

## 2026-09-15 10:34 +08 — Follow-up corrections completed (Codex)

Scene: Kitchen Prototype, Unity 6000.5.0f1. User reached a successfully baked four-piece Brownie serving, supplied three issue screenshots and a stacked-food reference, then approved two/three servings. This is additional player acceptance evidence, with defects recorded in FB-09 through FB-13.

Files changed in this follow-up: Scripts/Ingredients/PourableIngredient.cs, PourableIngredientReceiver.cs, MeasureTransferVisual.cs and MeasuringScoop.cs; Scripts/Interactions/ServingPlateReceiver.cs; Scripts/Recipes/CookieRecipeSessionController.cs and TutorialHighlightDirector.cs; Editor/PlayerFeedbackSetup.cs, PlayerFeedbackQA.cs and BrowniePanStageSetup.cs; Kitchen Prototype.unity. New asset: Generated/Serving and Pouring/Sage Mixing Bowl.mat plus meta. Original model/material assets and other pending work were preserved. Notes updated: current tracker, Brownies, serving, measuring, bowl/mixing, guide, decisions, issues, acceptance checklist, development and test logs. New evidence uses player-followup/feedback-followup prefixes; earlier evidence was not overwritten.

Behavior and values:
- Butter threshold 75 -> 55 degrees; sustained valid pour .35 -> 1.6 seconds; ray reach .35 -> .40 m. Continuous golden LineRenderer width .009 -> .006 from spout to receiver. Visible fill lowers from saved full position toward local Y=.035 and tapers to 84% width. Invalid/level pose pauses flow, same-destination progress resumes; accepted full-cup quantity commits once at completion. Rejected destinations do not preview a stream. Uses an unlit golden visual material for readable color, no light or bloom. No spill-volume accounting added.
- Walnut transfer: 12 tiny generic particles -> 24 visible tumbling existing walnut meshes, .008-.011 m widths, .42-second flights staggered .020 seconds, total effect about one second. Accepted walnuts become visible on the batter before finishing passes. Other measures keep their existing short timing and sizes.
- Full batter local position (0,.065,0) -> (0,.073,0); scale (.088,.025,.088) -> (.075,.018,.075). This addresses tapered walls, not just rim height. Brownie bowl uses new Sage material RGB(.24,.36,.21), smoothness .22, emission disabled. Shared Panda atlas material unchanged.
- Empty transferred bowl skips preparation targeting and obsolete placement correction. Next pan tasks remain highlighted. Ordinary hover feedback is independent.
- Brownie target 4 -> default 2, selectable 2/3 with number keys at plating; Cookies remain 3. Reducing below current count requires removing a piece. Stack has small center offsets and -8/12 degree yaw; actual world scale remains unchanged. Contact uses primary mesh bounds, plate-space coordinates and explicit body poses. Plated bodies are kinematic, interpolation None. Receiver LateUpdate executes after SmoothPlacement to retain the stack through plate movement.
- Plate central well measured Y=.0145 (outer rim .0384); surface contact is .016. Existing plate position/visual/colliders unchanged. Layer clearance .0015 above crust bounds; decorative kernels do not lift the next brownie and may become concealed within the upper piece.

Failed/refined approaches: one patch context mismatch made no edit and was reapplied against the actual method name. First integration passed logic but side render exposed floating gaps from using outer-rim/all-decoration bounds. Changed to measured central well and primary crust bounds; added loaded-plate carry/rotation/settle verification and reran both recipes. Retained all test history. Final focused run PASS; clean Console and saved Edit Mode confirmed. No package/project-setting change, commit, remote write, grading or cooling system. Full natural mouse/keyboard feel and visual preference remain for user retest. Git source/doc diff checks pass; normal Unity YAML empty-name trailing spaces remain. Native cm CLI remains unavailable as recorded previously.

## 2026-09-15 10:38 +08 — Cupcake source selection (Codex)

User selected Life, Love and Sugar's Easy Homemade Vanilla Cupcakes recipe as the Cupcake basis. Read the linked current recipe card and compared the existing recipe data/notes. Record its source, exact formula and method facts, distinguish gameplay adaptations from published instructions, and update the tracker. Frosting preparation versus a prepared supply is the remaining requested preference. This is source selection/design documentation, not a claim that Cupcake gameplay exists; no scene/runtime change or Play Mode test is needed for this documentation-only work.

### Cupcake source selection recorded

Created BakeIT_Notes/cupcakes.md; updated the current tracker, design decisions, recipe-data notes and test log. The source formula is selected; game adaptations are proposals. No runtime code, RecipeDefinition asset, material, package or scene was changed. Follow-up: resolve frosting preparation preference and define staged Cupcake workflow. Documentation diff checked.

## 2026-09-15 10:45 +08 — Cupcake difficulty and frosting decision (Codex)

User confirmed buttercream must be made from ingredients and identified Cupcakes as the Hard recipe. They asked whether the source steps were followed. Rechecked the published recipe-card instructions against the planning notes. Expand the ordered batter/frosting sequence and record cooling/doneness requirements; distinguish planned source fidelity from unimplemented gameplay. This changes design documentation only, not Unity scene/runtime or tests.

Completed documentation update: cupcakes.md, Current Project Status.md, design-decisions.md, test-log.md and this log. Source instructions are recorded as recipe requirements; timed simulation/interaction adaptations remain implementation work. No files removed, no scene/assets/packages changed. Markdown diff validation passed.

## 2026-09-15 — Cupcake foundation and staged batter implementation started (Codex)

User authorized continuing the announced ingredients/equipment foundation and staged batter milestone. Scene Kitchen Prototype starts clean in Edit Mode; Console errors=0/warnings=0. Existing recipe/receiver/measuring/session code and source notes inspected. Reuse licensed milk/oil/bowl/mixer assets; extend the existing receiver and recipe data for ordered mixing rather than duplicating pickup/measuring. Expose a clearly labeled Cupcake batter-practice branch, ending at batter ready with baking/frosting explicitly pending. Preserve the full confirmed Hard/from-scratch recipe scope for subsequent stages. No grading or future-mode implementation. Existing pending changes retained.

### Cupcake foundation implementation details

Added Cupcakes.asset, RecipeMixingPhase, MixingSequence and StagedMixingVisual. Extended RecipeDefinition with appended tablespoon role and explicit batter-practice boundary; extended shared receivers/pour source for ordered phases and two dry-mixture halves. Added a separate Scrape action to MixingTool, tutorial guide/paper progress, recipe-specific bowl selection, and the incomplete-recipe oven guard. Milk/oil fill colors and dry powder transfer reuse existing presentation infrastructure. No prepared frosting, grading, new game mode, package installation or external asset download.

CupcakeFoundationSetup authors the conditional supplies, licensed models, two bowl rests, 84 g butter portion, eggs, tablespoon/rack return, scraper, materials, recipe data and revised poster, then saves the scene/assets. CupcakeFoundationQA provides controlled Play Mode integration checks. Existing PlayerFeedbackQA now explicitly resolves the Brownie receiver and excludes inactive Cupcake fixture eggs; its configurable evidence prefix preserves earlier screenshots. Runtime/UI/data notes and current tracker updated. Cupcake test runs and presentation iterations are recorded in test-log.md. Existing user/prior-turn pending changes were preserved; no commit made.

Final UI refinement: Cupcake guide rows measure wrapped text height, the stage prefix is shortened, and the current bowl's rest row reflects its real placement. Actual Game-view capture verified readable creaming instructions; placed/picked-up/returned checkbox checks passed. No Cookie/Brownie row sizing change. Source/editor/Markdown diff checks pass; Unity scene serialization emits its usual blank YAML property trailing spaces, so no unrelated scene-wide whitespace rewrite was performed.

Cupcake foundation milestone completed and saved. Final Cupcake integration PASS; existing Cookie/Brownie regression PASS; final Console errors=0/warnings=0; Edit Mode scene dirty=false and missing scripts=0. Cupcake poster is selectable as Hard batter practice only. Next implementation: tray/liners, filling, bake/doneness and cooling, then from-scratch buttercream/piping/serving. Natural player testing and grading remain open/deferred respectively. No user approval or new asset upload was needed for this milestone.

## 2026-09-15 — Cupcake tray, liners and baking continuation started (Codex)

User asked for the tray with individual cupcake slots, then authorized proceeding after the stated tray/liners, filling, baking and cooling sequence. Inspect existing recipe/bowl/pour, parchment dispensing, pickup/placement and oven code before extending. No matching cupcake/muffin/liner model exists in the assets inspected. Kitchen Prototype starts in Edit Mode, dirty=false; Console errors=0/warnings=0. Build a purpose-made recessed 4-by-3 pan and fluted liners; reuse pickup, dispenser, pour, placement and oven systems. Connect lining, twelve three-quarter fills, bake/doneness and individual rack cooling. Preserve confirmed from-scratch frosting/piping/serving as the following unfinished stage, without grading. Use 180 C as the existing ten-degree-control approximation of source 176 C; ten-second bake/cooling prototype timers are game tuning, not source timings. Preserve all existing pending changes.

## 2026-09-15 — Cupcake tray and baking extension implemented

User authorization: after asking whether a tray with individual cupcake slots existed, the user said to proceed with the proposed tray/liners/filling/baking/cooling milestone. The selected formula and confirmed from-scratch frosting requirement remain unchanged.

Added CupcakeTraySetup and persisted meshes/materials under `Assets/BakeIT/Generated/Cupcake Tray`, plus scene equipment: one 4-by-3 recessed tray with side grips, twelve separate fluted liners dispensed from a finite stack, a twelve-position wire cooling rack and a wooden doneness tester. Existing scene objects and prior pending work were retained. The Cupcake foundation builder invokes the extension so it survives a deliberate rebuild.

New runtime components: CupcakeBatch, CupcakeSlot, CupcakePortion and CupcakeDonenessProbe. New shared contract: IPourDestination. Extended existing PourableIngredient/receiver, StagedMixingVisual, MixingSequence, ParchmentDispenser, PickupController, OvenBakeZone/Controller, session guide and tutorial target. The Cupcake source drains across twelve independently persistent progressive fills; each stops at three-quarters. Reused release placement, existing tool controls and oven behavior. The preparation gate requires preheat and twelve liners, then preserves the existing nine batter phases. Filled raw portions cannot be removed; baked portions require a centre tester check before rack transfer.

Configured Cupcakes.asset for a 180 C perfect target, ten-second bake and 220 C burnt threshold. Cooling uses ten seconds per cupcake on the rack. These are explicitly documented prototype adaptations of the source's listed 176 C and 15–18-minute bake. The selector says HARD | BAKING PRACTICE; cooling is a partial endpoint and does not latch full recipe completion or a grade. R replenishes the selected recipe; M returns to selection. Buttercream from ingredients, piping and a smaller serving are next.

Test harness: CupcakeTrayQA extends the existing CupcakeFoundationQA sequence; test outcomes, fixture failures, corrections and visual evidence are recorded in test-log.md. Updated cupcakes/current status and the relevant tray, oven, measuring, recipe data, guide/session, decision and known-issue notes. No commit was created.

Final verification at 2026-09-15 13:02 +08: Cupcake integration PASS, Cookie/Brownie regression PASS; final mesh/rack and non-perfect presentation checks passed. Scene/assets saved in Edit Mode, scene not dirty, missing scripts zero, Console errors/warnings zero. Source/editor/Markdown diff checks passed. The bake HUD wording was simplified without changing its ten-second behavior. User-facing next work remains from-scratch buttercream, piping and serving; natural input/art acceptance is still required.
